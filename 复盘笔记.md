# 复盘笔记

> 随手记录的复盘要点，持续补充。系统性复盘见《SmartFactoryMonitor/复盘文档.md》。

## 导航属性（EF Core / 外键关系）

```csharp
public int DeviceId { get; set; }     // ① 外键列，存于数据库
[ForeignKey(nameof(DeviceId))]        // ② 绑定①和③
public Device? Device { get; set; }   // ③ 导航属性，仅内存
```

- 数据库只存 `DeviceId`，`Device` 只是内存里的对象引用，没有对应字段。
- 关联事实靠 `DeviceId` 承载：去掉 `Device` 也能查（SQL JOIN / `FindAsync`）；它是"减少手写关联"的便利层。
- 去掉它则失去 `Include` 直连加载，只能二次查询。
- `Device?` 可空 → LEFT JOIN，未 `Include`/懒加载时为 null。

**一句话**：外键值存库、导航属性只在内存；`DeviceId` 决定能不能查，`Device` 决定方便与否。

## 集合导航属性（一对多）

- 一对多的"多"方用 `ICollection<T>`（如 `ProductionLine.Devices`），"一"方用单个导航（如 `Device.ProductionLine`）。
- 必须用 `ICollection`（可增删）而非 `IEnumerable`（只读）：EF 需向集合 `Add/Remove` 来跟踪关系。
- 用接口不绑定 `List` → 实现可替换、解耦；实际赋 `new List<T>()`。
- 数据库仍只靠"多"方外键（`Device.ProductionLineId`）承载关联，集合导航仅内存便利。

**一句话**：`ICollection<T>` 是 EF 一对多"多"方导航，提供可增删的最小接口以满足 EF 跟踪关系，同时保持实现解耦。

## 唯一索引（`[Index(..., IsUnique = true)]`）

- EF 声明式建唯一索引，迁移时翻译为 SQL（如 `CREATE UNIQUE INDEX ... ON T(Token)`）。
- **索引 ≠ 阻止重复**：索引只加速查找；阻止重复的是**唯一性**（`IsUnique=true`）。
- 作用（一石二鸟）：① 值重复时数据库拒绝写入（防重复，硬保证）；② 按该列查时走索引快速定位。
- 索引**按值定位**（B+树），不是顺序逐条扫描；无索引则只能全表扫描。
- **没有"只唯一、无索引"**：SQL Server/EF 里唯一约束必然以唯一索引为载体，二者是一体两面。
- 区分主键：主键标识行（每表一个、非空）；唯一索引约束列值（可多个）。Token 与 Id 都唯一但语义不同。

**一句话**：唯一索引 = 唯一性约束（挡重复）+ 索引结构（加速查重），"避免重复"靠 `IsUnique`，数据库强制执行。

## 连接数据库的步骤（EF Core + DI）

```
① appsettings.json ConnectionStrings 写连接串
② Program.cs: AddDbContext(opts => opts.UseSqlServer(连接串)) 打包成 DbContextOptions 注册进容器
③ 容器创建 DbContext 时自动注入 options 到构造函数 base(options)
④ 首次执行查询/保存时才真正建立连接（懒连接）
```

- `base(options)` = 调用父类 DbContext 构造函数，把 options 交给父类。
- **options 是 DI 容器自动造的、自动注入的**——你从不在代码里手动 `new DbContext(options)`，只在 Service 构造里"声明需要"，容器负责组装。
- 连接串在 Program.cs 用 `builder.Configuration.GetConnectionString("Default")` 读；DbContext 构造本身不读串，只接收组装好的 options。

**一句话**：连接串写 json → Program 打包 options 进 DI → 容器创建 DbContext 时自动注入并 `base(options)` → 首次查询才真连库；全程 DI 驱动，非手动。

## 级联删除（DeleteBehavior / OnDelete）

- 删父表是否连带删子表，由每条关系的 `DeleteBehavior` 决定：`Cascade`（连带删）/ `Restrict`（有子则拒删父）/ `SetNull`（外键置空，需外键可空）。
- **关键：没配置 ≠ 不删除**。EF 对**必需关系（外键非空）的默认是 `Cascade`**——不写反而级联删。
- 判定规则：子是**重要数据**（报警/历史）→ Restrict；子是**附属数据**（令牌，失父无意义）→ Cascade（默认即满足）。
- 本项目隐患：`DataPoint→Device`、`Device→ProductionLine` 两条未显式配置，走默认级联（删设备删测点 / 删产线删设备），常非预期，建议补 `Restrict`。
- `WithMany()` 空参 = 父侧无对应导航属性；有则 `WithMany(d => d.xxx)`。

**一句话**：级联与否看 `DeleteBehavior`，但"没配"会走默认级联（必需关系）；重要子数据必须显式 `Restrict`，否则删父会误删子。

## 复合索引（多列联合索引）

- 定义：在多个列上联合建索引（如 `(DeviceId, DataPointId, RecordedAt)`），一次索引服务"设备→测点→时间"的查询。
- **代码里没有显式用法**：你写普通 LINQ → EF 翻译 SQL → 数据库优化器自动判断是否走索引。代码侧只需让 WHERE/ORDER BY 列**从第一列开始按顺序带**（最左前缀）。
- 命中判定（最左前缀原则）：从第 1 列起按序带上前缀列即命中；跳过最左列（如只查中间/右列）则不命中 → 全表扫描。
- 索引加速查询，但**每次写库都要同步维护**，会拖慢插入——只给"查询多、写入少"的大表建（历史/报警）。
- 验证是否生效：看执行计划 `Index Seek`=走索引，`Table Scan`=全表扫。
- 项目例子：`DeviceData(DeviceId,DataPointId,RecordedAt)`、`Alarm(DeviceId,Status,OccurredAt)`。

**一句话**：复合索引=大表的快速查找通道，查询列按最左前缀命中则自动走索引（快），否则全表扫（慢）；代码不显式使用，只保证查询列顺序匹配。

## JWT 认证（双 Token / 签发与验证）

```
Token = 头部.载荷.签名
         (算法)  (claims+iss+aud+exp)  (Key对前两段算的HMAC)
```

- **iss/aud/key 都是配置里的普通字符串**：签发时写进载荷（`GenerateAccessToken`），验证时与配置值做字符串比对。iss 防别家系统的票混进来，aud 防发出去的票被别家用。
- **验 key 的本质 = 重算签名**：用同一把 Key 对"头部.载荷"重新算签名，和 Token 第三段比对；相等 → 载荷没被篡改，里面的 iss/aud/exp 才可信。
- **验签是"防伪"，iss/aud 是"防冒用"**：先确认没改过，再确认是本系统签发、发给本客户端。
- **claims → ClaimsPrincipal**：验证通过后载荷每个字段转成 Claim 装进 Principal（`HttpContext.User`），取值用 `principal.FindFirst(ClaimTypes.NameIdentifier)`。
- **双 Token 分工**：AccessToken = 短命通行证（2h，自包含，验证不查库）；RefreshToken = 长命续期凭证（7天，纯随机串，查库验证，一次性，可撤销→强制下线）。
- **刷新流程只验签名**（不验 iss/aud/lifetime）：来刷新的 Token 按定义就是过期的，lifetime 必须关；签名通过已隐式保证 iss/aud 正确。且 refresh 接口无 `[Authorize]`，中间件不拦，服务层手动验。
- **Token 绑定的是用户不是客户端**：B 的 Token 拿到 A 用照样通过（iss/aud/key 对所有客户端相同），服务器无法区分持有人——"持有即身份"，防冒用靠短有效期+HTTPS+可撤销，不靠验证层。
- **多次登录的 Token 并行有效**：JWT 无状态，验证不查"是不是最新的"，10 次登录 10 个 Token 同时能用（天然多设备）；想单会话需登录时主动撤销旧 RefreshToken。
- **7 天是滑动窗口**：每次刷新 `ExpiresAt = 现在+7天` 倒计时重置；只要相邻两次刷新间隔 <7 天就能无限无感续期，连续沉寂 7 天链条断裂才强制重登。
- 只验 key 行不行？单系统能，但 iss/aud 是零成本的多系统隔离墙 + Key 泄露时的第二道门槛，正常请求别省。

**一句话**：签发时把 iss/aud/claims 写进载荷、用 Key 签名；验证时重算签名防篡改，再字符串比对 iss/aud/exp；AccessToken 自包含免查库，RefreshToken 查库一次性续期实现滑动 7 天无感登录。

## 认证/授权中间件与特性（Authentication vs Authorization）

- **两个中间件一字之差、职责不同**：`UseAuthentication()`（认证）解析 Token → 挂 `HttpContext.User`；`UseAuthorization()`（授权）检查接口的 `[Authorize]` 标记决定放行/401。顺序固定：认证在前。
- **`[Authorize]` 是服务端准入标记，不加东西**：Bearer Token 是**客户端**加在**请求头**（`Authorization: Bearer xxx`）里的；`[Authorize]` 只负责"查票放行"——未认证直接 401，方法体不执行。
- **接口要不要认证由特性决定**：`[Authorize]` 要求已认证，`[AllowAnonymous]` 白名单跳过（如 signIn/refresh），都不标 = 匿名可访问。
- **`User` 是 ControllerBase 自带属性** = `HttpContext.User`（ClaimsPrincipal），中间件验证后自动挂上；取值 `User.FindFirst(ClaimTypes.NameIdentifier)?.Value`，与刷新流程手动解析出的 principal 同源同内容。
- **`[ApiController]` = API 自动化开关**：模型验证不过自动 400（无需手写 ModelState.IsValid）、绑定源自动推断（复杂类型默认FromBody）、强制特性路由、ProblemDetails 标准错误格式。
- 配套约定：`[Route("api/[controller]")]` 中 `[controller]` = 类名去 Controller；Action 上 `[HttpPost("signIn")]` 拼出 `api/Auth/signIn`。

**一句话**：认证中间件验票挂身份（User），授权中间件按 `[Authorize]` 查票放行；Token 由客户端放请求头，`[ApiController]` 则把参数验证/400/路由推断全自动化。

## AsNoTracking 的坑（EF Core 只读查询的隐性契约）

- **`AsNoTracking()` 语义**：查出的实体是"游离副本"，不进变更跟踪器；对它改属性后 `SaveChangesAsync` **不会生成 UPDATE**——不报错、不崩溃，就是静默丢失。
- **适用场景（可加）**：**大结果集只读**查询（如历史数据一次上万行），省掉每行的跟踪开销，收益 > 风险（DeviceData 历史查询已加）。
- **不适用场景（别加）**：**通用加载器**（如 `GetWithDetailsAsync`）——今天只读不写，但将来"查→改→存"的用法会静默失败，且坑埋仓储层、报错点在业务层，极难排查（Device 详情加载保持默认跟踪）。
- **判断标准**：单条/少条查询 tracking 开销可忽略，加了纯属埋雷；只看"会不会改"不保险，还要看**方法是否可能被复用于写路径**。

**一句话**：AsNoTracking = 断线副本，改了不保存且无报错；只给"纯读大结果集"加，通用加载器别加——省的是芝麻，埋的是暗雷。

## Modbus RTU 模块（主从模型 / 数据流 / NModbus 分层）

**概念与角色**

- **主从协议**：从站（PLC）永不主动上报，一切由主站"问"；问谁谁答（帧首字节=从站地址），无应答=超时。
- **主站口/从站口**：RS485 总线在电脑侧是主站口（COM1），设备侧是从站口（COM2）。程序只开**主站口**；从站口只是设备的"门牌"（Device 表 ComPort 存从站口），查映射（setting.json `PortMappings`）把它翻译成要打开的主站口。
- **虚拟串口分工（com0com/VSPD）**：虚拟口对 = 内存里的字节管道，**只搬字节、不懂 Modbus**；协议语义由两端各自的程序承担——主站侧 NModbus Master（本项目），从站侧独立模拟器程序的 NModbus Slave。模拟器没开/地址对不上 → 主站超时。
- **COM 口 vs SerialPort**：COM 口是 OS 资源，SerialPort 是程序操作它的"把手"（类比文件之于 FileStream）；`Open()` 后独占，一个口同时只能一个 SerialPort——这就是 `_portPool` 缓存防重复打开的原因。

**配置分工：DB 管"采什么"，setting.json 管"怎么采"**

- Device 表：SlaveAddress（从站地址门牌）+ ComPort（从站口）；DataPoint 表：RegisterAddress（PLC 绝对地址 40001 起）、RegisterType（决定功能码 03/04）、DataType（Int16 占1个寄存器、Float/Int32 占2个）、ScaleFactor（原始值×因子→工程值）、报警阈值。**数据来源是人工录入**（客户端 CRUD，内容来自 PLC 点表）。
- setting.json：串口参数（9600/8/One/None，必须与从站一致）、ReadTimeout、PollIntervalMs、PortMappings。

**NModbus 三层封装（帧由谁拼？）**

```
SerialPort           只认字节，Write/Read 纯搬运
SerialPortAdapter    包装成 IStreamResource
CreateRtuTransport   传输层：拼帧 + CRC16 计算/校验 ← 帧在这里现场拼装
CreateMaster         业务层：ReadHoldingRegistersAsync(地址, 起始, 数量)
```

- 帧不是预拼好的，**调用读方法那一刻由传输层拼出**；发送最终就是 `port.Write(帧字节)`。
- **双向翻译都在本端进程内**：发送=编码（业务语义→帧字节），接收=解码（帧字节→ushort[]），类比 JSON 序列化/反序列化；帧是两端约定的公共语言，协议的本质。

**等待的不对称（谁阻塞？）**

- **主站发完必须等**：发完请求帧阻塞在 Read，等到响应或 ReadTimeout 兜底（`await` 逻辑上未完成则下一步不发生）；同口设备串行（RS485 半双工一问一答），跨口才并行。
- **从站发完就完**：响应无确认回执，发完立即回到监听循环；CRC 错了主站自己重问，从站不感知失败。
- 一句话：**等号只挂在提问者身上**——这就是主站需要超时+重试（递增 50/100ms）、从站不需要的根源。

**一次轮询的完整链路**

```
启动：查库(2条SQL)→过滤Offline→40001转协议地址(−40001)→打包 ModbusDeviceConfig
      查映射→Open主站口入 _portPool
每轮：按主站口分组设备 → GetPort取串口 → GetOrCreateMaster(缓存复用)
      → 分组合并连续地址(减少往返) → Read(从站地址,起始,数量)
      → 切片(Float取2个寄存器) → Parser缩放 → 量程校验 → 写 ModbusDataCache(只存最新值)
消费：RealtimeService(实时推送) / DataArchiveService(5秒批量落库) / AlarmDetectionService(1秒比对报警)
```

- **三种节奏解耦是缓存存在的理由**：采集 1s、报警 1s、落库 5s，各自按需读 cache。
- **失败层层隔离**：单组失败重试→跳过组→跳过设备→跳过端口，任何一层故障不拖垮整体。
- **ReadTimeout vs WriteTimeout**：ReadTimeout 等**远端从站**回答（跨设备，业务关键路径——设备离线/地址错全靠它兜底，直接决定重试节奏与离线判定速度，需按设备响应调优）；WriteTimeout 等**本机驱动**收走字节（纯本地，微秒级完成，仅驱动假死时触发，纯保险丝）。一句话：Read 是业务参数，Write 是健壮性参数。

**资源释放三段式（Dispose / Clear / key 与 value）**

- **释放的是 value 不是 key**：字典里 key（"COM1" 字符串）只是名字，不是资源；value（Master/SerialPort 对象）内部握着 OS 句柄——遍历 `.Values` 逐个 Dispose 才是"归还资源"。
- **Dispose 由 value 的类型实现**（IDisposable 协议）：GC 只管托管内存，不管 OS 句柄（文件/串口/连接），且时机不确定；类通过 Dispose 敞开"立即归还"的口子。COM 口不立即释放 → 下次 Open 报"端口被占用"。
- **Clear() 是善后不是释放**：Dispose 后对象已是死对象（再调抛 ObjectDisposedException），但字典还攥着强引用——不 Clear 则缓存"看似有 Master 可用"（命中返回死对象）且 GC 无法回收残骸。主从关系：Dispose 归还 OS 资源（急事），Clear 断引用（善后）。
- 标准模式：**逐个 Dispose（单个失败不中断）→ Clear → 下次 Initialize 重建**。

**一句话**：释放资源靠遍历 value 逐个 Dispose（IDisposable 协议，GC 管不了句柄），Clear 只断引用防"命中死对象"；Dispose 是急事，Clear 是善后。

**缓存读取的引用 vs 拷贝（GetDeviceData / GetAllData 两法并存的原因）**

- **引用（不拷贝）**：返回 `_cache` 内部字典本身（接口降级为 IReadOnlyDictionary 只防改结构），调用方拿到"望远镜"——零开销、看到实时值，但生命周期与缓存绑死。适用：内部消费、即取即用（AlarmDetection 每秒比对当前值）。
- **拷贝（深拷贝快照）**：两层 `ToDictionary` new 出独立对象树，拿到"照片"——冻结在调用瞬间的值，原件怎么变都不影响；代价是每次两层分配。适用：跨边界交付（API 返回 JSON，序列化/传输期间须保持不变）。
- 口诀：**马上用完就扔 → 引用；值要离开现场独立存活（序列化/存储/慢处理）→ 拷贝**。

**一句话**：内部即取即用给引用（零开销实时），跨边界交付给深拷贝快照（隔离防中途变值）——按消费者生命周期特征二选一。

**同引用的判断与防护（deviceCache 为何能直接写进缓存）**

- **同一个引用，两扇门**：`UpdateDataPoint` 里 `GetOrAdd` 取出的 `deviceCache` 与 `_cache` 的 Value 是同一对象，写它=写缓存（写入路径利用引用穿透落数据，无需写回）；`GetDeviceData` 返回的也是同一对象，但用 `IReadOnlyDictionary` 封死写入口——契约式防护，误改在编译期报错（并非运行时魔防，故意 as 转回仍可改）。
- **代码判断**：`object.ReferenceEquals(a, b)` 比对象身份（地址同一性），与内容无关——内容相同的两个不同对象返回 false；判断"改 A 影响 B"只能用它，不能用 Equals/==。
- **心智推断（盯 new）**：传递（字段/参数/返回/字典取值/GetOrAdd/TryGetValue）不产生新引用；出现 new/ToDictionary/ToList/Clone 才分家。

**一句话**：引用传递下改局部变量=改原件，隔离必须显式 new；判断用 ReferenceEquals，读代码盯有没有 new。

**测点分组：为什么、凭什么（GroupingHelper）**

- **为什么分组**：一次读请求最多 125 个寄存器且**必须连续**（帧只描述 [起始, 数量] 一段），散点逐点读太浪费串口往返；分组把散点压缩成最少次数的整段读取。
- **连续判据两条，缺一不可**：①RegisterType 一致（保持/输入寄存器对应功能码 03/04，一次请求只能一种）；②地址 == 游标 expectedNextAddr（上一测点地址+其占用宽度）。
- **DataType 不参与分组**：Int16/Float 混在一组完全合法——帧层面读回的都是裸 16 位寄存器，怎么解释是 Parser 的事；DataType 只决定占用宽度（Int32/Float=2），影响游标步进（Float@2 读完游标到 4，Float@4 依然连续）。
- **GroupLength 是寄存器个数**，不是测点个数（2 Float + 1 Int16 = 5）。
- **宁断不跨**：断点处绝不跨洞合并（3→8 必切），保守但正确——跨洞省往返但洞大时浪费带宽、可能撑爆 125 上限。
- 算法骨架：排序 → 单遍扫描 → 断点切组；`ToList()` 快照防组间共享同一 List（引用陷阱）。

**一句话**：分组只看寄存器类型和地址连续性，与数据类型无关——DataType 只影响游标步进和事后解析。

**一句话（闭环心智模型）**：主站用 NModbus 把业务意图翻译成请求帧，经 COM 虚拟管道送给从站；Slave 阻塞监听 COM 缓冲区（OS 有数据唤醒，非轮询空转），收到帧→CRC 校验→地址匹配→查寄存器→回响应帧；主站再用 NModbus 解码拿到业务数据。管道只搬字节，翻译永远在两端进程内。

**系统全景：三层架构与职责边界**

- **控制面（配置，程序只读）**：ProductionLine/Device/DataPoint 三张表人工提前录入，是整个系统的"图纸"——Device 定从站地址/串口，DataPoint 定寄存器地址/类型/阈值。
- **数据面（Modbus 模块，只到缓存为止，不落库）**：ConfigLoader 按在线过滤加载成 ModbusDevice 内存对象 → 轮询时分组、经 PortManager 提供的串口/Master 资源由 DataReader+NModbus 收发帧 → 解析成工程值写缓存 `<deviceId, <pointId, value>>`。模块对外的唯一产出就是缓存，不碰任何业务表。
- **沉淀面（两个后台服务负责落库）**：
  - **AlarmDetection**（1s）：缓存当前值 + DataPoint 表阈值比对 → 查 Alarm 表去重（无"未确认的同设备+同测点+同类型"报警才生成；被确认后同故障再超限会生成新报警——确认即重置去重）→ 含等级判定（超限幅度占量程比例分 Critical/Major/Minor）→ 写 Alarm 表。它是**三个数据源的汇合点**（缓存/测点配置/报警历史）。
  - **DataArchive**（5s）：缓存快照批量写 DeviceData 表；按**两个独立配置**的保留天数清理 DeviceData 与 Alarm（DataRetention.DeviceDataDays / AlarmDays，每次现读即生效）——进（归档）出（清理）平衡防表膨胀。

**一句话**：配置表是图纸，Modbus 模块照图施工只产出到缓存，两个后台服务从缓存各取所需沉淀成历史与报警——控制面、数据面、沉淀面三层各不越界。

**Service 分层原则：返回什么、给谁用**

- **Repo 返回实体、Service 返回 DTO 是铁律**：实体是数据库的形状（外键、导航属性），DTO 是前端的形状（聚合展示字段）；Service 层是唯一的合法"翻译点"（AutoMapper.Map 只出现在这）。
- **实体出库 = 泄漏内部结构**：实体改字段名会炸所有 API 契约；序列化带导航属性有循环引用风险；JsonIgnore 补丁撒满实体。DTO 是挡在中间的防火墙。
- **Service 不只给 Controller 用**：它是"某块业务能力的封装"，谁需要谁注入——Controller（请求响应型，返回 DTO+ApiResult）、其他 Service（如 ModbusService 注入 ISystemConfigService）、后台服务（DataArchive/AlarmDetection 注入配置服务）都是客户。
- **返回 DTO 还是内存类型，看跨什么边界**：跨网络（给前端序列化）→ DTO；进程内（给别的类用）→ 按对方需要给原始类型（如 ISystemConfigService.GetValueInt 直接给 int）。

**一句话**：Repo 说数据库的话，Service 做翻译——跨网络才翻译成 DTO，进程内就说"普通话"；"给谁用"由谁注入决定，Controller 只是最常见的客户而非唯一客户。