# 数据库设计文档

> SmartFactoryMonitor — Entity 实体类与数据库表设计（基于实际代码）

---

## 一、表关系总览

### 1.1 ER 关系图

```
用户子系统：
User (用户) 1:N RefreshToken (刷新令牌)

设备监控子系统：
ProductionLine (产线) 1:N Device (设备)
Device 1:N DataPoint (测点配置)
Device 1:N DeviceData (历史数据)
DataPoint 1:N DeviceData (历史数据)
Device 1:N Alarm (报警记录)
DataPoint 1:N Alarm (报警记录)
```

### 1.2 表清单

| 序号 | 表名 | Entity | 说明 | 关系 |
|------|------|--------|------|------|
| 1 | Users | User | 系统用户账号 | 1:N → RefreshTokens |
| 2 | RefreshTokens | RefreshToken | JWT刷新令牌 | N:1 → Users |
| 3 | ProductionLines | ProductionLine | 生产线定义 | 1:N → Devices |
| 4 | Devices | Device | 设备信息（含Modbus通信参数） | N:1 → ProductionLines<br>1:N → DataPoints<br>1:N → DeviceDatas<br>1:N → Alarms |
| 5 | DataPoints | DataPoint | 测点配置（Modbus寄存器映射） | N:1 → Devices<br>1:N → DeviceDatas<br>1:N → Alarms |
| 6 | DeviceDatas | DeviceData | 历史数据记录（高频写入） | N:1 → Devices<br>N:1 → DataPoints |
| 7 | Alarms | Alarm | 报警历史记录 | N:1 → Devices<br>N:1 → DataPoints |

---

## 二、各表详细设计

### 2.1 Users（用户表）

**作用**：存储系统用户账号，支持基于角色的权限控制（RBAC）。

**表关系**：
- 1:N → RefreshTokens（一个用户可以有多个刷新令牌，用于多端登录）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | int | PK, 自增 | - | 用户唯一标识，主键 |
| `Username` | nvarchar(50) | NOT NULL | - | 登录用户名，用于认证 |
| `PasswordHash` | nvarchar(100) | NOT NULL, 唯一索引 | - | BCrypt加密后的密码哈希值，**不是明文密码** |
| `Role` | tinyint (枚举) | - | Viewer(2) | 用户角色：Admin(0)=管理员<br>Operator(1)=操作员<br>Viewer(2)=观察员 |
| `ComPort` | nvarchar(10) | 可NULL | NULL | 分配给该用户的串口端口（可选字段） |
| `IsActive` | bit | - | true | 账号是否启用，false时禁止登录 |
| `LastLoginAt` | datetime2 | 可NULL | NULL | 最后一次成功登录的时间，用于安全审计 |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 账号创建时间（UTC时区） |

**业务场景**：
- Admin 角色：可管理用户、配置设备、确认报警、查看所有数据
- Operator 角色：可操作设备启停、确认报警、查看实时数据
- Viewer 角色：只能查看实时数据和历史报表，无操作权限

**设计要点**：
- `[Index(nameof(PasswordHash), IsUnique = true)]` — 唯一索引避免密码哈希重复（其实是冗余的，因为Username已经唯一）
- `PasswordHash` 存储的是 BCrypt 哈希值（如 `$2a$11$xxxx...`），不是明文密码
- `LastLoginAt` 在每次登录成功后由 AuthService 更新

---

### 2.2 RefreshTokens（刷新令牌表）

**作用**：存储 JWT 双令牌机制中的长效 RefreshToken，支持令牌刷新和主动注销。

**表关系**：
- N:1 → Users（每个刷新令牌属于一个用户）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | int | PK, 自增 | - | 记录唯一标识 |
| `UserId` | int | FK → Users.Id | - | 外键，关联到所属用户 |
| `Token` | nvarchar(512) | NOT NULL, 唯一索引 | - | RefreshToken 的字符串值（GUID格式） |
| `ExpiresAt` | datetime2 | NOT NULL | - | 过期时间点（通常是 CreatedAt + 7天） |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 令牌签发时间 |
| `RevokedAt` | datetime2 | 可NULL | NULL | 撤销时间，**NULL表示未撤销，非NULL表示已注销** |

**JWT 双令牌机制**：

```
AccessToken（短效）：
  - 有效期：2小时
  - 存储：不存数据库，放 HTTP Header
  - 用途：每次 API 请求的身份凭证
  - 验证：中间件自动验证签名和过期时间

RefreshToken（长效）：
  - 有效期：7天（滑动窗口）
  - 存储：存此表（有状态）
  - 用途：刷新 AccessToken
  - 验证：查数据库校验 Token 值 + ExpiresAt + RevokedAt
```

**防重放攻击**：
- RefreshToken 设计为**一次性使用**：刷新成功后，旧的 RefreshToken 设 RevokedAt 作废，签发新的双令牌
- 撤销逻辑：`RevokedAt = DateTime.UtcNow` 即视为已注销
- 查询有效 Token 的 WHERE 条件：`RevokedAt == null && ExpiresAt > DateTime.UtcNow`

**滑动窗口策略**：
- 每次刷新时，新的 RefreshToken 过期时间从当前时间重新计算 `DateTime.UtcNow.AddDays(7)`
- 活跃用户可以一直保持登录状态，7天不登录才强制重新认证

**设计要点**：
- `[Index(nameof(Token), IsUnique = true)]` — Token 唯一索引，用于快速查找
- `RevokedAt` 用 nullable datetime，NULL 表示未撤销，比用 bool Status 字段更清晰

---

### 2.3 ProductionLines（产线表）

**作用**：定义生产线，作为设备的分组容器。

**表关系**：
- 1:N → Devices（一条产线包含多个设备）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | int | PK, 自增 | - | 产线唯一标识 |
| `Name` | nvarchar(50) | NOT NULL | - | 产线名称，如 "一号生产线(CNC)" |
| `Description` | nvarchar(50) | 可NULL | NULL | 产线描述说明 |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 创建时间 |

**业务场景**：
- 一条产线可能包含 CNC、注塑机、环境监测等多个设备
- 前端按产线分组展示设备列表
- 用于统计报表的分组维度

**设计要点**：
- 产线本身没有状态字段（设备才有状态）
- 不记录 UpdatedAt，产线信息很少变动

---

### 2.4 Devices（设备表）

**作用**：存储设备的完整信息，包括显示信息、Modbus 通信参数、运行状态。

**表关系**：
- N:1 → ProductionLines（每个设备属于一条产线）
- 1:N → DataPoints（一个设备有多个测点配置）
- 1:N → DeviceDatas（一个设备产生多条历史数据记录）
- 1:N → Alarms（一个设备可能产生多条报警记录）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | int | PK, 自增 | - | 设备唯一标识（数据库内部） |
| `Name` | nvarchar(50) | NOT NULL | - | 设备显示名称，如 "CNC加工中心 #01" |
| `Code` | nvarchar(20) | NOT NULL | - | **设备编码**（业务唯一标识），如 "CNC-001"<br>区别于 Id，用于和其他系统关联、报表引用 |
| `ProductionLineId` | int | FK → ProductionLines.Id | - | 外键，关联所属产线 |
| `SlaveAddress` | tinyint | - | 1 | Modbus 从站地址（1~247） |
| `BaudRate` | int | - | 9600 | 串口波特率（9600/19200/38400等） |
| `ComPort` | nvarchar(10) | NOT NULL | "COM3" | 串口端口名（如 "COM1", "COM3"） |
| `Status` | tinyint (枚举) | - | Offline(0) | 设备运行状态：<br>Offline(0)=离线<br>Running(1)=运行中<br>Alarm(2)=报警中<br>Stopped(3)=已停机 |
| `Description` | nvarchar(200) | 可NULL | NULL | 设备描述说明 |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 设备创建时间 |

**Modbus 通信参数**：
- `ComPort + BaudRate + SlaveAddress` 三者组成完整的 Modbus RTU 通信配置
- ModbusService 根据这些参数建立串口连接，轮询读取测点数据
- 同一个 ComPort 下可以有多个设备（通过不同的 SlaveAddress 区分）

**Status 字段更新时机**：
- 离线 → 运行中：ModbusService 成功读取到数据
- 运行中 → 报警中：测点触发报警
- 运行中 → 离线：连续多次通信失败
- 运行中 → 停机：用户手动执行停机命令

**设计要点**：
- `Code` 字段必须唯一（业务标识，不同于数据库 Id）
- `Status` 由 ModbusService 自动更新，不依赖人工干预
- 一个 ComPort 可能对应多个设备（同一串口下多从站）

---

### 2.5 DataPoints（测点配置表）— 核心表

**作用**：将 Modbus 寄存器地址映射为有业务意义的测点，定义量程、单位、报警阈值等。

**表关系**：
- N:1 → Devices（每个测点属于一个设备）
- 1:N → DeviceDatas（一个测点产生多条历史数据记录）
- 1:N → Alarms（一个测点可能触发多条报警记录）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | int | PK, 自增 | - | 测点唯一标识 |
| `DeviceId` | int | FK → Devices.Id | - | 外键，关联所属设备 |
| `Name` | nvarchar(50) | NOT NULL | - | 测点名称，如 "主轴转速"、"冷却液温度" |
| `RegisterAddress` | int | NOT NULL | - | **Modbus 寄存器地址**（如 40001, 30001） |
| `RegisterType` | tinyint (枚举) | - | HoldingRegister(4) | 寄存器类型：<br>InputRegister(3)=输入寄存器（只读）<br>HoldingRegister(4)=保持寄存器（可读写） |
| `DataType` | tinyint (枚举) | - | Int16(0) | 数据类型：<br>Int16(0)=16位有符号整数<br>UInt16(1)=16位无符号整数<br>Int32(2)=32位有符号整数（占2个寄存器）<br>Float(3)=32位浮点数（占2个寄存器） |
| `MinValue` | float | - | 0 | 物理量最小量程（如 0°C） |
| `MaxValue` | float | - | 0 | 物理量最大量程（如 100°C） |
| `Unit` | nvarchar(20) | 可NULL | NULL | 单位，如 "°C", "rpm", "MPa" |
| `AlarmHigh` | float | 可NULL | NULL | 报警上限阈值（超过此值触发高限报警） |
| `AlarmLow` | float | 可NULL | NULL | 报警下限阈值（低于此值触发低限报警） |
| `ScaleFactor` | float | - | 1.0 | **缩放因子**：原始值 × ScaleFactor = 实际物理值 |
| `DecimalPlaces` | int | - | 0 | 显示时保留的小数位数（如 2 → 12.34） |
| `SortOrder` | int | - | 0 | 排序序号，前端列表按此字段排序显示测点 |

**Modbus 地址约定**：
- Holding Register 地址范围：40001 ~ 49999（功能码 0x03）
- Input Register 地址范围：30001 ~ 39999（功能码 0x04）
- 40001 表示"保持寄存器第1个"，对应协议层地址 0
- 30001 表示"输入寄存器第1个"，对应协议层地址 0

**数据解析流程**：

```
NModbus 读取原始值：
  registers = master.ReadHoldingRegisters(40001, 1)
  rawValue = registers[0]  ← 原始整数

应用 ScaleFactor：
  physicalValue = rawValue × ScaleFactor

范围校验：
  if (physicalValue < MinValue || physicalValue > MaxValue)
      → 日志记录异常值

报警判断：
  if (AlarmHigh != null && physicalValue > AlarmHigh)
      → 触发高限报警
  if (AlarmLow != null && physicalValue < AlarmLow)
      → 触发低限报警
```

**Int32/Float 占用2个寄存器**：
- 32位数据在 Modbus 中拆成2个16位寄存器存储
- 如 Int32 存在 40005~40006，读取时需要 `ReadHoldingRegisters(40005, 2)`
- 拼接公式：`(uint)registers[0] << 16 | registers[1]`

**批量读取优化**：
- 按 `RegisterAddress` 排序，合并连续地址的测点
- 如 40001~40003 连续，一次读3个寄存器，减少通信次数

**设计要点**：
- `SortOrder` 控制测点显示顺序，不设时按 Id 排（Id 和业务无关）
- `ScaleFactor` 解决原始值与实际物理量不一致的问题（如 PLC 存的是放大10倍的整数）

---

### 2.6 DeviceDatas（设备数据记录表）— 高频写入

**作用**：存储历史数据，用于趋势分析、报表查询、数据回溯。

**表关系**：
- N:1 → Devices（关联设备）
- N:1 → DataPoints（关联测点）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | bigint | PK, 自增 | - | 记录唯一标识（用 bigint 防溢出） |
| `DeviceId` | int | FK → Devices.Id | - | 外键，关联设备 |
| `DataPointId` | int | FK → DataPoints.Id | - | 外键，关联测点 |
| `Value` | float | NOT NULL | - | 实际物理值（已应用 ScaleFactor） |
| `Quality` | int | - | 0 | 数据质量：<br>0=好（通信正常）<br>1=不确定（部分异常）<br>2=坏（通信失败） |
| `RecordedAt` | datetime2 | NOT NULL | DateTime.UtcNow | **数据采集时间**（非入库时间） |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 数据入库时间 |

**RecordedAt vs CreatedAt 的区别**：

| | RecordedAt | CreatedAt |
|---|---|---|
| 含义 | 数据采集的时刻 | 数据写入数据库的时刻 |
| 来源 | ModbusService 轮询时间 | EF Core SaveChanges 时间 |
| 用途 | 查询历史趋势按此排序 | 审计入库时间 |
| 批量入库场景 | 每条数据各不相同 | 多条数据可能相同 |

**Quality 字段用途**：
- 区分"值为0是正常测量结果"还是"通信断了不知道真实值"
- 前端根据 Quality 值用不同样式渲染（如红色表示坏质量）
- 报警判断时跳过 Quality=2 的数据

**写入频率估算**：
- 10 台设备 × 每台 6 个测点 = 60 条/秒
- 一天：60 × 86400 = 5,184,000 条
- int 类型约 1000 天溢出 → 用 bigint

**设计要点**：
- 复合索引：`(DeviceId, DataPointId, RecordedAt)` — 覆盖最常见查询模式
- 外键设 Restrict（非 Cascade），避免 SQL Server 多重级联路径错误
- 后期优化：按月分区、30天数据归档、可考虑 TimescaleDB

---

### 2.7 Alarms（报警记录表）

**作用**：存储报警历史，用于故障追溯、统计分析、报警确认流程。

**表关系**：
- N:1 → Devices（关联触发设备）
- N:1 → DataPoints（关联触发测点）

**字段详解**：

| 字段 | 类型 | 约束 | 默认值 | 作用说明 |
|------|------|------|--------|----------|
| `Id` | bigint | PK, 自增 | - | 报警记录唯一标识 |
| `DeviceId` | int | FK → Devices.Id | - | 外键，关联触发设备 |
| `DataPointId` | int | FK → DataPoints.Id | - | 外键，关联触发测点 |
| `Level` | tinyint (枚举) | - | Info(3) | 报警级别：<br>Critical(0)=紧急<br>Major(1)=重要<br>Minor(2)=一般<br>Info(3)=提示 |
| `AlarmType` | tinyint (枚举) | NOT NULL | - | 报警类型：<br>HighLimit(0)=高限超限<br>LowLimit(1)=低限超限 |
| `Message` | nvarchar(200) | NOT NULL | - | 自动生成的报警消息文本 |
| `Value` | float | NOT NULL | - | 触发报警时的实际值 |
| `Threshold` | float | NOT NULL | - | 触发报警的阈值（AlarmHigh 或 AlarmLow） |
| `Status` | tinyint (枚举) | - | Unacknowledged(0) | 报警状态：<br>Unacknowledged(0)=未确认<br>Acknowledged(1)=已确认 |
| `AcknowledgedBy` | nvarchar(50) | 可NULL | NULL | 确认人用户名 |
| `AcknowledgedAt` | datetime2 | 可NULL | NULL | 确认时间 |
| `OccurredAt` | datetime2 | NOT NULL | DateTime.UtcNow | 报警发生时间 |
| `CreatedAt` | datetime2 | - | DateTime.UtcNow | 记录创建时间 |

**双外键设计（反范式）**：
- DeviceId + DataPointId 同时存在，虽然 DataPoint 已经包含 DeviceId
- 原因：方便直接查报警关联的设备和测点名称，无需 JOIN 三层
- 查询示例：`SELECT d.Name, p.Name, a.Message FROM Alarms a JOIN Devices d JOIN DataPoints p`

**Message 模板**：

```
"[{DeviceName}] {DataPointName} {AlarmType}报警: 当前值{Value}{Unit}, 阈值{Threshold}{Unit}"

示例：
"[CNC-001] 冷却液温度 高限报警: 当前值85.2°C, 阈值80°C"
"[注塑机A] 螺杆位置 低限报警: 当前值12.5mm, 阈值15mm"
```

**报警确认流程**：
- Status=0（未确认）时，前端显示红色闪烁
- Operator/Admin 点击确认 → Status=1，记录 AcknowledgedBy 和 AcknowledgedAt
- 确认后前端显示为普通颜色

**复合索引**：
- `(DeviceId, Status, OccurredAt)` — 覆盖活跃报警查询和历史查询

**设计要点**：
- 外键设 Restrict（非 Cascade）
- 同一测点可能产生多条报警（如持续超限期间每分钟记一条）

---

## 三、枚举类型定义

### 3.1 UserRole（用户角色）

```csharp
public enum UserRole : byte
{
    Admin = 0,      // 管理员：全部权限
    Operator = 1,   // 操作员：设备操作 + 报警确认
    Viewer = 2      // 观察员：只读权限
}
```

**权限矩阵**：

| 功能 | Admin | Operator | Viewer |
|------|-------|----------|--------|
| 用户管理 | ✅ | ❌ | ❌ |
| 设备配置 | ✅ | ❌ | ❌ |
| 设备启停 | ✅ | ✅ | ❌ |
| 报警确认 | ✅ | ✅ | ❌ |
| 查看数据 | ✅ | ✅ | ✅ |

---

### 3.2 DeviceStatus（设备状态）

```csharp
public enum DeviceStatus : byte
{
    Offline = 0,    // 离线（通信失败）
    Running = 1,    // 运行中（正常）
    Alarm = 2,      // 报警中（有测点超限）
    Stopped = 3     // 已停机（手动停机）
}
```

**状态转换逻辑**：

```
Offline → Running：ModbusService 成功读取数据
Running → Alarm：测点触发报警
Alarm → Running：报警消除
Running → Offline：连续通信失败（如3次超时）
Running → Stopped：用户执行停机命令
Stopped → Running：用户执行启动命令
```

---

### 3.3 RegisterType（寄存器类型）

```csharp
public enum RegisterType : byte
{
    InputRegister = 3,      // 输入寄存器（只读）
    HoldingRegister = 4     // 保持寄存器（可读写）
}
```

**对应 Modbus 功能码**：

| 类型 | 地址范围 | 功能码 | NModbus 方法 |
|------|---------|--------|-------------|
| Input Register | 30001~39999 | 0x04 | `ReadInputRegistersAsync()` |
| Holding Register | 40001~49999 | 0x03 | `ReadHoldingRegistersAsync()` |

---

### 3.4 DataPointType（测点数据类型）

```csharp
public enum DataPointType : byte
{
    Int16 = 0,      // 16位有符号整数（占1个寄存器）
    UInt16 = 1,     // 16位无符号整数（占1个寄存器）
    Int32 = 2,      // 32位有符号整数（占2个寄存器）
    Float = 3       // 32位浮点数（占2个寄存器）
}
```

**解析方式**：
- Int16：直接 cast `(short)registers[0]`
- UInt16：直接 cast `(ushort)registers[0]`
- Int32：拼接 `(int)((uint)registers[0] << 16 | registers[1])`
- Float：拼接后用 `BitConverter.ToSingle()`

---

### 3.5 AlarmLevel（报警级别）

```csharp
public enum AlarmLevel : byte
{
    Critical = 0,   // 紧急：需立即处理
    Major = 1,      // 重要：需尽快处理
    Minor = 2,      // 一般：可延后处理
    Info = 3        // 提示：仅记录
}
```

**前端样式建议**：
- Critical：红色闪烁 + 声音报警
- Major：红色高亮
- Minor：橙色提示
- Info：蓝色信息

---

### 3.6 AlarmType（报警类型）

```csharp
public enum AlarmType : byte
{
    HighLimit = 0,  // 高限报警：值 > AlarmHigh
    LowLimit = 1    // 低限报警：值 < AlarmLow
}
```

---

### 3.7 AlarmStatus（报警状态）

```csharp
public enum AlarmStatus : byte
{
    Unacknowledged = 0,  // 未确认（活跃报警）
    Acknowledged = 1     // 已确认（历史报警）
}
```

---

## 四、表关系图（完整）

```
┌──────────────────┐       ┌──────────────────┐
│      Users       │ 1   N │  RefreshTokens   │
├──────────────────┤───────├──────────────────┤
│ Id (PK)          │       │ Id (PK)          │
│ Username         │◄──────│ UserId (FK)      │
│ PasswordHash     │       │ Token            │
│ Role             │       │ ExpiresAt        │
│ IsActive         │       │ RevokedAt        │
│ LastLoginAt      │       └──────────────────┘
└──────────────────┘

┌──────────────────┐       ┌──────────────────┐       ┌──────────────────┐
│  ProductionLines │ 1   N │     Devices      │ 1   N │   DataPoints     │
├──────────────────┤───────├──────────────────┤───────├──────────────────┤
│ Id (PK)          │       │ Id (PK)          │       │ Id (PK)          │
│ Name             │◄──────│ ProductionLineId │◄──────│ DeviceId (FK)    │
│ Description      │       │ Name / Code      │       │ Name             │
│ CreatedAt        │       │ SlaveAddress     │       │ RegisterAddress  │
└──────────────────┘       │ BaudRate / ComPort│      │ RegisterType     │
                           │ Status           │       │ DataType         │
                           │ Description      │       │ MinValue/MaxValue│
                           └──────┬───────────┘       │ AlarmHigh/Low    │
                          1       │ N                 │ ScaleFactor      │
                                  │                   │ SortOrder        │
                                  ▼                   └────────┬─────────┘
                    ┌──────────────────┐                       │ N
                    │   DeviceDatas    │◄──────────────────────┘ 1
                    ├──────────────────┤               ┌──────────────────┐
                    │ Id (bigint)      │               │     Alarms       │
                    │ DeviceId (FK)    │◄──────────────├──────────────────┤
                    │ DataPointId (FK) │               │ Id (bigint)      │
                    │ Value / Quality  │               │ DeviceId (FK)    │
                    │ RecordedAt       │               │ DataPointId (FK) │
                    └──────────────────┘               │ Level / AlarmType│
                                                       │ Message / Value  │
                                                       │ Threshold        │
                                                       │ Status           │
                                                       │ AcknowledgedBy   │
                                                       └──────────────────┘
```

---

## 五、数据流向图

```
PLC/模拟设备 (Modbus Slave)
    │ 串口通信 (RTU模式)
    │ NModbus 读取寄存器
    ▼
ModbusService (BackgroundService)
    ├─ 1. 根据 ComPort + SlaveAddress 建立连接
    ├─ 2. 按 RegisterAddress 批量读取
    ├─ 3. 根据 DataType 拼接/解析原始值
    ├─ 4. 应用 ScaleFactor → 物理值
    ├─ 5. 检查 MinValue/MaxValue → 异常值日志
    ├─ 6. 检查 AlarmHigh/AlarmLow → 触发报警
    │       └─► 写入 Alarms 表
    ├─ 7. 更新 _cache 内存缓存（实时API）
    ├─ 8. 写入 DeviceDatas 表（历史记录）
    └─ 9. 更新 Device.Status 字段
```

---

## 六、设计要点总结

### 6.1 主键选择

- **int 主键**：用于低频写入表（Users, Devices, DataPoints）
- **bigint 主键**：用于高频写入表（DeviceDatas, Alarms），防溢出

### 6.2 外键约束

- **Cascade**：ProductionLine → Device（删除产线时级联删除设备）
- **Restrict**：Device → DeviceData, Device → Alarm（禁止级联删除，避免 SQL Server 多重级联路径错误）

### 6.3 索引设计

- **唯一索引**：Username, PasswordHash, Token, Code（业务唯一性）
- **复合索引**：
  - `(DeviceId, DataPointId, RecordedAt)` — DeviceDatas 查询优化
  - `(DeviceId, Status, OccurredAt)` — Alarms 查询优化

### 6.4 时区统一

- 所有 datetime 字段使用 **DateTime.UtcNow**（UTC时区）
- 前端显示时用 `.ToLocalTime()` 转为本地时区
- 避免服务器时区和客户端时区不一致导致的时间混乱

### 6.5 Entity vs DTO

- **Entity**：映射数据库表，含导航属性、外键字段
- **DTO**：面向 API，裁剪/组合字段，**不暴露 Entity 直接给前端**

### 6.6 性能优化方向

- DeviceDatas 表：按月分区、30天归档、考虑 TimescaleDB
- Alarms 表：已确认报警定期归档
- Modbus 批量读取：合并连续地址减少通信次数

---

## 七、后期扩展建议

1. **线圈和离散输入**：
   - 扩展 DataPoint.RegisterType 支持 Coil(0x01) 和 DiscreteInput(0x02)
   - 用于设备启停控制（Day4 控制模块）

2. **设备控制表**：
   - 新增 DeviceCommands 表，记录用户下达的启停命令
   - 字段：CommandId, DeviceId, CommandType, IssuedBy, IssuedAt, ExecutedAt, Status

3. **用户操作日志**：
   - 新增 UserOperationLogs 表，审计用户的关键操作
   - 字段：UserId, Action, TargetType, TargetId, Timestamp

4. **实时数据缓存优化**：
   - 当前 `_cache` 是内存 Dictionary
   - 可考虑 Redis 分布式缓存，支持多实例部署