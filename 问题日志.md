# 问题日志

> 记录开发过程中遇到的编译错误、运行时问题、概念疑问及对应解决方案

---

## 一、WPF Client 端问题

### Q1：Shell 基类不匹配 — "无法将类型'Client.Views.Shell'隐式转换为'System.Windows.Window'"

**现象**：XAML 使用 `<hc:Window>` 但 code-behind 继承 `System.Windows.Window`，分部声明指定了不同基类。

**原因**：HandyControl 的 `hc:Window` 对应 `HandyControl.Controls.Window`，不是 `System.Windows.Window`。

**解决**：
```csharp
// Shell.xaml.cs — 使用完整类型名避免歧义
public partial class Shell : HandyControl.Controls.Window
{
    public Shell() { InitializeComponent(); }
}
```

---

### Q2：Window 命名空间歧义 — "'Window'是'HandyControl.Controls.Window'和'System.Windows.Window'之间的不明确的引用"

**原因**：同时引入了 `using HandyControl.Controls` 和默认的 `System`（含 `System.Windows.Window`），两个 namespace 都有 `Window` 类，编译器无法确定用哪个。

**解决**：去掉 `using HandyControl.Controls`，直接在基类处写完整命名空间 `HandyControl.Controls.Window`。

---

## 二、NuGet 包与版本问题

### Q3：Swashbuckle.AspNetCore 和 Swashbuckle.AspNetCore.Swagger 选哪个？

**结论**：装 **Swashbuckle.AspNetCore** 即可。它是主包，内部已包含 Swagger 所需的全部功能。`Swashbuckle.AspNetCore.Swagger` 是子包，不需要单独安装。

实际安装版本：**10.2.1**

---

### Q4：AutoMapper 和 AutoMapper.Extensions.Microsoft.DependencyInjection 选哪个？

**结论**：只装 **AutoMapper**（当前版本 16.1.1）即可。

**原因**：`AutoMapper.Extensions.Microsoft.DependencyInjection` 已被官方标记为弃用（Deprecated），推荐直接使用 AutoMapper 主包。AutoMapper 12.0+ 已内置 DI 支持，无需额外扩展包。

---

### Q5：Microsoft.OpenApi.Models 命名空间找不到

**现象**：Swagger 配置中使用 `using Microsoft.OpenApi.Models;` 编译报错。

**原因**：Swashbuckle.AspNetCore 10.2.1 依赖的 Microsoft.OpenApi 是 **2.x 版本**（当前为 2.7.5），该版本的**所有类型都在 `Microsoft.OpenApi` 根命名空间下**，不存在 `.Models` 子命名空间。（3.x 版本才有 .Models）

**解决**：将 `using Microsoft.OpenApi.Models;` 改为 `using Microsoft.OpenApi;`

影响范围：`OpenApiInfo`、`OpenApiSecurityScheme`、`SecuritySchemeType`、`ParameterLocation` 等全部改用此命名空间。

---

### Q6：AddSecurityRequirement 签名不匹配（Swashbuckle 10.x）

**现象**：尝试添加全局 JWT 要求时参数类型不匹配，经历多次迭代均失败：

| 尝试 | 写法 | 错误 |
|------|------|------|
| 1 | `Dictionary<string, IEnumerable<string>>` | 无法转换为 `Func<OpenApiDocument, ...>` |
| 2 | `Func<OpenApiDocument, OpenApiSecurityRequirement>` | `OpenApiSecurityScheme` 未包含 `Reference` 的定义 |

**原因**：Swashbuckle 10.x 的 API 与旧版 7.x 有显著差异，`AddSecurityRequirement` 的签名完全变了，且 `OpenApiSecurityScheme` 不再有 `Reference` 属性。

**解决方案**：暂时移除 `AddSecurityRequirement`，只保留 `AddSecurityDefinition`。JWT 认证功能不受影响，Swagger UI 上仍然会出现 "Authorize" 按钮供用户输入 Token。全局安全要求为可选配置，不影响实际认证逻辑。

---

## 三、AutoMapper 问题

### Q7：AddAutoMapper 参数类型错误 — AutoMapper 16.x API 变更

**现象**：按旧文档写的注册方式在 16.x 下全部报错：

| 写法 | 错误信息 |
|------|----------|
| `AddAutoMapper(typeof(MappingProfile).Assembly)` | 无法从 `Assembly` 转换为 `Action<IMapperConfigurationExpression>` |
| `AddAutoMapper(typeof(MappingProfile))` | 同上 |
| `AddAutoMapper(cfg => cfg.AddProfile(MappingProfile))` | "MappingProfile"是一个类型，在此上下文中无效 |
| `AddAutoMapper(cfg => cfg.AddProfile(new MappingProfile()))` | 可行但繁琐 |

**原因**：AutoMapper 16.x 重构了 API，移除了接受 `Type` 或 `Assembly` 的便捷重载，统一使用 `Action<IMapperConfigurationExpression>` 表达式形式。

**最终解决方案**：
```csharp
builder.Services.AddAutoMapper(cfg => cfg.AddMaps(typeof(MappingProfile).Assembly));
```

使用泛型 `AddMaps<T>()` 扫描程序集，以后新增 Profile 类无需回来修改 Program.cs。

---

### Q8：AutoMapper 基础概念 — 它是什么？为什么需要？

**疑问**：手动赋值和 AutoMapper 有什么区别？

**解答**：AutoMapper = 对象拷贝机器。把一个对象的值按规则搬到另一个对象里。
- 手动写：每个接口都要逐字段赋值 -> 大量重复代码、易遗漏
- 用 AutoMapper：一行 `_mapper.Map<TDto>(entity)` 搞定
- 内部机制：首次映射时编译成表达式树，后续调用接近手写性能

---

### Q9：DeviceDto vs DeviceListItemDto — 为什么一个 Entity 映射两个 DTO？

**疑问**：两个 CreateMap 有什么区别？

**解答**：这是 DTO 分层模式（列表/详情分离）：
- **DeviceDto** = 完整版（档案），字段全，给详情页用
- **DeviceListItemDto** = 缩略版（名片），只有 4-5 个字段，给列表页用，JSON 小、传输快
- 控制映射范围的**不是** CreateMap 写法，而是 DTO 类本身定义了多少属性。DTO 写几个字段，AutoMapper 就拷几个

---

### Q10：ForMember 语法详解

**疑问**：`ForMember(dest => dest.XXX, opt => opt.MapFrom/Ignore())` 各部分含义？

**解答**：
- 第 1 个参数 `dest => dest.Xxx`：Lambda 选中目标 DTO 的某个属性（指定"对谁操作"）
- 第 2 个参数 `opt => opt.Xxx(...)`：告诉 AutoMapper 怎么处理这个字段
  - `MapFrom(src => ...)`：目标字段源对象没有同名属性，从指定表达式计算
  - `Ignore()`：即使名字匹配也不拷贝（安全脱敏）
- 其余字段按默认规则自动匹配

三种典型场景：
```csharp
// 场景A：补充计算值（源没有但目标需要）
.ForMember(dest => dest.DeviceCount, opt => opt.MapFrom(src => src.Devices.Count))

// 场景B：从导航属性取值
.ForMember(dest => dest.ProductionLineName,
           opt => opt.MapFrom(src => src.ProductionLine != null ? src.ProductionLine.Name : null))

// 场景C：安全脱敏（源有但不想拷贝）
.ForMember(dest => dest.PasswordHash, opt => opt.Ignore())
```

---

### Q11：UserDto 没有 PasswordHash，Ignore() 是否多余？

**疑问**：UserDto 里根本没有 PasswordHash 属性，为什么要写 Ignore？

**解答**：确实多余。两种处理方式：
- **方式A（防御性）**：写 Ignore()，防止未来有人误给 DTO 加了 PasswordHash 字段导致泄露
- **方式B（简洁）**：不写 Ignore，DTO 不定义该字段 -> 天然不可能被映射过去（**推荐本项目采用的方式**）

AutoMapper 只能拷贝目标对象存在的属性，不定义 = 彻底安全。

---

### Q12：Enum -> String 是否需要额外配置？

**疑问**：Entity 用 enum（如 DeviceStatus），DTO 用 string，AutoMapper 能自动转换吗？

**解答**：**能，不需要额外配置。** AutoMapper 默认支持 enum -> string 自动转换（调用 .ToString()）。反向（string -> enum）不支持，因为字符串可能非法。本项目目前全是 Entity(enum) -> DTO(string) 方向，全自动。

---

### Q13：Update 方法中 _mapper.Map<T>(src) 未更新已追踪实体

**现象**：调用更新 API 返回"更新成功"，但数据库中的数据没有变化。

**原因**：`_mapper.Map<T>(request)` 创建了一个**新对象**并返回，而不是将 request 的值合并到已存在的实体上。EF Core ChangeTracker 追踪的仍是原来的 `point` / `device` 实体，其属性未被修改，`SaveChangedAsync()` 不会生成 UPDATE。

**错误代码（DataPointService）**：
```csharp
var point = await _pointRepo.GetByIdAsync(id);  // point 已被追踪
_mapper.Map<DataPoint>(request);                 // ❌ 创建新对象，point 没变
await _pointRepo.SaveChangedAsync();             // ❌ 无变化，不生成 UPDATE
```

**修复**：使用 `_mapper.Map(src, dest)` 重载，将 request 的值合并到已追踪的实体上：

```csharp
_mapper.Map(request, point);  // ✅ 把 request 的值合并到 point 上
await _pointRepo.SaveChangedAsync();
```

**影响范围**：
- [DataPointService.cs](file:///d:/Code/C%23/Project/SmartFactoryMonitor/Server/Services/DataPointService.cs#L53-L54)
- [DeviceService.cs](file:///d:/Code/C%23/Project/SmartFactoryMonitor/Server/Services/DeviceService.cs#L69-L70)

**深层理解**：`_mapper.Map<Dest>(src)` = "创建一个 Dest 类型的对象，用 src 填充"。`_mapper.Map(src, dest)` = "把 src 的值更新到现有的 dest 对象上"。前者适合创建场景（POST），后者适合更新场景（PUT）。

---

### Q14：Alarm 双外键设计是否合理？

**疑问**：Alarm 同时有 DeviceId 和 DataPointId 两个外键，为什么不只用一个？

**解答**：合理，是标准的多对一设计。一条报警 = "某台设备的某个数据点超标"，两个外键分别回答不同维度的问题：
- DeviceId：谁报的警？
- DataPointId：什么指标超标？

如果只保留一个，要么无法按数据点统计报警频率，要么每次查询都多 JOIN 一层。

---

## 四、EF Core / 数据库问题

### Q15：HasName() 过时警告

**现象**：`RelationalIndexBuilderExtensions.HasName(IndexBuilder, string?)已过时: Use HasDatabaseName() instead.`

**原因**：EF Core 新版本将 `HasName()` 标记为过时，改名为 `HasDatabaseName()` 以更准确表达语义。

**解决**：将 `.HasName("IX_xxx")` 改为 `.HasDatabaseName("IX_xxx")`

附带发现：DeviceData 表的索引名误写成了 Alarm 的索引名，一并纠正。

---

### Q16：种子数据 DateTime.UtcNow 导致迁移报错

**现象**：执行 `Update-Database` 时报错：The model for context 'SmartFactoryDbContext' changes each time it is built. This is usually caused by dynamic values used in a 'HasData' call (e.g. new DateTime(), Guid.NewGuid()).

**原因**：EF Core 要求种子数据是**确定性**的——同样代码每次运行结果必须一致。`DateTime.UtcNow` 每次编译都不同，EF Core 认为模型一直在变。

**解决**：使用硬编码固定时间：
```csharp
var seedTime = new DateTime(2025, 1, 1, 0, 0, 0, DateTimeKind.Utc);
modelBuilder.Entity<ProductionLine>().HasData(
    new ProductionLine { Id = 1, Name = "一号生产线", ..., CreatedAt = seedTime },
    // ...
);
```

---

### Q17：PendingModelChangesWarning — 需要先创建新迁移

**现象**：修复动态值后再次 `Update-Database` 报错：model has pending changes. Add a new migration before updating.

**原因**：Entity 或 DbContext 有改动，但还没生成对应的迁移文件。

**解决**：先创建迁移再更新：
```powershell
Add-Migration InitialCreate
Update-Database
```

---

### Q18：SQL Server 多重级联路径错误

**现象**：执行迁移建表时报 SqlException (1785)：将 FOREIGN KEY 约束 'FK_Alarms_Devices_DeviceId' 引入表 'Alarms' 可能会导致循环或多重级联路径。

**原因**：Alarm 表有 DeviceId 和 DataPointId 两个外键，都设置了 ON DELETE CASCADE。删除一个 Device 时会通过两条路径级联删除同一批 Alarms，SQL Server 不允许这种多重级联路径。

**解决**：在 DbContext 的 OnModelCreating 中将 Alarm 和 DeviceData 的外键改为 Restrict：
```csharp
modelBuilder.Entity<Alarm>()
    .HasOne(a => a.Device).WithMany()
    .HasForeignKey(a => a.DeviceId)
    .OnDelete(DeleteBehavior.Restrict);

modelBuilder.Entity<Alarm>()
    .HasOne(a => a.DataPoint).WithMany()
    .HasForeignKey(a => a.DataPointId)
    .OnDelete(DeleteBehavior.Restrict);

// DeviceData 同理...
```
Restrict 含义：如果还有关联记录则阻止删除（防止孤儿数据），业务上删设备前应先清理关联数据或使用软删除。

---

### Q19：BCrypt HashPassword 在 HasData 中导致 PendingModelChangesWarning

**现象**：种子数据中写入 `BCrypt.Net.BCrypt.HashPassword("Admin@2026")` 后执行 `Add-Migration UpdateSeedData` 报 PendingModelChangesWarning。

**原因**：BCrypt 每次运行结果不同（自动加盐），EF Core 要求种子数据是确定性的——同样代码每次运行必须一致。

**解决**：先临时跑代码算出哈希值，再硬编码到种子数据中：
```csharp
// 1. 先算哈希（控制台输出一次即可）
var hash = BCrypt.Net.BCrypt.HashPassword("Admin@2026");
Console.WriteLine(hash);

// 2. 硬编码到 HasData 中
modelBuilder.Entity<User>().HasData(
    new User {
        Id = 1,
        Username = "admin",
        PasswordHash = "$2a$11$粘贴上面输出的哈希字符串",  // <- 固定值
        // ...
    }
);
```

---

## 五、Program.cs / ASP.NET Core 问题

### Q20：根路径访问 404

**现象**：启动项目后访问 http://localhost:5078 显示 404。

**原因**：WebAPI 只响应显式注册的路由（如 `/api/devices`），根路径 `/` 没有处理器。

**解决**：这不是 bug。应该访问 `/swagger` 查看 API 文档页面。如需让根路径自动跳转：
```csharp
app.MapControllers();
app.MapGet("/", () => Results.Redirect("/swagger"));
```

---

### Q21：JwtBearerDefaults 找不到

**现象**：编写 JWT 配置时 `JwtBearerDefaults.AuthenticationScheme` 编译报错。

**原因**：缺少命名空间引用。

**解决**：添加 `using Microsoft.AspNetCore.Authentication.JwtBearer;`。.NET 10 已内置该包，只需加 using 即可。

---

### Q22：SmartFactoryDbContext 找不到

**现象**：Program.cs 中引用 SmartFactoryDbContext 时报找不到类型。

**原因**：正常现象——当时 Entity 和 DbContext 还未创建。属于开发顺序问题。

**解决**：按正确顺序创建：Entity -> DbContext -> 补全 Program.cs。避免循环依赖。

---

### Q23：Program.cs 两阶段模型 — builder.Services vs app.xxx 的关系

**疑问**：这两个阶段是什么关系？注册到什么容器里？

**解答**：

**阶段1 — builder.Services.AddXxx()（注册/备料）**：
- 所有 AddXxx() 都往同一个 `IServiceCollection`（服务清单）里添加条目
- 包括 DbContext、AutoMapper、Authentication、Controllers 等
- 此时不会创建任何实例，只是"登记"

**阶段2 — app.UseXxx() / app.MapXxx()（运行/做菜）**：
- `builder.Build()` 将 IServiceCollection 转为 IServiceProvider（智能工厂/容器）
- 中间件管道按顺序处理 HTTP 请求
- 顺序不能乱：CORS -> Authentication -> Authorization -> MapControllers

**DI 注入原理**：Controller 构造函数声明需要的依赖（如 DbContext、IMapper），容器自动创建并注入。三个生命周期：
- Transient：每次 new
- Scoped：每请求一个（DbContext 必须用这个）
- Singleton：全局唯一（AuthenticationService）

---

### Q24：IConfiguration 是否需要手动注册就能使用？

**疑问**：JwtHelper/Service 层用的 IConfiguration 是框架默认注册的还是需要手动注册？

**解答**：**框架已自动注册**，不需要手动注册。ASP.NET Core 在 `WebApplication.CreateBuilder(args)` 时自动将以下服务注册到 DI 容器：
- `IConfiguration`（Singleton）— 读取 appsettings.json 的内容
- `ILogger<T>` — 日志
- `IHostEnvironment` — 环境信息

所以 Service 层通过构造函数注入 `IConfiguration` 直接可用。

**读取方式**：JSON 嵌套结构会被"拍平"成 `:` 分隔的键值对：
```csharp
_jwtConfig["Jwt:Key"]           // 直接用冒号路径读
var section = _jwtConfig.GetSection("Jwt");  // 先取子节点再读属性
```

---

### Q25：Program.cs 中文注释乱码

**现象**：保存 Program.cs 后中文注释变成乱码。

**原因**：VS2022 保存文件时编码选错了——文件以 ANSI/GBK 编码保存，导致 UTF-8 的中文字符被损坏。

**解决**：VS 中打开文件 -> **文件 -> 高级保存选项** -> 编码改为 **"Unicode (UTF-8 with签名)"** -> 确定。乱码的字节已被损坏需手动重新打字。

---

## 六、JWT / 认证相关问题

### Q26：JwtRegisteredClaimNames 找不到

**现象**：JwtHelper 中使用 `JwtRegisteredClaimNames.Jti` 编译报错"当前上下文中不存在名称"。

**原因**：缺少命名空间引用。该类在 `System.IdentityModel.Tokens.Jwt` 命名空间下。

**解决**：添加 `using System.IdentityModel.Tokens.Jwt;`。此包作为 `Microsoft.AspNetCore.Authentication.JwtBearer` 的传递依赖已自动引入，无需额外安装 NuGet 包。

**相关类对照表**：

| 类型 | 命名空间 | 用途 |
|------|---------|------|
| `JwtBearerDefaults` | `Microsoft.AspNetCore.Authentication.JwtBearer` | 认证 Scheme 名称 |
| `JwtSecurityTokenHandler` | `System.IdentityModel.Tokens.Jwt` | 序列化/反序列化 Token |
| `JwtSecurityToken` | `System.IdentityModel.Tokens.Jwt` | 构建 Token 对象 |
| `JwtRegisteredClaimNames` | `System.IdentityModel.Tokens.Jwt` | 标准 Claim 名称常量 |
| `SigningCredentials` | `Microsoft.IdentityModel.Tokens` | 签名凭证 |
| `SymmetricSecurityKey` | `Microsoft.IdentityModel.Tokens` | 安全密钥 |

简单记：**认证配置用 JwtBearer 包，Token 生成/解析用 IdentityModel.Tokens.Jwt 包**。

---

### Q27：System.IdentityModel.Tokens.Jwt 是 .NET 自带的还是 NuGet 安装的？

**解答**：**NuGet 包**，但**你不需要手动安装**。依赖链如下：
```
你安装的:
Microsoft.AspNetCore.Authentication.JwtBearer (Day1)
  |- 依赖 -> Microsoft.IdentityModel.JsonWebTokens
            |- 依赖 -> System.IdentityModel.Tokens.Jwt   <- 自动带入
```
装一个后面三个全部自动跟着来。

---

### Q28：Jti（JWT ID）的作用是什么？

**疑问**：为什么要给每个 Token 加一个 GUID？

**解答**：Jti = **JWT ID**，给每个 Token 生成唯一标识符，用于**防重放攻击**。
- 正常流程：第1次刷新服务端记录 jti="abc123" 已使用；攻击者拿同一 Token 再来 -> 服务端发现 jti 已用过 -> 拒绝
- 本项目目前只写入 Claim，暂不建 UsedJtis 表做校验（RefreshToken 的唯一索引 + RevokedAt 已经够防重放）。留着是为了规范性和面试加分项。

---

### Q29：GetPrincipalFromExpiredToken 应该放在哪里更好？

**疑问**：放在 AuthService 私有方法里还是 JwtHelper 静态方法里？

**解答**：放在 **JwtHelper** 更合理。理由：
- **凝聚力**：JwtHelper 本来就是处理 Token 的工具类，生成（Generate）+ 解析（GetPrincipalFromExpiredToken）放一起
- **可复用**：以后其他地方（比如 SignalR 校验 token）也能用
- **AuthService 瘦身**：只保留业务编排逻辑

改造后调用变为：`JwtHelper.GetClaimsPrincipalFromExpiredToken(request.AccessToken, secretKey)`

---

### Q30：AuthDtos 放在哪里更合适？

**疑问**：SignInRequest/LoginResponse/RefreshTokenRequest 放 Dtos 目录还是 Common 目录？

**解答**：放在 **Common** 更合适。因为它们没有对应的 Entity 参与 AutoMapper 映射，属于通用请求契约，和 ApiResult、PageResult 同类。Dtos 目录留给有 Entity 对应的 DTO（如 DeviceDto、UserDto）。

---

### Q31：async 方法缺少 async 关键字导致返回类型不匹配

**现象**：AuthService.RefreshTokenAsync 方法体中 `return ApiResult<LoginResponse>.Fail(401, "...")` 报错：
```
无法将类型"ApiResult<LoginResponse>"隐式转换为"Task<ApiResult<LoginResponse>>"
```

**原因**：方法声明缺少 `async` 关键字。非 async 方法不会自动包装返回值为 Task。

**解决**：方法签名加 `async`：
```csharp
public async Task<ApiResult<LoginResponse>> RefreshTokenAsync(...)
```

---

### Q32：AuthController Refresh 方法返回类型错误

**现象**：
```
无法将类型"OkObjectResult"隐式转换为"LoginResponse"
无法将类型"BadRequestObjectResult"隐式转换为"LoginResponse"
```

**原因**：Refresh 方法返回类型声明为 `Task<LoginResponse>`，但方法体内 return 了 `Ok(result)` / `BadRequest(result)`（都是 `IActionResult`）。

**解决**：改为 `Task<IActionResult>`：
```csharp
// 错误
public async Task<LoginResponse> Refresh(...)

// 正确
public async Task<IActionResult> Refresh(...)
```

---

### Q33：User.FindFirst() 是什么？这样操作合理吗？

**疑问**：AuthController.Logout 中 `User.FindFirst(ClaimTypes.NameIdentifier)` 是什么？

**解答**：`User` 是 `ControllerBase` 的属性，来自**中间件解析后的当前请求用户身份**。JWT 中间件验证通过后，把 Token 里的 Claims 还原成 ClaimsPrincipal 存入 HttpContext.User。

这种做法**完全合理且标准**。注销时不需要客户端传 userId（防止伪造），而是直接从 Token 里取。因为 Logout 加了 `[Authorize]`，走到这里说明 Token 已经验证过签名了，里面的 Claims 是可信的。

---

### Q34：设置 RevokedAt = DateTime.UtcNow 就算撤销了吗？

**疑问**：只是改了一个属性值，怎么影响数据库里的记录？

**解答**：**EF Core ChangeTracker 自动追踪**。

流程：
1. `await _tokenRepo.GetByTokenValueAsync(...)` 从数据库加载 RefreshToken 实体
2. EF Core 的 ChangeTracker 自动追踪该实体
3. `storedToken.RevokedAt = DateTime.UtcNow` —— 修改被追踪实体的属性
4. `await _tokenRepo.SaveChangedAsync()` —— ChangeTracker 检测到属性变更，自动生成 UPDATE 语句执行

**为什么改 RevokedAt 就算撤销？**
- 因为 `GetByTokenValueAsync` 查询条件包含 `&& t.RevokedAt == null`
- 一旦 RevokedAt 被赋值，该 Token 就再也查不到了 = 逻辑上已作废

---

### Q35：为什么要对 storedToken 做 Update 操作？

**疑问**：如果是撤销旧 token，直接调用 RevokeAllTokenAsync 不就行了？

**解答**：`RevokeAllUserTokensAsync` 是**撤销某个用户的所有未撤销 Token**（用于注销/强制下线）。而 Refresh 流程中只需要**撤销当前这一个旧 Token**（因为 RefreshToken 是一次性使用的）。

两种场景：
- **刷新 Token**：只撤销当前这一个旧 RefreshToken（设置 RevokedAt）
- **注销/强制下线**：撤销该用户的**所有** RefreshToken（调用 `RevokeAllUserTokensAsync`）

**注意**：当前代码中 `storedToken.RevokedAt = DateTime.UtcNow` 后**没有手动调用 `_tokenRepo.Update(storedToken)`**，这是因为 EF Core 的 ChangeTracker 已经自动追踪了该实体，直接 `SaveChangedAsync()` 即可。

---

### Q36：RefreshToken 的过期策略是什么？

**疑问**：RefreshToken 一直刷新，用户是不是可以一直用下去？

**解答**：**不能**。RefreshToken 有**7 天绝对过期时间**（滑动窗口 + 绝对上限）：
- 每次刷新生成新的 RefreshToken，新 Token 的 ExpiresAt = 当前时间 + 7 天
- 旧 Token 被撤销（RevokedAt = 现在）
- 如果用户 7 天内没有操作，RefreshToken 过期，必须重新登录
- 即使用户每天都在刷新，**最多 7 天后也必须重新登录一次**（因为 AccessToken 只有 2 小时，必须在 RefreshToken 有效期内完成刷新）

---

### Q37：EF Core 的自动追踪功能详解

**疑问**：哪些操作会触发自动追踪？

**解答**：

**会自动追踪的操作**（返回实体）：
- `FirstOrDefaultAsync()` / `FirstOrDefault()`
- `ToListAsync()` / `ToList()`
- `FindAsync()` / `Find()`
- `SingleOrDefaultAsync()` / `SingleOrDefault()`
- `Include()` 后的查询

**不会自动追踪的操作**：
- `Select()` 投影返回 DTO（非实体类型）
- 任何返回匿名类型或非 DbSet 注册类型的查询

**追踪范围**：
- 对返回的实体，EF Core 会追踪该实体**所有映射到数据库列的属性**
- 修改任意属性后，SaveChanges 会自动生成对应的 UPDATE 语句

---

## 七、Modbus 通信问题

### Q38："ModbusSerialMaster"未包含"CreateRtu"的定义

**现象**：尝试使用 `ModbusSerialMaster.CreateRtu(port)` 编译报错。

**原因**：NModbus 3.0.83 的 API 与旧版本不同，没有 `ModbusSerialMaster.CreateRtu` 静态方法。

**解决**：使用 NModbus 3.0.83 的正确 API：
```csharp
var adapter = new SerialPortAdapter(port);           // 包装成 IStreamResource
var factory = new ModbusFactory();                   // 工厂
var transport = factory.CreateRtuTransport(adapter); // 创建 RTU 传输层
var master = factory.CreateMaster(transport);        // 创建 Master
```

> 不需要额外安装 `NModbus.Serial` 包，NModbus 3.0.83 主包已包含 `SerialPortAdapter`。

---

### Q39：Device 表无数据导致 Modbus 服务空转

**现象**：启动后日志显示：
```
warn: ModbusConfigLoader[0] 没有在线的设备
warn: ModbusService[0] 未加载到任何设备配置，服务将空转
```

**原因**：`ModbusConfigLoader.LoadConfigsAsync()` 过滤了 `Status == Offline` 的设备。如果 Device 表为空或所有设备都是 Offline，则加载不到任何配置。

**解决**：
1. 检查数据库 Device 表是否有数据
2. 确保至少有一条记录的 `Status != Offline`
3. 确保 `ComPort` 字段有值（如 "COM2"）
4. 重启服务使配置重新加载

> **注意**：`ModbusConfigLoader.LoadConfigsAsync()` 只在服务启动时执行一次，数据库变更后需要重启服务才能生效。

---

### Q40：Float 解析结果为 0（字节序问题）

**现象**：数据库中电压测点配置正确（RegisterAddress=40007, DataType=Float, ScaleFactor=1），但显示值为 0。

**原因**：`BitConverter` 是平台相关的。Windows 是**小端序**（Little-Endian），而 Modbus 数据是**大端序**（Big-Endian）。直接构造字节数组会导致高/低位颠倒，解析结果错误。

**错误代码**：
```csharp
// 错误！在小端平台上会得到 0 或其他错误值
byte[] bytes = new byte[4];
bytes[0] = (byte)(registers[0] >> 8);
bytes[1] = (byte)registers[0];
bytes[2] = (byte)(registers[1] >> 8);
bytes[3] = (byte)registers[1];
return BitConverter.ToSingle(bytes, 0);
```

**正确代码**：
```csharp
private static float ParseFloatBigEndian(ushort[] registers)
{
    uint intValue = ((uint)registers[0] << 16) | registers[1];
    byte[] bytes = BitConverter.GetBytes(intValue);
    return BitConverter.ToSingle(bytes, 0);
}
```

**原理**：
1. `((uint)registers[0] << 16) | registers[1]` 组合成平台无关的 uint（如 0x43480000）
2. `BitConverter.GetBytes(uint)` 在小端平台自动输出 `[0x00, 0x00, 0x48, 0x43]`
3. `BitConverter.ToSingle` 正确解析为 200.0

---

### Q41：IEnumerable<Task<T>> 是什么？

**疑问**：`configTasks` 为什么是 `IEnumerable<Task<ModbusDeviceConfig>>` 类型？

**解答**：
```csharp
var configTasks = onlineDevices.Select(async device => { ... return config; });
```

- `Select` 是 LINQ 的**延迟执行**方法，返回 `IEnumerable<T>`
- `async lambda` 的返回类型自动包装为 `Task<T>`
- 因此整体类型是 `IEnumerable<Task<ModbusDeviceConfig>>`
- 需要 `await Task.WhenAll(configTasks)` 来解包：
  - `Task.WhenAll` 接收 `IEnumerable<Task>`，等待所有 Task 完成
  - 返回 `Task<T[]>`，`.ToList()` 后得到 `List<ModbusDeviceConfig>`

---

### Q42：Array.Copy 越界问题的完整分析

**疑问**：`Array.Copy(registers, offset, pointRegisters, 0, regCount)` 为什么会越界？

**分析**：越界只可能在以下情况发生：
1. `GroupLength` 计算错误（小于实际需要的寄存器数量）
2. `registers` 数组长度不等于 `GroupLength`

**本项目的设计保证不越界**：
- `GroupLength = expectedNextAddr - groupStartAddr`
- `expectedNextAddr` 每次累加的是测点的**寄存器数量**（Int16=1, Float=2）
- 因此 `GroupLength` = 组内所有测点占用的寄存器**总和**
- `NModbus` 返回的 `registers` 长度 = 请求的 `GroupLength`
- 每个测点的 `offset + regCount <= GroupLength`

**示例**：
```
测点 A: addr=0, Int16(1寄存器)  → expectedNextAddr = 0+1 = 1
测点 B: addr=1, Float(2寄存器)   → expectedNextAddr = 1+2 = 3
测点 C: addr=3, Int16(1寄存器)   → expectedNextAddr = 3+1 = 4

GroupLength = 4 - 0 = 4
registers 长度 = 4

A: offset=0, count=1  → Array.Copy(registers, 0, ..., 0, 1)  // 索引 0，安全
B: offset=1, count=2  → Array.Copy(registers, 1, ..., 0, 2)  // 索引 1~2，安全
C: offset=3, count=1  → Array.Copy(registers, 3, ..., 0, 1)  // 索引 3，安全
```

---

### Q43：为什么用 ConcurrentDictionary 而不是 Dictionary + lock？

**解答**：

| 方案 | 优点 | 缺点 |
|------|------|------|
| Dictionary + lock | 实现简单 | 全局锁，并发性能差 |
| ConcurrentDictionary | 细粒度锁，读几乎无锁，并发性能高 | 内存开销略大 |

**本项目场景**：
- 写入：每秒几十到几百次（轮询所有测点）
- 读取：每次 HTTP 请求读取（查询频率取决于前端操作）
- 并发度中等，用 `ConcurrentDictionary` 是合理选择

---

### Q44：CancellationToken 可以手动创建吗？

**解答**：**可以**。虽然 ModbusService 的 `ExecuteAsync` 的 CancellationToken 由框架自动传入，但你也可以手动创建：

```csharp
var cts = new CancellationTokenSource();
cts.CancelAfter(TimeSpan.FromSeconds(30));  // 30秒后自动取消
await SomeLongRunningOperation(cts.Token);
```

**注意**：手动创建的 CancellationToken 和框架传入的不是同一个，但使用方式完全相同。

---

### Q46：Modbus SlaveException — Illegal Data Address（功能码 0x02）

**现象**：日志重复输出：
```
fail: ModbusDataReader[0] 设备 测试从站设备 分组读取失败 [起始:10 长度:1]
System.Exception: 重试 2 次后仍失败
 ---> NModbus.SlaveException: Function Code: 131, Exception Code: 2
      The data address received in the query is not an allowable address
```

**原因**：从站返回 **异常码 0x02 (Illegal Data Address)**，表示请求的**寄存器地址不在从站的可用范围内**。

读取的实际协议地址经过 `ConvertToProtocolAddress` 转换：
```
数据库存储 PLC 地址 40011 → 减去 40001 → 协议地址 10
```
如果 Modbus Slave 模拟器上起始地址设为 0、Quantity 不够大或者没有配置地址 10 的寄存器，从站就会拒绝请求。

**解决**：
1. **确认从站配置**：Modbus Slave 模拟器中设置 Starting Address = 0，Quantity 足够大（如 50）
2. **确认数据库地址**：检查 DeviceData 表 `RegisterAddress` 字段，确保转换后的协议地址在从站范围内
3. **调试日志输出**：在 `ModbusConfigLoader.LoadConfigsAsync()` 末尾添加调试日志确认实际加载的测点地址：

```csharp
_logger.LogDebug("设备 {Name} 测点 {Point} → 协议地址 {Addr}, 寄存器类型 {Type}",
    cfg.Name, p.Name, p.RegisterAddress, p.RegisterType);
```

**常见地址转换对照（保持寄存器）**：

| 数据库存的值 | 转换公式 | 协议地址 |
|-------------|---------|---------|
| 40001 | 40001-40001 = 0 | 0 |
| 40011 | 40011-40001 = 10 | 10 |
| 40101 | 40101-40001 = 100 | 100 |

Modbus 协议地址从 0 开始编号，PLC 地址从 40001 开始编号，两者偏差 40001。

---

### Q47：Modbus 模块没写线圈和离散输入

**原因**：本项目场景是**模拟量数据采集**（温度、电压、电流等连续值），核心使用保持寄存器（Holding Register）和输入寄存器（Input Register）。线圈（Coil，功能码 01/05）和离散输入（Discrete Input，功能码 02）用于开关量（布尔值），当前业务场景不需要。

**扩展方式**：如需添加，参照现有模式：
```csharp
// 在 ModbusDataReader 中添加
private async Task<bool[]> ReadCoilsAsync(...)
    => await master.ReadCoilsAsync(slaveAddress, startAddress, numberOfPoints);
```

---

## 八、概念疑问汇总

### 三层架构职责再确认

| 层级 | 可以操作 Entity | 可以返回 Entity | 可以返回 DTO |
|------|----------------|----------------|-------------|
| Repository | 是 | 是 | 否（除非特殊查询投影） |
| Service | 通过 Repository | **否** | 是 |
| Controller | **否** | **否** | 是（通过 Service） |

### Repository CRUD vs Service CRUD 的关系

- **Repository CRUD**：对数据库的**原子操作**（增删改查一行/多行）
- **Service CRUD**：对业务的**完整操作**，可能包含：
  - 多次 Repository 调用
  - 业务规则校验（如编码唯一性）
  - 多个实体的事务性操作
  - DTO 映射和组装

示例：Service 的 `CreateDevice` 可能调用 Repository 的 `AddAsync` + `SaveChangedAsync`，但还包含了"检查产线是否存在"、"检查编码是否重复"等业务逻辑。

### SortOrder 的作用

DataPoint 的 `SortOrder` 字段用于**控制前端测点的显示顺序**。数值越小越靠前。Repository 层查询时按 `SortOrder` 排序：
```csharp
await _dbSet.Where(dp => dp.DeviceId == deviceId)
    .OrderBy(dp => dp.SortOrder)
    .ToListAsync();
```

### Device 的 Code 字段意义

`Code` 是设备的**唯一业务编码**（如 "CNC-001"、"INJ-003"），区别于自增主键 `Id`。
- 用途：供人员识别、报表统计、外部系统对接
- 约束：全局唯一（数据库加唯一索引 + Service 层校验）

---

## 八、Day 4 WPF 客户端问题

### Q40：ListBox 导航不跳转 — SelectedItem 只执行 get 不执行 set

**现象**：点击左侧 ListBox 菜单项，`SelectedMenuItem` 的 get 被调用但 set 不执行，导航逻辑未触发。

**原因**（三个叠加问题）：
1. `MenuItems` 属性使用表达式体 `=> new ObservableCollection<...>()`，每次访问返回新实例，绑定失效
2. `SelectedItem="{Binding SelectedMenuItem}"` 默认绑定模式是 OneWay，点击只触发 get
3. `SelectedIndex="0"` 与 `SelectedMenuItem = MenuItems[0]` 同时设置造成冲突

**解决**：
```csharp
// 1. MenuItems 在构造函数中初始化（不是表达式体）
public ObservableCollection<MenuItemModel> MenuItems { get; }
public ShellViewModel(IRegionManager regionManager)
{
    MenuItems = new ObservableCollection<MenuItemModel> { ... };
}

// 2. XAML 中添加 Mode=TwoWay
SelectedItem="{Binding SelectedMenuItem, Mode=TwoWay}"

// 3. 移除 SelectedIndex="0"，不手动设置默认选中
```

---

### Q41：Prism 导航后 MainRegion 空白

**现象**：`RequestNavigate("RealtimeView")` 执行后 MainRegion 区域为空白，无任何内容显示。

**原因**（多个叠加问题）：
1. **RegisterViewWithRegion 干扰**：`DeviceManagementModule.OnInitialized` 中使用了 `RegisterViewWithRegion("MainRegion", ...)`，该方法在模块初始化时自动注入 View，后续导航会被忽略
2. **其他 View 未注册导航**：RealtimeView、AlarmView、SettingsView 没有在 App.xaml.cs 中注册 `RegisterForNavigation`
3. **ViewModel 访问级别错误**：ViewModel 类声明为 `internal`，Prism 容器无法创建实例
4. **View 缺少 AutoWireViewModel**：XAML 中没有 `prism:ViewModelLocator.AutoWireViewModel="True"`

**解决**：
```csharp
// 1. Module 的 OnInitialized 保持为空，移除 RegisterViewWithRegion
public void OnInitialized(IContainerProvider containerProvider) { }

// 2. App.xaml.cs 中注册所有导航 View
containerRegistry.RegisterForNavigation<RealtimeView, RealtimeViewModel>();
containerRegistry.RegisterForNavigation<AlarmView, AlarmViewModel>();
containerRegistry.RegisterForNavigation<SettingsView, SettingsViewModel>();

// 3. ViewModel 声明为 public + 继承 BindableBase
public class RealtimeViewModel : BindableBase { }

// 4. View XAML 添加 AutoWireViewModel
prism:ViewModelLocator.AutoWireViewModel="True"
```

---

### Q42：ShellView 的 RegionManager 未绑定

**现象**：登录成功后 ShellView 显示，但左侧菜单导航时 MainRegion 找不到，导航静默失败。

**原因**：Prism 只有 `CreateShell()` 返回的窗口才会自动绑定 RegionManager。本项目 `CreateShell()` 返回 LoginView，后续通过 `Container.Resolve<ShellView>()` 创建的 ShellView 不会自动获得 RegionManager。

**解决**：在 ShellService 中手动绑定 RegionManager：
```csharp
public void LoginToShell()
{
    var shellView = _container.Resolve<ShellView>();
    Application.Current.MainWindow.Close();
    RegionManager.SetRegionManager(shellView, _regionManager);  // 关键
    Application.Current.MainWindow = shellView;
    shellView.Show();
}
```

---

### Q43：IDeviceService 未注册导致导航失败

**现象**：导航到 DeviceListView 时崩溃，提示无法解析 IDeviceService。

**原因**：`IDeviceService` 在 App.xaml.cs 的 `RegisterTypes` 中遗漏注册。DeviceListViewModel 构造函数需要注入 IDeviceService，容器找不到会抛异常。

**解决**：
```csharp
containerRegistry.RegisterSingleton<IDeviceService, DeviceService>();
```

---

### Q44：DeviceEditView 命名空间不匹配

**现象**：XAML 中 `x:Class` 与 code-behind 的命名空间不一致导致编译错误。

**原因**：XAML 使用 `DeviceManagement`（正确拼写），code-behind 使用 `DeviceMangement`（拼写错误）。

**解决**：统一使用 `DeviceMangement`（虽然拼写错误，但项目内保持一致，避免大面积重命名）。

---

### Q45：DeviceEditView ComboBox 无内容

**现象**：设备编辑弹窗中"所属产线"ComboBox 展开后为空白。

**原因**：`DeviceEditViewModel.LoadAsync` 中调用了 `_productionLineService.GetAllAsync()` 但忘记将结果赋值给 `ProductionLines` 属性。

**解决**：
```csharp
// 错误：只调用方法，未赋值
await _productionLineService.GetAllAsync();

// 正确：赋值给绑定属性
ProductionLines = await _productionLineService.GetAllAsync();
```

---

### Q46：ProductionLineService 返回类型不匹配

**现象**：方法签名返回 `ApiResult<List<ProductionLineDto>>` 但方法体内 `return result.Data`，类型不匹配。

**原因**：接口定义返回 `ApiResult<...>`，实现中直接返回了 `result.Data`（即 `List<ProductionLineDto>`）。

**解决**：统一接口和实现的返回类型为 `List<ProductionLineDto>?`：
```csharp
// 接口
Task<List<ProductionLineDto>?> GetAllAsync();

// 实现
public async Task<List<ProductionLineDto>?> GetAllAsync()
{
    var response = await _http.GetAsync("api/productionlines");
    if (!response.IsSuccessStatusCode) return null;
    var result = await response.Content.ReadFromJsonAsync<ApiResult<List<ProductionLineDto>>>(_jsonOptions);
    return result?.Code == 200 ? result.Data : null;
}
```

---

### Q47：AsyncDelegateCommand<int> NullReferenceException

**现象**：DataGrid 中点击"编辑"或"删除"按钮时抛出 `NullReferenceException`，堆栈指向 `DelegateCommandBase.CanExecute`。

**原因**：`AsyncDelegateCommand<int>` 的泛型参数是 `int`（非空值类型）。DataGrid 模板中 `CommandParameter="{Binding Id}"`，在行未完全加载或模板初始化阶段，Binding 未就绪，CommandParameter 的值为 `DependencyProperty.UnsetValue`。Prism 内部尝试将 `UnsetValue` 转换为 `int` 时失败，抛出 NullReferenceException。

**解决**：将泛型参数改为可空类型 `int?`：
```csharp
// 错误
public AsyncDelegateCommand<int> EditCommand { get; }
EditCommand = new AsyncDelegateCommand<int>(ExecuteEdit);

// 正确
public AsyncDelegateCommand<int?> EditCommand { get; }
EditCommand = new AsyncDelegateCommand<int?>(ExecuteEdit);

private async Task ExecuteEdit(int? id)
{
    if (id == null) return;
    // ...
}
```

---

### Q48：DataGrid 选中行信息看不到

**现象**：点击 DataGrid 行后，选中行的背景变蓝、文字变白，与浅色行背景对比度极低导致信息几乎不可见。

**原因**：WPF DataGrid 默认选中样式覆盖了自定义样式。系统使用 `SystemColors.HighlightBrush`（蓝色背景）和 `SystemColors.HighlightTextBrush`（白色文字）。即使自定义了 RowStyle，Cell 级别的默认选中样式仍会覆盖。

**解决**：自定义 CellTemplate，不包含任何选中触发器，通过覆盖系统选中画刷确保选中时无视觉变化：
```xml
<Style TargetType="DataGridCell">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="DataGridCell">
                <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <!-- 无 IsSelected 触发器，选中时无视觉变化 -->
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>
```

关键点：必须在 Cell 级别自定义 Template，仅在 Row 级别覆盖是不够的，因为系统默认的 Cell 选中样式优先级更高。

---

### Q49：EF Core DbContext 并发访问异常

**现象**：`ModbusConfigLoader.LoadConfigsAsync()` 中使用 `Select(async ...)` + `Task.WhenAll()` 并发查询，抛出 `InvalidOperationException: A second operation was started on this context instance before a previous operation completed.`

**原因**：EF Core 的 DbContext **不是线程安全的**，不支持并发访问。`Select(async)` 会同时启动多个异步操作，它们并发访问同一个 DbContext 实例。

**解决**：改为串行执行：
```csharp
// 错误：并发访问
var tasks = devices.Select(async d => await LoadDeviceConfigAsync(d));
await Task.WhenAll(tasks);

// 正确：串行执行
foreach (var device in devices)
{
    await LoadDeviceConfigAsync(device);
}
```

---

### Q50：EF Core 外键约束冲突

**现象**：创建设备时抛出 `DbUpdateException: FOREIGN KEY constraint conflict`，ProductionLineId=0 在 ProductionLines 表中不存在。

**原因**：新增设备时未选择产线，`ProductionLineId` 默认值为 0，而数据库中不存在 Id=0 的产线记录。

**解决**：确保创建设备前数据库中已有产线数据，或在 UI 中校验 ProductionLineId 必须大于 0。

---

### Q51：Prism 9.0 IDialogAware 接口变化

**现象**：编译报错，`IDialogAware` 的 `RequestClose` 不是 event，无法使用 `+=` 订阅。

**原因**：Prism 9.0 将 `RequestClose` 从 `event Action<IDialogResult>` 改为 `DialogCloseListener` 属性。同时命名空间从 `Prism.Services.Dialogs` 变为 `Prism.Dialogs`。

**解决**：
```csharp
// Prism 9.0 正确写法
public DialogCloseListener RequestClose { get; }

// 关闭弹窗
RequestClose.Invoke(new DialogResult(ButtonResult.OK));

// 命名空间
using Prism.Dialogs;  // 不是 Prism.Services.Dialogs
```

---

### Q52：OnDialogOpened 的 async void 异常安全问题

**现象**：在 `OnDialogOpened` 中使用 `async void` 加载数据，异常时导致应用崩溃。

**原因**：`async void` 中的未捕获异常会直接抛到同步上下文（通常导致应用崩溃），不像 `async Task` 那样可以被调用方捕获。

**解决**：使用 Fire-and-Forget 模式，将异步逻辑委托给返回 Task 的方法：
```csharp
public void OnDialogOpened(IDialogParameters parameters)
{
    _ = LoadAsync(parameters);  // Fire-and-Forget
}

private async Task LoadAsync(IDialogParameters parameters)
{
    try
    {
        // 异步加载逻辑...
    }
    catch (Exception ex)
    {
        // 安全捕获异常
        ErrorMessage = $"加载数据失败：{ex.Message}";
        HasError = true;
    }
}
```

---

### Q53：HttpClient BaseAddress 末尾斜杠问题

**现象**：HttpClient 的 `GetAsync("api/devices")` 请求发送到了错误的 URL。

**原因**：`BaseAddress` 末尾不带 `/` 时（如 `http://localhost:5078`），相对路径 `api/devices` 会替换整个路径段，变成 `http://localhost:5078/api/devices`（看似正确但实际拼接行为不确定）。当 BaseAddress 末尾带 `/`（如 `http://localhost:5078/`），相对路径 `api/devices` 会正确拼接为 `http://localhost:5078/api/devices`。

**解决**：
```csharp
// 正确：BaseAddress 末尾带 /
private const string BaseUrl = "http://localhost:5078/";

// 相对路径不带前导 /
await _http.GetAsync("api/devices");  // 不是 "/api/devices"
```

---

## 九、Day 5 实时监控页面问题

### Q54：LiveCharts2 命名空间错误 — Axis、Separators、Labelers 不存在

**现象**：按照 LiveCharts1 的写法配置 X/Y 轴时编译报错，找不到 `Axis`、`Separators`、`Labelers` 等类型。

**原因**：LiveCharts2（2.0.5）的 API 与 LiveCharts1 完全不同。LiveCharts2 大幅简化了配置：
- 不再需要手动配置 `Axis.Separators`、`Axis.Labelers`
- 直接绑定 `Series`、`XAxes`、`YAxes` 即可
- `Axis` 类在 `LiveChartsCore.SkiaSharpView` 命名空间下

**解决**：使用 LiveCharts2 的简化配置：
```csharp
using LiveChartsCore.SkiaSharpView;

// X 轴配置
TrendXAxes = new Axis[]
{
    new Axis { Name = "时间 (秒)", Labels = null }
};

// Y 轴配置
TrendYAxes = new Axis[]
{
    new Axis { Name = "数值", MinLimit = null, MaxLimit = null }
};

// Series 配置
TrendSeries = new ISeries[]
{
    new LineSeries<double>
    {
        Values = values,
        Stroke = new SolidColorPaint(SKColors.DodgerBlue) { StrokeThickness = 2 },
        Fill = null
    }
};
```

---

### Q55：ComboBox 绑定 DataPoints 导致显示空白

**现象**：ComboBox 绑定 `DataPoints` 集合后，初始显示正常，但几秒后变成空白，SelectedItem 也丢失。

**原因**：`DataPoints` 是实时数值面板的数据源，每 2 秒刷新时会重新创建整个集合：
```csharp
DataPoints = new ObservableCollection<DataPointDisplayModel>(pointsDisplay);
```
这导致 ComboBox 绑定的集合被替换，SelectedItem 引用的对象也失效。

**解决**：ComboBox 绑定独立的字符串数组，只包含测点名称：
```csharp
// 错误：绑定实时刷新的对象集合
<ComboBox ItemsSource="{Binding DataPoints}"
          DisplayMemberPath="Name"
          SelectedItem="{Binding SelectedTrendPoint}"/>

// 正确：绑定独立的字符串数组
public string[] TrendPointNames { get; set; }

<ComboBox ItemsSource="{Binding TrendPointNames}"
          SelectedItem="{Binding SelectedTrendPoint}"/>
```

---

### Q56：LiveCharts2 趋势图空白不显示

**现象**：CartesianChart 控件加载后显示为空白区域，没有任何曲线。

**原因**（多个叠加问题）：
1. **缺少 Height 设置**：趋势图在 Grid 中使用 `Height="*"`，但父容器没有设置高度
2. **数据集合首次加载为空**：初始化时创建的 `ObservableCollection<double>` 没有初始值
3. **Series 绑定的数据源为 null**：首次选中设备时未初始化 Series

**解决**：
```xml
<!-- 1. 使用 Grid 结构设置高度 -->
<Grid Grid.Row="1">
    <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <!-- 趋势图使用 Height="*" -->
    <lvc:CartesianChart Grid.Row="1"
                        Series="{Binding TrendSeries}"
                        XAxes="{Binding TrendXAxes}"
                        YAxes="{Binding TrendYAxes}"/>
</Grid>
```

```csharp
// 2. 初始化时装入初始值
_trendDataCollections[p.DataPointId] = new ObservableCollection<double> { p.Value };

// 3. 首次选中设备时初始化 Series
private void InitializeTrendChart(RealtimeDeviceDataDto device)
{
    foreach (var p in device.DataPoints)
    {
        _trendDataCollections[p.DataPointId] = new ObservableCollection<double> { p.Value };
    }
    UpdateTrendSeries();  // 绑定 Series
}
```

---

### Q57：ShellViewModel 构造函数中导航失败

**现象**：在 ViewModel 构造函数中设置 `SelectedMenuItem = MenuItems[2]`，左侧导航栏显示选中，但右侧 MainRegion 没有加载对应界面。

**原因**：构造函数执行时，`ShellView` 的 `MainRegion` 可能还未完成创建和注册。此时调用 `_regionManager.RequestNavigate("MainRegion", viewName)` 会被忽略或静默失败。

**解决**：使用 Dispatcher 延迟导航，确保 Region 已创建：
```csharp
public ShellViewModel(IRegionManager regionManager)
{
    _regionManager = regionManager;
    
    MenuItems = new ObservableCollection<MenuItemModel> { ... };
    
    // 使用 Dispatcher 延迟导航
    Application.Current.Dispatcher.InvokeAsync(() =>
    {
        SelectedMenuItem = MenuItems[2];
    }, DispatcherPriority.Loaded);
}
```

> **DispatcherPriority.Loaded**：在视图加载完成后执行，确保 Region 已注册。

---

## 十、Day 6 报警系统问题

### Q58：低限报警不生成 — 检测逻辑嵌套错误

**现象**：测点数据超出高限后数据库有报警记录，改为超出低限后没有生成新的报警记录。

**原因**：低限报警检测被**嵌套在高限判断内部**，导致逻辑错误：
```csharp
// 错误：低限检测嵌套在高限条件内
if (currentValue > AlarmHigh)
{
    await CreateAlarmIfNotExistsAsync(..., AlarmType.HighLimit, ...);
    
    // 只有同时满足 > High 且 < Low 才会检测低限（不可能）
    if (currentValue < AlarmLow)
    {
        await CreateAlarmIfNotExistsAsync(..., AlarmType.LowLimit, ...);
    }
}
```

**解决**：将低限报警检测移出高限判断，改为并列关系：
```csharp
// 正确：高限和低限检测独立并列
if (currentValue > AlarmHigh)
{
    await CreateAlarmIfNotExistsAsync(..., AlarmType.HighLimit, ...);
}

if (currentValue < AlarmLow)
{
    await CreateAlarmIfNotExistsAsync(..., AlarmType.LowLimit, ...);
}
```

---

### Q59：AlarmRepository.Where 缺少赋值导致筛选失效

**现象**：查询 `GET /api/alarms/history?deviceId=1`，返回 `totalCount: 8`，但 `items` 只有 1 条记录（属于其他设备）。

**原因**：`query.Where(...)` 返回新的 `IQueryable`，但没有重新赋值给 `query` 变量：
```csharp
// 错误：Where 的结果被丢弃
if (deviceId.HasValue)
    query.Where(a => a.DeviceId == deviceId);  // 缺少 query =
```

后续的 `CountAsync()` 和 `Skip().Take()` 仍然基于未过滤的原始查询。

**解决**：
```csharp
// 正确：重新赋值
if (deviceId.HasValue)
    query = query.Where(a => a.DeviceId == deviceId);
```

---

### Q60：AlarmService 参数传递错误导致分页失效

**现象**：请求 `page=1, pageSize=20`，但返回结果只有 1 条记录。

**原因**：Service 层调用 Repository 时参数顺序错误：
```csharp
// 错误：第二个参数传了 page 而非 pageSize
var paged = await _alarmRepo.GetHistoryAsync(page, page, deviceId, levelEnum, statusEnum);
//                                               ↑      ↑
//                                             page   page（错误）
```

导致查询变成 `Skip(0).Take(1)`，只返回 1 条记录。

**解决**：
```csharp
// 正确：第二个参数是 pageSize
var paged = await _alarmRepo.GetHistoryAsync(page, pageSize, deviceId, levelEnum, statusEnum);
//                                               ↑      ↑↑↑↑↑↑↑
//                                             page   pageSize
```

---

### Q61：Alarm 主键类型不匹配导致 FindAsync 异常

**现象**：调用 `PUT /api/alarms/{id}/acknowledge` 时抛出 `ArgumentException: The key value at position 0 of the call to 'DbSet<Alarm>.Find' was of type 'int', which does not match the property type of 'long'.`

**原因**：
- `Alarm` 实体的主键 `Id` 类型是 `long`
- `RepositoryBase.GetByIdAsync(int id)` 参数类型是 `int`
- `FindAsync` 内部检查参数类型与主键类型必须一致

**解决**：在 `AlarmRepository` 中直接使用 `_dbSet.FindAsync(alarmId)`：
```csharp
// 错误：基类方法参数类型不匹配
var alarm = await GetByIdAsync((int)alarmId);

// 正确：直接调用 FindAsync，参数为 long
public async Task<bool> AcknowledgeAsync(long alarmId, string acknowledgedBy)
{
    var alarm = await _dbSet.FindAsync(alarmId);  // 参数类型为 object，运行时检查
    if (alarm == null || alarm.Status == AlarmStatus.Acknowledged)
        return false;

    alarm.Status = AlarmStatus.Acknowledged;
    alarm.AcknowledgedBy = acknowledgedBy;
    alarm.AcknowledgedAt = DateTime.UtcNow;

    Update(alarm);
    await SaveChangedAsync();
    return true;
}
```

**根本原因**：`RepositoryBase<T>` 的 `GetByIdAsync(int id)` 设计为 `int` 类型主键，但 `Alarm.Id` 是 `long` 类型。泛型约束无法表达"主键类型"，需要运行时处理。

---

### Q62：AlarmsController 注入非泛型 ILogger 导致 DI 解析失败

**现象**：访问 `/api/alarms/current` 或 `/api/alarms/history` 时抛出 `InvalidOperationException: Unable to resolve service for type 'Microsoft.Extensions.Logging.ILogger' while attempting to activate 'Server.Infrastructure.Controllers.AlarmsController'`。

**原因**：Controller 构造函数注入的是 `ILogger`（非泛型），但 ASP.NET Core DI 容器中只注册了泛型版本 `ILogger<T>`。非泛型 `ILogger` 没有被注册，无法解析。

**错误代码**：
```csharp
public AlarmsController(IAlarmService alarmService, ILogger logger)  // ← 错误
```

**解决**：使用泛型日志接口：
```csharp
private readonly IAlarmService _alarmService;
private readonly ILogger<AlarmsController> _logger;

public AlarmsController(IAlarmService alarmService, ILogger<AlarmsController> logger)
{
    _alarmService = alarmService;
    _logger = logger;
}
```

> **当前代码状态**：`AlarmsController` 未使用日志，已将 `ILogger` 完全移除，构造函数仅保留 `IAlarmService`。

---

### Q63：Client AlarmService URL 末尾斜杠导致服务端参数解析失败

**现象**：报警中心历史报警页面没有数据，请求发送后服务端返回错误或无数据。

**原因**：URL 拼接时在 `pageSize` 参数后多加了 `/`：
```csharp
var url = $"api/alarms/history?page={page}&pageSize={pageSize}/";  // ← 错误
```

实际请求的 URL 变成 `api/alarms/history?page=1&pageSize=20/&status=...`，服务端将 `pageSize` 解析为 `"20/"` 字符串，导致模型绑定失败。

**解决**：删除末尾斜杠：
```csharp
var url = $"api/alarms/history?page={page}&pageSize={pageSize}";  // ← 正确
```

---

### Q64：AlarmViewModel SelectedStatus setter 字段错误导致筛选无效

**现象**：报警中心切换"报警状态"ComboBox 后，历史报警列表没有按新状态筛选；查询历史和刷新当前都没有数据。

**原因**：`SelectedStatus` 的 setter 中 `SetProperty` 传错了字段：
```csharp
public string SelectedStatus
{
    get => _selectedStatus;
    set
    {
        if (SetProperty(ref _selectedLevel, value))  // ← 错误！应该是 _selectedStatus
        {
            _currentPage = 1;
            _ = LoadHistoryPageAsync();
        }
    }
}
```

这导致 `_selectedStatus` 字段从未被更新，`LoadHistoryPageAsync` 读取的始终是初始值 `"Unacknowledged"`。

**解决**：
```csharp
public string SelectedStatus
{
    get => _selectedStatus;
    set
    {
        if (SetProperty(ref _selectedStatus, value))  // ← 正确
        {
            _currentPage = 1;
            _ = LoadHistoryPageAsync();
        }
    }
}
```

**附带建议**：历史报警默认显示所有状态更合理：
```csharp
private string _selectedStatus = "全部";
```

---

### Q65：报警中心无数据的排查思路

**现象**：报警中心页面空白，无论点击"查询历史"还是"刷新当前"都没有数据。

**排查步骤**：

1. **直接访问 API 验证服务端**：
   ```
   GET http://localhost:5078/api/alarms/current
   GET http://localhost:5078/api/alarms/history?page=1&pageSize=20
   ```
   - 如果 API 返回数据 → 问题在客户端
   - 如果 API 报错 → 根据错误信息修复服务端

2. **检查数据库**：
   ```sql
   SELECT COUNT(*) FROM Alarms;
   SELECT TOP 10 * FROM Alarms ORDER BY OccurredAt DESC;
   ```

3. **检查服务端日志**：Controller/Service/Repository 是否有异常

4. **在客户端加调试日志**：
   ```csharp
   System.Diagnostics.Debug.WriteLine($"Status={response.StatusCode}");
   var content = await response.Content.ReadAsStringAsync();
   System.Diagnostics.Debug.WriteLine(content);
   ```

**本项目遇到的根因链**：
```
Client URL 斜杠 → 服务端参数解析异常 → 修复后 ILogger 注入错误 → API 仍不可用
→ 修复 ILogger 后 API 正常 → 但 SelectedStatus setter 字段错误 → 筛选不生效
```

> **当前状态**：以上问题在当前代码中均已修复。

---

## 十一、Day 7 历史数据归档与查询问题

### Q66：历史趋势图无曲线 — 前后端查询参数名不一致（pointId vs dataPointId）

**现象**：`DeviceDatas` 表里已有归档的实时数据，但历史趋势图查询后没有曲线；同样的时间范围下，统计面板却有数据。

**原因**：客户端 data 接口的 URL 参数名用的是 `pointId`，而服务端控制器绑定的是 `dataPointId`：

```csharp
// 客户端 HistoryService
var url = $"api/history/data?deviceId={deviceId}&pointId={dataPointId}&start={start:O}&end={end:O}";  // ← pointId

// 服务端 HistoryController
[FromQuery] int deviceId,
[FromQuery] int dataPointId,   // ← 期待 dataPointId
```

参数名对不上 → 服务端 `dataPointId` 绑定失败，恒为默认值 **0** → 查询 `d.DataPointId == 0` → 查不到任何数据 → 曲线为空。而 statistics 接口两端都用 `pointId`，能正常绑定，所以统计能显示——形成"统计有、曲线没有"的典型现象。

**解决**：统一参数名（推荐客户端改成与服务端一致）：
```csharp
var url = $"api/history/data?deviceId={deviceId}&dataPointId={dataPointId}&start={start:O}&end={end:O}";
```

**教训**：`[FromQuery]` 参数绑定失败**不会报错**，只是静默取默认值，极难排查。前后端接口参数名应形成契约（统一命名规范）。

---

### Q67：EPPlus 8 LicenseNotSetException — 许可证 API 变更

**现象**：导出 Excel 时抛出 `OfficeOpenXml.LicenseNotSetException`，栈指向 `new ExcelPackage()`（HistoryController.Export）。

**原因**：项目使用的 EPPlus 是 **8.6.3**，EPPlus 8 起强制要求设置许可证。旧的 `ExcelPackage.LicenseContext` 属性已标记过时（will be removed in coming versions），且未设置任何许可证信息。

**解决**：
```csharp
// EPPlus 5~7（已过时）
ExcelPackage.LicenseContext = LicenseContext.NonCommercial;

// EPPlus 8+（新 API，三选一）
OfficeOpenXml.ExcelPackage.License.SetNonCommercialPersonal("你的姓名");        // 个人非商用
OfficeOpenXml.ExcelPackage.License.SetNonCommercialOrganization("组织名称");     // 非营利组织
OfficeOpenXml.ExcelPackage.License.SetCommercial("许可证密钥");                  // 企业商用（需购买）
```

要求：必须放在 `new ExcelPackage()` **之前**执行，且**全局只调用一次**；企业商业项目不得使用 `NonCommercialPersonal`（违反 Polyform 协议）。

---

### Q68：HistoryViewModel 编译错误 — 主构造函数与普通构造函数冲突

**现象**：编译报错 `在带参数列表的类型中声明的构造函数必须拥有"this"构造函数初始化表达式`。

**原因**：类声明用了**主构造函数**（C# 12）：

```csharp
public class HistoryViewModel(
    IProductionLineService _lineService, ...) : BindableBase, INavigationAware
```

同时又声明了一个**无参构造函数**（初始化时间、命令、坐标轴）。C# 12 规则：带主参数列表的类型**不允许再声明独立构造函数**（除非用 `this(...)` 链回主构造函数）。

**解决**：去掉主构造函数，改回传统构造注入（与项目其他 ViewModel 如 AlarmViewModel 风格统一，Prism 容器也只会解析带参数的构造函数）：

```csharp
public class HistoryViewModel : BindableBase, INavigationAware
{
    private readonly IProductionLineService _lineService;
    private readonly IRealtimeService _realtimeService;
    private readonly IHistoryService _historyService;

    public HistoryViewModel(
        IProductionLineService lineService,
        IRealtimeService realtimeService,
        IHistoryService historyService)
    {
        _lineService = lineService;
        _realtimeService = realtimeService;
        _historyService = historyService;

        _endTime = DateTime.Now;
        _startTime = _endTime.AddHours(-1);
        // ...命令与坐标轴初始化
    }
}
```

**教训**：主构造函数和普通构造函数不能混用；即使能编译，无参构造 + 主构造并存也会让 DI/Prism 的构造函数选择变得不确定。

---

### Q69：Excel 导出方案演进 — EPPlus → CSV（零第三方依赖）

**背景**：EPPlus 的许可证配置繁琐（8.x API 变更 + 非商用协议约束 + 需在 Program.cs 全局初始化）。

**演进路线**：
1. **EPPlus**（许可证麻烦，弃用）
2. **ClosedXML**（MIT 完全开源，可替代 EPPlus，但仍是第三方依赖）
3. **CSV**（最终方案，零第三方依赖）

**CSV 方案实现要点**（HistoryController.Export）：
- `StringBuilder` 拼接表头 + 数据行；
- **BOM**：`Encoding.UTF8.GetPreamble()` 前缀，保证 Excel 打开中文 CSV 不乱码；
- **转义**：字段含逗号、引号、换行时用双引号包裹，内部引号翻倍（`"` → `""`）；
- Content-Type：`text/csv; charset=utf-8`，文件名后缀 `.csv`；
- 前端保存对话框 Filter 改为 `CSV 文件 (*.csv)|*.csv`，文件名后缀 `.csv`；
- 清理：删除 EPPlus 包引用和 Program.cs 中全部许可证代码。

**收益**：彻底绕开许可证问题，导出逻辑更简单，接口契约不变（仍是返回字节流）。

---

## 十二、Day 8 系统配置（SystemConfig）落地问题

### Q70：编译错误 CS0246 — 找不到类型"SystemSettingsDto"（缺 using + 缺文件）

**现象**：`ISystemConfigService.cs` 报 `未能找到类型或命名空间名"SystemSettingsDto"(是否缺少 using 指令或程序集引用?)`，指向 `SaveAllAsync(List<SystemSettingsDto>)` 签名。

**原因**：两个原因叠加：
1. **类文件根本没创建**——只新建了 `ISystemConfigService.cs` 接口文件，`Server/Models/Dtos/SystemSettingsDto.cs` 还不存在，编译器找不到这个类型（根本原因）；
2. **接口代码块缺 using**——`SystemSettingsDto` 定义在 `Server.Models.Dtos` 命名空间，接口在 `Server.Services` 命名空间，跨命名空间引用必须显式 `using`。ImplicitUsings 只覆盖 `System.*` / `Microsoft.Extensions.*`，**不覆盖项目自定义命名空间**。

**解决**：
```csharp
// ① 创建 Server/Models/Dtos/SystemSettingsDto.cs
using System.ComponentModel.DataAnnotations;

namespace Server.Models.Dtos
{
    public class SystemSettingsDto
    {
        [Required]
        public string Key { get; set; } = string.Empty;

        [Required]
        public string Value { get; set; } = string.Empty;

        public string? Description { get; set; }
    }
}

// ② ISystemConfigService.cs 顶部补 using
using Server.Models.Dtos;
```

**教训**：落地模块时要按依赖链顺序建文件（实体 → DTO → 仓储接口/实现 → 服务接口/实现），"只建一个文件"必然引发连锁编译错误；自定义命名空间必须手动 using，别指望 ImplicitUsings。

---

### Q71：dotnet ef migrations add 报 Build failed — 接口与实现方法名不一致（CS0535）

**现象**：`dotnet ef migrations add AddSystemConfig` 提示 `Build failed. Use dotnet build to see the errors.`；`dotnet build` 后看到：

```
error CS0535: "SystemConfigService"不实现接口成员"ISystemConfigService.SaveAsync(string, string, string?)"
error CS0535: "SystemConfigService"不实现接口成员"ISystemConfigService.SaveAllAsync(List<SystemSettingsDto>)"
```

**原因**：文档把 `SaveAsync` 改名为 `AddOrUpdateAsync` 后，**实现类**同步用了新名（且批量方法自定义成了 `AddOrUpdateAllAsync`），但**接口**还保留旧名 `SaveAsync` / `SaveAllAsync`。C# 要求接口成员必须被实现类一一实现，名字对不上就报 CS0535。EF Core 工具执行迁移前会先编译项目，编译不过自然加不了迁移。

**解决**：接口与实现对齐（向实现类靠拢）：

```csharp
// ISystemConfigService.cs
Task AddOrUpdateAsync(string key, string value, string? description = null);   // 原 SaveAsync
Task AddOrUpdateAllAsync(List<SystemSettingsDto> settings);                     // 原 SaveAllAsync
```

**教训**：
1. `dotnet ef` 报 Build failed 时先 `dotnet build` 看 C# 编译错误，错误根因往往不是迁移本身；
2. 方法改名必须**接口 + 实现 + 调用方三处同步**（单一事实来源），只改一处必然 CS0535。

---

### Q72：LoadAsync 里解析不到 GetAllAsync — GetRequiredService 拿错接口

**现象**：`SystemConfigService.LoadAsync()` 里 `systemConfigRepo` 调用 `.GetAllAsync()` 报"没有此方法"。

**原因**：解析的是服务接口而不是仓储接口：

```csharp
var systemConfigRepo = scope.ServiceProvider.GetRequiredService<ISystemConfigService>();  // ← 拿错了
// GetAllAsync 定义在仓储接口 ISystemConfigRepository（继承自 IRepositoryBase<TEntity>）里，
// 服务接口 ISystemConfigService 只有 LoadAsync/Get/AddOrUpdate 等方法
```

**解决**：

```csharp
var systemConfigRepo = scope.ServiceProvider.GetRequiredService<ISystemConfigRepository>();
// 文件顶部需补：using Server.Infrastructure.Repositories;
```

**教训**：单例服务用 `IServiceScopeFactory` 开作用域解析 Scoped 服务时，应解析**仓储**（数据访问入口），而不是**服务**——服务会再依赖仓储形成"服务→服务→仓储"三层调用，绕远路且易拿错接口。解析入口尽量贴底层。

---

### Q73：私有字段命名 _DefaultConfigs 不符合规范 — IDE0055 黄波浪线

**现象**：IDE 对 `private readonly Dictionary<...> _DefaultConfigs = new()` 标出命名分析警告。

**原因**：私有字段（含 `readonly` / `static readonly`）命名规范是 **`_camelCase`**（下划线 + 小驼峰），`_` 后跟大写 D 违反 IDE0055。

**解决**：

```csharp
private readonly Dictionary<string, (string Value, string Description)> _defaultConfigs = new();
```

**规则**：方法名和 `const` 用 **PascalCase**；`private` 字段统一 **`_camelCase`**。文档与代码同步命名（本文档已统一为 `_defaultConfigs`）。

---

### Q74（知识点）：为什么 LoadAsync / EnsureDefaultConfigsAsync 不能放构造函数

**背景**：曾考虑把配置加载写进 `SystemConfigService` 构造函数，省掉 Program.cs 启动初始化代码。

**结论**：**不能**。原因链：
1. **构造函数不能是 `async`**（C# 语言限制，无法 `await`）；
2. 改用 `_ = LoadAsync()`（fire-and-forget）：不等待，后台服务可能读到**空缓存**（竞态），异步异常还会**丢失**；
3. 改用 `.GetAwaiter().GetResult()`：数据库 IO 阻塞在构造函数，异常直接炸 DI 容器，且第一个使用方要多等一次完整查库。

**正确做法**：构造函数只做字段赋值；需要 IO 的初始化放显式方法，由宿主在启动时调用（Program.cs 的 `EnsureDefaultConfigsAsync()` + `LoadAsync()`），或由后台服务 `ExecuteAsync` 启动时 `await` 兜底。

**原则**：构造函数只组装依赖，不做 IO 副作用（依赖注入最佳实践）。

---

### Q75：setting.json 带 `//` 注释导致 JsonReaderException — "'/' is an invalid start of a value"

**现象**：启动时 `JsonNode.Parse` 抛 `JsonReaderException`，LineNumber: 0 | BytePositionInLine: 0。

**原因**：setting.json 第一行写了 `// Modbus 配置` 注释。JSON 标准**不支持注释**，`JsonNode.Parse` 默认不跳过注释，读到第一个 `/` 直接抛异常。

**解决（二选一）**：

方案 A（推荐）：删掉文件里所有注释行，让文件以 `{` 开头，代码零改动。

方案 B：解析时传入选项，允许注释：

```csharp
var docOptions = new JsonDocumentOptions
{
    CommentHandling = JsonCommentHandling.Skip,   // 跳过 // 和 /* */ 注释
    AllowTrailingCommas = true
};
_root = JsonNode.Parse(json, nodeOptions: null, documentOptions: docOptions)?.AsObject() ?? new JsonObject();
```

---

### Q76：Flatten 递归把 PortMappings 对象"拆碎"— 从站端口无映射 / 主站端口不可用

**现象**：启动日志连续出现三条警告：
```
warn: 从站端口 COM2 无对应的主站端口映射
warn: 主站端口 COM1 不可用
```

**原因链**：`Modbus.PortMappings` 在 setting.json 里是对象 `{"COM1":"COM2","COM3":"COM4"}`。`Flatten` 遇到 `JsonObject` 一律递归下钻，缓存里只有 `Modbus.PortMappings.COM1`、`Modbus.PortMappings.COM3` 两个碎键，**没有** `Modbus.PortMappings` 整键 → `GetValueStr("Modbus.PortMappings")` 返回 null → 端口映射字典为空 → 任何端口都没打开 → 后续 `GetPort("COM1")` 返回 null 报"不可用"。

**解决**：让 `Flatten` 区分"容器对象"（继续下钻，如 `Modbus`）和"叶子对象"（子节点全是基元，如 `PortMappings`，整体存 JSON 字符串）：

```csharp
switch (node)
{
    // 容器对象（含嵌套对象/数组）→ 继续下钻
    case JsonObject inner when inner.Any(kv => kv.Value is JsonObject or JsonArray):
        Flatten(inner, key);
        break;
    // 叶子对象（子节点全是基元）→ 整体存 JSON 字符串
    case JsonObject leaf:
        _cache[key] = leaf.ToJsonString();
        break;
    case JsonValue v when v.TryGetValue<string>(out var s):
        _cache[key] = s;
        break;
    default:
        _cache[key] = node?.ToString() ?? string.Empty;
        break;
}
```

**注意**：`JsonObject` 没有公开 `Values` 属性（只有 `IEnumerable<KeyValuePair<string, JsonNode?>>` 枚举），判断子节点用 `inner.Any(kv => kv.Value is JsonObject or JsonArray)`，不能写 `.Values.Any(...)`。

**验证前提**：机器上要真实存在 COM1/COM2 一对串口（测试可用 com0com/ELTIMA 建虚拟对），否则 `port.Open()` 仍会失败。

---

### Q77：GetMasterCOM 读 IConfiguration 而非 setting.json — 两处配置源不一致

**现象**：`ModbusPortManager.BuildComMapping` 读 setting.json，而 `ModbusService.GetMasterCOM` 读 `IConfiguration`（appsettings.json），同一份端口映射存在两个来源。

**解决**：把 `BuildComMapping` 的可见性从 `private` 改为 `public`（或 `internal`，两者同程序集，效果等同；单机内部服务类改 public 无风险——方法无副作用、可安全重复调用），`GetMasterCOM` 直接复用：

```csharp
// ModbusService.cs
private string? GetMasterCOM(string slaveCOM)
{
    var slaveToMaster = _portManager.BuildComMapping();
    return slaveToMaster.TryGetValue(slaveCOM, out var masterCOM) ? masterCOM : null;
}
```

改完后 `ModbusService` 里的 `_config`（IConfiguration）字段及构造参数不再使用，一并删除。appsettings 里的 `Modbus:PortMappings` 节点留着也不读了，可删可留。

---

### Q78：SystemController 无法激活 — ISystemService 未注册

**现象**：请求 `/api/System/info` 抛 `Unable to resolve service for type 'Server.Services.ISystemService' while attempting to activate 'SystemController'`。

**原因**：`SystemController` 构造注入 `ISystemService`，但 Program.cs 只注册了 `ISystemConfigService`，漏了 `ISystemService`。

**解决**：补注册：

```csharp
builder.Services.AddSingleton<ISystemService, SystemService>();
```

---

### Q79：注册 ISystemService 后启动报 AggregateException — IModbusService 未注册

**现象**：补上 `AddSingleton<ISystemService, SystemService>()` 后，`builder.Build()` 抛 `Unable to resolve service for type 'IModbusService' while attempting to activate 'SystemService'`。

**原因**：`ModbusService` 只用 `AddHostedService<ModbusService>()` 注册，这只注册了 `IHostedService`，**不会**同时把类注册为它实现的 `IModbusService` 接口；而 `SystemService` 构造需要 `IModbusService`。

**解决**：接口显式注册为单例，与托管服务共享同一实例：

```csharp
builder.Services.AddSingleton<IModbusService, ModbusService>();
builder.Services.AddHostedService<ModbusService>();
```

两条注册共享同一个单例实例（`AddHostedService` 用 `TryAddEnumerable` 注册 `IHostedService`，解析时复用已注册的 `ModbusService` 单例），不会重复创建串口/重复轮询。

---

### Q80：读取 Serilog 日志文件抛 IOException — 文件被另一个进程使用

**现象**：`/api/System/logs` 抛 `The process cannot access the file 'logs/log-xxx.txt' because it is being used by another process`（HResult 0x80070020）。

**原因**：Serilog File sink 持有写入句柄，`GetLogsAsync` 用 `File.ReadAllLinesAsync` 打开同一文件冲突。写入端默认不共享读。

**解决（两端配合）**：

① 写入端（Program.cs 的 `.WriteTo.File`）加 `shared: true`：

```csharp
.WriteTo.File("logs/log-.txt",
    rollingInterval: RollingInterval.Day,
    retainedFileCountLimit: 14,
    shared: true,    // 写入端允许他人读
    outputTemplate: "...")
```

② 读端（SystemService.GetLogsAsync）改用 `FileShare.ReadWrite` 并带重试，扛住滚动瞬间的瞬态冲突：

```csharp
private static async Task<List<string>> ReadLastLinesAsync(string path, int maxLines)
{
    for (var attempt = 0; attempt < 3; attempt++)
    {
        try
        {
            await using var fs = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
            using var reader = new StreamReader(fs);
            var all = new List<string>();
            string? line;
            while ((line = await reader.ReadLineAsync()) != null)
                all.Add(line);
            return all.TakeLast(Math.Max(maxLines, 1)).ToList();
        }
        catch (IOException) when (attempt < 2)
        {
            await Task.Delay(100);   // 写入端滚动/瞬间占用时重试
        }
    }
    return new List<string>();
}
```

**踩坑提醒**：`shared: true` 只对**新打开的**写入句柄生效——加了配置后必须**完全重启进程**（注意清理残留的 dotnet/Server.exe 进程），否则旧进程仍持独占句柄，怎么改读端都没用。

---

### Q81：Client — PutAsJsonAsync 不存在于 HttpClientBase

**现象**：编译错误 "'HttpClientBase'未包含'PutAsJsonAsync'的定义…"。

**原因**：`PutAsJsonAsync` 是 `HttpClient` 的扩展方法，只能作用于真正的 `HttpClient` 实例；自己的封装类 `HttpClientBase` 提供的是 `PutAsync<T>`。

**解决**：调用封装类自己的方法（泛型自动推断）：

```csharp
var response = await _http.PutAsync("api/system/settings", settings);
```

---

### Q82：Client — PostAsync 传 null 无法推断泛型 T

**现象**：编译错误 "无法从用法中推断出方法'HttpClientBase.PostAsync<T>(string, T)'的类型参数"。

**原因**：`PostAsync<T>(string url, T body)` 需要从 `body` 推断 `T`，传 `null` 时编译器无法推断。

**解决（二选一）**：

```csharp
// 方案 A：显式指定类型参数（最小改动）
var response = await _http.PostAsync<object>("api/system/modbus/reload", null);

// 方案 B（更规整）：HttpClientBase 加无 body 重载
public async Task<HttpResponseMessage> PostAsync(string url)
    => await SendAsync(() => new HttpRequestMessage(HttpMethod.Post, url));
```

---

### Q83：Client — DelegateCommand<object?> 无法隐式转换为 DelegateCommand

**现象**：编译错误 "无法将类型'DelegateCommand<object?>'隐式转换为'DelegateCommand'"。

**原因**：属性声明为非泛型 `DelegateCommand`，却创建了泛型 `DelegateCommand<object?>`；两者是不同类型，不能隐式转换。

**解决**：属性类型与命令签名一致（删除行命令需要参数，用泛型版）：

```csharp
public DelegateCommand<object?> RemoveMappingCommand { get; }   // 属性声明
RemoveMappingCommand = new DelegateCommand<object?>(RemoveMapping);  // 创建处不变
```

---

### Q84：Client — SettingsView 加载抛 NullReferenceException — XAML 手动 new 有参构造的 ViewModel

**现象**：进入设置页抛 `NullReferenceException`，栈顶停在 `SettingsView.InitializeComponent()`（XAML 第 1 行），`DefaultBinder.BindToMethod` 内部。

**原因**：SettingsView.xaml 里手动写了 `<UserControl.DataContext><vm:SettingsViewModel/></UserControl.DataContext>`。XAML 加载器尝试用**无参构造** new ViewModel，而 `SettingsViewModel(ISystemService)` 只有带参构造 → 构造失败。但项目实际用 Prism `RegisterForNavigation<SettingsView, SettingsViewModel>()`，应由 ViewModelLocator 通过容器解析（可注入 ISystemService），手动 DataContext 是多余且冲突的。

**解决**：删除 XAML 里的 `<UserControl.DataContext>` 三行；在 UserControl 根元素上确保有 `prism:ViewModelLocator.AutoWireViewModel="True"`（含 `xmlns:prism="http://prismlibrary.com/"`），让 Prism 容器装配 DataContext。

---

### Q85：async Main 改错成 async void — "返回入口点的 void 或 int 不能是异步的"

**现象**：把 `Main` 改成 `public static async void Main(string[] args)` 后编译报错。

**原因**：入口点只允许 `async Task Main` 或 `async Task<int> Main`；`async void` 调用方无法等待完成，运行时无法知道程序何时结束，编译器直接禁止。

**解决**：

```csharp
public static async Task Main(string[] args)   // void → Task
```

**顺带的知识点**：
- `async Task Main` 是官方标准写法（C# 7.1+），编译器生成同步入口包装，行为与 void Main 一致（异常照样崩、退出码 0）；控制台程序无 SynchronizationContext，无死锁风险。
- 启动初始化（`await configService.LoadAsync()`）放在 `app.Run()` 前的意义：① 避免多个后台服务并发调 LoadAsync 的竞态（`_cache.Clear()` + Flatten 非线程安全）；② 后台服务启动时配置已就绪；③ setting.json 缺失/损坏时 fail-fast，不会带默认值空转。因此**不要删** Program.cs 里的启动初始化；反而 `ModbusService.ExecuteAsync` 里那次重复的 `LoadAsync` 可以删掉（配置已就绪）。

---
