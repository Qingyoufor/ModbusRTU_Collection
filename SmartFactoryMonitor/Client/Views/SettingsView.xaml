<UserControl x:Class="Client.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Client.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="16">
        <TabControl>
            <!-- Tab1: Modbus 配置 -->
            <TabItem Header="Modbus 配置">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <GroupBox Header="端口映射（从站 → 主站）">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding PortMappings}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <TextBox Text="{Binding SlaveCom, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                                                <TextBlock Text=" → " VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding MasterCom, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                                                <Button Content="删除" Margin="12,0,0,0" Padding="6,2"
                                                        Command="{Binding DataContext.RemoveMappingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ 添加映射" Command="{Binding AddMappingCommand}" HorizontalAlignment="Left" Margin="0,8,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="串口参数" Margin="0,12,0,0">
                            <UniformGrid Columns="3">
                                <StackPanel Margin="4">
                                    <TextBlock Text="波特率" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding BaudRate, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="数据位" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding DataBits, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="停止位 (One/Two)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding StopBits, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="校验位 (None/Odd/Even)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding Parity, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="读超时 (ms)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ReadTimeout, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="写超时 (ms)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding WriteTimeout, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </UniformGrid>
                        </GroupBox>

                        <GroupBox Header="采集周期" Margin="0,12,0,0">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Text="轮询间隔 (ms)" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding PollIntervalMs, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="12,0,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                            <Button Content="重载 Modbus" Command="{Binding ReloadModbusCommand}" Width="110" Height="30" Margin="0,0,8,0"/>
                            <Button Content="保存配置" Command="{Binding SaveCommand}" Width="110" Height="30"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab2: 数据保留 -->
            <TabItem Header="数据保留">
                <StackPanel Margin="8" Width="380" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Margin="0,12">
                        <TextBlock Text="历史数据保留天数" Width="140" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding DeviceDataDays, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,12">
                        <TextBlock Text="报警记录保留天数" Width="140" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding AlarmDays, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                    </StackPanel>
                    <Button Content="保存" Command="{Binding SaveCommand}" Width="110" Height="30" HorizontalAlignment="Left" Margin="0,20,0,0"/>
                    <TextBlock Text="归档服务每 5 秒检查一次，超过保留天数的数据会被自动清理。"
                               Foreground="Gray" TextWrapping="Wrap" Margin="0,12,0,0"/>
                </StackPanel>
            </TabItem>

            <!-- Tab3: 日志查看 -->
            <TabItem Header="日志查看">
                <DockPanel Margin="8">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="加载日志" Command="{Binding LoadLogsCommand}" Width="100" Height="28"/>
                        <TextBlock Text="{Binding LogFileName}" VerticalAlignment="Center" Foreground="Gray" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBox Text="{Binding LogText}" IsReadOnly="True" FontFamily="Consolas"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </DockPanel>
            </TabItem>

            <!-- Tab4: 关于 -->
            <TabItem Header="关于">
                <StackPanel Margin="20" Width="400" HorizontalAlignment="Left">
                    <TextBlock Text="SmartFactory Monitor" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding SystemInfo.Version, StringFormat=版本：{0}}" Margin="0,20,0,4"/>
                    <TextBlock Text="{Binding SystemInfo.ServerStartedAt, StringFormat=服务启动：{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.UpTime, StringFormat=已运行：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.OnlineDeviceCount, StringFormat=在线设备：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.CachedPointCount, StringFormat=缓存测点：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.ModbusRunning, StringFormat=Modbus 状态：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.LastPollTime, StringFormat=最近轮询：{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,4"/>
                    <Button Content="刷新" Command="{Binding LoadInfoCommand}" Width="90" Height="28" HorizontalAlignment="Left" Margin="0,16,0,0"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
