# 开发日志（Day 5-8）

***

## Day 5 — WPF 实时监控页面（设备树 + 实时数据）

**目标**：对接 Modbus 缓存数据，实现实时监控主界面，包括设备树导航和实时数值面板。

**实现前提**：

- Server 端 `ModbusDataCache`（单例）已提供线程安全的内存缓存，存储 `ConcurrentDictionary<deviceId, ConcurrentDictionary<dataPointId, double>>`
- Server 端 `IModbusService` 已暴露 `GetCurrentData()` 方法获取全量缓存快照
- Client 端 `HttpClientBase` + `TokenStore` 已封装 Token 自动附加 + 401 刷新
- Client 端 Prism 导航框架已就绪，`RealtimeView` 已注册 `RegisterForNavigation`

**目录规划**（新建以下文件和文件夹）：

```
Server/
├── Controllers/
│   └── RealtimeController.cs                         # 实时数据 API
├── Services/
│   ├── IRealtimeService.cs                           # 实时数据服务接口
│   └── RealtimeService.cs                            # 实时数据服务实现
└── Models/Dtos/
    ├── RealtimeDeviceDataDto.cs                      # 单设备实时数据
    └── RealtimeAllDataDto.cs                         # 全量实时数据

Client/
├── Models/
│   ├── Dtos/
│   │   ├── RealtimeDeviceDataDto.cs                  # 客户端实时数据 DTO
│   │   └── RealtimeAllDataDto.cs                     # 客户端全量数据 DTO
│   ├── ProductionLineTreeNode.cs                     # 产线树节点模型
│   ├── DeviceTreeNode.cs                            # 设备树节点模型
│   └── DataPointDisplayModel.cs                     # 测点显示模型
├── Service/
│   ├── IRealtimeService.cs                           # 客户端实时数据服务接口
│   └── RealtimeService.cs                            # 客户端实时数据服务实现
└── Views/
    └── RealtimeView.xaml                             # 实时监控页面
    ViewModels/
    └── RealtimeViewModel.cs                          # 实时监控 ViewModel
```

***

### 任务一：服务器端实时数据 API

#### 1a. RealtimeDeviceDataDto.cs — 单设备实时数据

```csharp
namespace Server.Models.Dtos
{
    public class RealtimeDeviceDataDto
    {
        public int DeviceId { get; set; }
        public int ProductLineId { get; set; }          // 产线 ID（用于客户端过滤）
        public string DeviceName { get; set; } = string.Empty;
        public string Status { get; set; } = string.Empty;
        public List<DataPointValueDto> DataPoints { get; set; } = new();
    }

    public class DataPointValueDto
    {
        public int DataPointId { get; set; }
        public string Name { get; set; } = string.Empty;
        public double Value { get; set; }
        public string? Unit { get; set; }
        public string DataType { get; set; } = string.Empty;
        public double? AlarmHigh { get; set; }
        public double? AlarmLow { get; set; }
        public int DecimalPlaces { get; set; }
        public string AlarmStatus { get; set; } = "Normal"; // Normal / High / Low
    }
}
```

> **ProductLineId 字段**：客户端构建设备树时需要按产线过滤设备，所以 DTO 中包含此字段。`AlarmStatus` 由服务端判定，前端只负责渲染颜色。

#### 1b. RealtimeAllDataDto.cs — 全量实时数据

```csharp
namespace Server.Models.Dtos
{
    public class RealtimeAllDataDto
    {
        public List<RealtimeDeviceDataDto> Devices { get; set; } = new();
        public DateTime Timestamp { get; set; } = DateTime.UtcNow;
    }
}
```

#### 1c. IRealtimeService.cs + RealtimeService.cs — 实时数据服务

```csharp
// IRealtimeService.cs
using Server.Models.Dtos;

namespace Server.Services
{
    public interface IRealtimeService
    {
        Task<ApiResult<RealtimeDeviceDataDto>> GetDeviceDataAsync(int deviceId);
        Task<ApiResult<RealtimeAllDataDto>> GetAllDataAsync();
    }
}
```

```csharp
// RealtimeService.cs
using Server.Infrastructure.Modbus;
using Server.Infrastructure.Repositories;
using Server.Models.Common;
using Server.Models.Dtos;
using Server.Models.Entities;

namespace Server.Services
{
    public class RealtimeService : IRealtimeService
    {
        private readonly ModbusDataCache _cache;
        private readonly IDeviceRepository _deviceRepo;
        private readonly IDataPointRepository _dataPointRepo;

        public RealtimeService(
            ModbusDataCache cache,
            IDeviceRepository deviceRepo,
            IDataPointRepository dataPointRepo)
        {
            _cache = cache;
            _deviceRepo = deviceRepo;
            _dataPointRepo = dataPointRepo;
        }

        public async Task<ApiResult<RealtimeDeviceDataDto>> GetDeviceDataAsync(int deviceId)
        {
            var device = await _deviceRepo.GetByIdAsync(deviceId);
            if (device == null)
                return ApiResult<RealtimeDeviceDataDto>.Fail(404, "设备不存在");

            if (device.Status == DeviceStatus.Offline)
                return ApiResult<RealtimeDeviceDataDto>.Fail(400, "设备不在线");

            var cachedData = _cache.GetDeviceData(deviceId);
            var dataPoints = await _dataPointRepo.GetByDeviceIdAsync(deviceId);

            var dto = new RealtimeDeviceDataDto
            {
                DeviceId = device.Id,
                ProductLineId = device.ProductionLineId,
                DeviceName = device.Name,
                Status = device.Status.ToString()
            };

            foreach (var point in dataPoints)
            {
                var value = cachedData?.GetValueOrDefault(point.Id) ?? 0;
                dto.DataPoints.Add(new DataPointValueDto
                {
                    DataPointId = point.Id,
                    Name = point.Name,
                    Value = value,
                    Unit = point.Unit,
                    DataType = point.DataType.ToString(),
                    AlarmHigh = point.AlarmHigh,
                    AlarmLow = point.AlarmLow,
                    DecimalPlaces = point.DecimalPlaces,
                    AlarmStatus = DetermineAlarmStatus(value, point.AlarmHigh, point.AlarmLow)
                });
            }

            return ApiResult<RealtimeDeviceDataDto>.Success(dto);
        }

        public async Task<ApiResult<RealtimeAllDataDto>> GetAllDataAsync()
        {
            var devices = await _deviceRepo.GetAllAsync();
            var onlineDevices = devices.Where(d => d.Status != DeviceStatus.Offline).ToList();
            var allCachedData = _cache.GetAllData();

            var result = new RealtimeAllDataDto();

            foreach (var device in onlineDevices)
            {
                var cachedData = allCachedData.GetValueOrDefault(device.Id);
                var dataPoints = await _dataPointRepo.GetByDeviceIdAsync(device.Id);

                var deviceDto = new RealtimeDeviceDataDto
                {
                    DeviceId = device.Id,
                    ProductLineId = device.ProductionLineId,
                    DeviceName = device.Name,
                    Status = device.Status.ToString()
                };

                foreach (var point in dataPoints)
                {
                    var value = cachedData?.GetValueOrDefault(point.Id) ?? 0;
                    deviceDto.DataPoints.Add(new DataPointValueDto
                    {
                        DataPointId = point.Id,
                        Name = point.Name,
                        Value = value,
                        Unit = point.Unit,
                        DataType = point.DataType.ToString(),
                        AlarmHigh = point.AlarmHigh,
                        AlarmLow = point.AlarmLow,
                        DecimalPlaces = point.DecimalPlaces,
                        AlarmStatus = DetermineAlarmStatus(value, point.AlarmHigh, point.AlarmLow)
                    });
                }

                result.Devices.Add(deviceDto);
            }

            return ApiResult<RealtimeAllDataDto>.Success(result);
        }

        private static string DetermineAlarmStatus(double value, double? alarmHigh, double? alarmLow)
        {
            if (alarmHigh.HasValue && value > alarmHigh.Value) return "High";
            if (alarmLow.HasValue && value < alarmLow.Value) return "Low";
            return "Normal";
        }
    }
}
```

> **GetAllDataAsync 只返回在线设备**：离线设备没有实时数据，无需返回给客户端。每个设备包含完整测点数据（含 Value），客户端可直接构建设备树。

#### 1d. RealtimeController.cs — 实时数据 API

```csharp
using Microsoft.AspNetCore.Mvc;
using Server.Services;

namespace Server.Infrastructure.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    public class RealtimeController : ControllerBase
    {
        private readonly IRealtimeService _realtimeService;

        public RealtimeController(IRealtimeService realtimeService)
        {
            _realtimeService = realtimeService;
        }

        /// <summary>GET api/realtime/devices/{deviceId}/data — 单设备实时数据</summary>
        [HttpGet("devices/{deviceId}/data")]
        public async Task<IActionResult> GetDeviceData(int deviceId)
        {
            var result = await _realtimeService.GetDeviceDataAsync(deviceId);
            return result.Code == 200 ? Ok(result) : NotFound(result);
        }

        /// <summary>GET api/realtime/devices/all — 所有在线设备实时数据</summary>
        [HttpGet("devices/all")]
        public async Task<IActionResult> GetAllDeviceData()
        {
            var result = await _realtimeService.GetAllDataAsync();
            return Ok(result);
        }
    }
}
```

> **路由设计**：`devices/{deviceId}/data` 和 `devices/all` 形成清晰的层级关系。注意实际 namespace 是 `Server.Infrastructure.Controllers`。

#### 1e. Program.cs 注册

```csharp
// DI 注册新增
builder.Services.AddScoped<IRealtimeService, RealtimeService>();
```

***

### 任务二：设备树（TreeView）

#### 2a. 树节点模型

```csharp
// Client/Models/ProductionLineTreeNode.cs
namespace Client.Models
{
    public class ProductionLineTreeNode
    {
        public int Id { get; set; }
        public string Name { get; set; } = string.Empty;
        public List<DeviceTreeNode> Devices { get; set; } = new();
    }
}
```

```csharp
// Client/Models/DeviceTreeNode.cs
namespace Client.Models
{
    public class DeviceTreeNode
    {
        public int Id { get; set; }
        public string Name { get; set; } = string.Empty;
        public string Status { get; set; } = "Offline";

        /// <summary>状态图标颜色：Running=绿色 / Alarm=红色 / 其他=灰色</summary>
        public string StatusColor => Status switch
        {
            "Running" => "#4CAF50",
            "Alarm" => "#F44336",
            _ => "#9E9E9E"
        };
    }
}
```

> **StatusColor 属性**：WPF Binding 不支持 switch 表达式，用 C# 计算属性将状态映射为颜色字符串。前端通过 `<Ellipse Fill="{Binding StatusColor}"/>` 显示状态指示灯。

#### 2b. 客户端 DTO

```csharp
// Client/Models/Dtos/RealtimeDeviceDataDto.cs
namespace Client.Models.Dtos
{
    public class RealtimeDeviceDataDto
    {
        public int DeviceId { get; set; }
        public int ProductLineId { get; set; }
        public string DeviceName { get; set; } = string.Empty;
        public string Status { get; set; } = string.Empty;
        public List<DataPointValueDto> DataPoints { get; set; } = new();
    }

    public class DataPointValueDto
    {
        public int DataPointId { get; set; }
        public string Name { get; set; } = string.Empty;
        public double Value { get; set; }
        public string? Unit { get; set; }
        public string DataType { get; set; } = string.Empty;
        public double? AlarmHigh { get; set; }
        public double? AlarmLow { get; set; }
        public int DecimalPlaces { get; set; }
        public string AlarmStatus { get; set; } = "Normal";
    }
}
```

```csharp
// Client/Models/Dtos/RealtimeAllDataDto.cs
namespace Client.Models.Dtos
{
    public class RealtimeAllDataDto
    {
        public List<RealtimeDeviceDataDto> Devices { get; set; } = new();
        public DateTime Timestamp { get; set; }
    }
}
```

#### 2c. IRealtimeService.cs + RealtimeService.cs — 客户端实时数据服务

```csharp
// Client/Service/IRealtimeService.cs
using Client.Models.Dtos;

namespace Client.Service
{
    public interface IRealtimeService
    {
        Task<RealtimeDeviceDataDto?> GetDeviceDataAsync(int deviceId);
        Task<RealtimeAllDataDto?> GetAllDataAsync();
    }
}
```

```csharp
// Client/Service/RealtimeService.cs
using Client.Infrastructure;
using Client.Models.Dtos;
using System.Net.Http.Json;
using System.Text.Json;

namespace Client.Service
{
    public class RealtimeService : IRealtimeService
    {
        private readonly HttpClientBase _http;
        private static readonly JsonSerializerOptions _jsonOptions = new()
        {
            PropertyNameCaseInsensitive = true  // 必须！服务端返回 camelCase
        };

        public RealtimeService(HttpClientBase http) { _http = http; }

        public async Task<RealtimeDeviceDataDto?> GetDeviceDataAsync(int deviceId)
        {
            var response = await _http.GetAsync($"api/realtime/devices/{deviceId}/data");
            if (!response.IsSuccessStatusCode) return null;
            var result = await response.Content.ReadFromJsonAsync<ApiResult<RealtimeDeviceDataDto>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<RealtimeAllDataDto?> GetAllDataAsync()
        {
            var response = await _http.GetAsync("api/realtime/devices/all");
            if (!response.IsSuccessStatusCode) return null;
            var result = await response.Content.ReadFromJsonAsync<ApiResult<RealtimeAllDataDto>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }
    }
}
```

> **PropertyNameCaseInsensitive = true**：ASP.NET Core 默认返回 camelCase JSON（`"code"` 而非 `"Code"`），必须设置大小写不敏感才能正确反序列化。

#### 2d. App.xaml.cs 注册

```csharp
// RegisterTypes 新增
containerRegistry.RegisterSingleton<IRealtimeService, RealtimeService>();
```

***

### 任务三：实时数值面板（纯 MVVM）

#### 3a. 安装 Microsoft.Xaml.Behaviors.Wpf

```xml
<PackageReference Include="Microsoft.Xaml.Behaviors.Wpf" Version="1.1.77" />
```

> **作用**：提供 `Interaction.Triggers` + `InvokeCommandAction`，在 XAML 中将事件绑定到 ViewModel 的 Command，无需在 code-behind 写事件处理代码。

#### 3b. DataPointDisplayModel — 测点显示模型

```csharp
// Client/Models/DataPointDisplayModel.cs
using System.Windows.Media;

namespace Client.Models
{
    public class DataPointDisplayModel
    {
        public int DataPointId { get; set; }
        public string Name { get; set; } = string.Empty;
        public double Value { get; set; }
        public string? Unit { get; set; }
        public int DecimalPlaces { get; set; }
        public string AlarmStatus { get; set; } = "Normal";
        public double? AlarmHigh { get; set; }
        public double? AlarmLow { get; set; }

        /// <summary>格式化显示值（按 DecimalPlaces 截断）</summary>
        public string DisplayValue => Value.ToString($"F{DecimalPlaces}");

        /// <summary>数值颜色：High=红 / Low=橙 / Normal=黑</summary>
        public Color ValueColor => AlarmStatus switch
        {
            "High" => Color.FromRgb(0xF4, 0x43, 0x36), // #F44336
            "Low" => Color.FromRgb(0xFF, 0x98, 0x00),  // #FF9800
            _ => Color.FromRgb(0x33, 0x33, 0x33)        // #333333
        };

        /// <summary>报警提示文本</summary>
        public string AlarmText => AlarmStatus switch
        {
            "High" => $"⚠ 高限报警 (>{AlarmHigh})",
            "Low" => $"⚠ 低限报警 (<{AlarmLow})",
            _ => "正常"
        };

        /// <summary>报警提示颜色</summary>
        public Color AlarmColor => AlarmStatus switch
        {
            "High" => Color.FromRgb(0xF4, 0x43, 0x36),
            "Low" => Color.FromRgb(0xFF, 0x98, 0x00),
            _ => Color.FromRgb(0x4C, 0xAF, 0x50)  // 绿色
        };
    }
}
```

> **DisplayModel vs DTO**：DTO 是 API 返回的原始数据，不含 UI 逻辑。DisplayModel 封装格式化显示、颜色映射、报警文本等展示逻辑，保持 DTO 纯净。

#### 3c. RealtimeView\.xaml — 实时监控页面（纯 MVVM）

```xml
<UserControl x:Class="Client.Views.RealtimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Client.Models"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--左侧设备树-->
        <DockPanel Grid.Column="0">
            <TextBlock Text="设备导航"
                       DockPanel.Dock="Top"
                       FontSize="12"
                       FontWeight="Bold"
                       Margin="10,10"/>

            <TreeView ItemsSource="{Binding ProductionLineTreeNodes}" Margin="5">
                <!-- 纯 MVVM：Interaction.Triggers 将 SelectedItemChanged 事件绑定到 Command -->
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectedItemChanged">
                        <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}"
                                               CommandParameter="{Binding SelectedItem,RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <TreeView.Resources>
                    <!--产线节点模板，有子节点-->
                    <HierarchicalDataTemplate DataType="{x:Type local:ProductionLineTreeNode}"
                                              ItemsSource="{Binding Devices}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>

                    <!--设备节点模板 没有子节点-->
                    <DataTemplate DataType="{x:Type local:DeviceTreeNode}">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Height="10" Width="10"
                                     Fill="{Binding StatusColor}"
                                     VerticalAlignment="Center"
                                     Margin="0,5,5,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
        </DockPanel>

        <!--右侧实时数据面板-->
        <Grid Grid.Column="1" Margin="10">
            <!--未选择设备时显示占位（用反向属性，无需自定义转换器）-->
            <TextBlock Text="请在左侧选择一个设备查看实时数据"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#999"
                       Visibility="{Binding IsDeviceNotSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <!--选中设备后显示数据-->
            <StackPanel Visibility="{Binding IsDeviceSelected,Converter={StaticResource BooleanToVisibilityConverter}}">
                <!--设备标题栏-->
                <DockPanel>
                    <TextBlock Text="{Binding SelectedDeviceName}"
                               FontSize="18" FontWeight="Bold" Margin="0,0,20,0"/>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding DeviceStatus}"
                               Foreground="#666" VerticalAlignment="Center"
                               FontWeight="Bold" FontSize="15"/>
                </DockPanel>

                <!--测点卡片列表-->
                <ItemsControl ItemsSource="{Binding DataPoints}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="12"
                                    Background="#FAFAFA"
                                    BorderBrush="#E0E0E0" BorderThickness="1"
                                    CornerRadius="4">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="12" Foreground="#666" Margin="0,0,0,6" />

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayValue}"
                                                   FontSize="22" FontWeight="Bold">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding ValueColor}" />
                                            </TextBlock.Foreground>
                                        </TextBlock>

                                        <TextBlock Text="{Binding Unit}"
                                                   FontSize="12" Foreground="#999"
                                                   VerticalAlignment="Bottom" Margin="4,0,0,2" />
                                    </StackPanel>

                                    <!--报警状态指示-->
                                    <TextBlock Text="{Binding AlarmText}"
                                               FontSize="10" Margin="0,4,0,0">
                                        <TextBlock.Foreground>
                                            <SolidColorBrush Color="{Binding AlarmColor}" />
                                        </TextBlock.Foreground>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
```

> **Interaction.Triggers**：将 TreeView 的 `SelectedItemChanged` 事件通过 `InvokeCommandAction` 绑定到 ViewModel 的 Command。`CommandParameter` 用 `RelativeSource` 向上找到 TreeView 控件，读取它的 `SelectedItem` 属性作为参数传入。
>
> **IsDeviceNotSelected 反向属性**：不需要自定义 `InvertBooleanToVisibilityConverter`，在 ViewModel 中定义计算属性 `public bool IsDeviceNotSelected => !IsDeviceSelected;`，setter 中触发 `RaisePropertyChanged(nameof(IsDeviceNotSelected))` 即可。

#### 3d. RealtimeViewModel.cs — 实时监控 ViewModel

```csharp
// Client/ViewModels/RealtimeViewModel.cs
using Client.Models;
using Client.Models.Dtos;
using Client.Service;
using System.Collections.ObjectModel;
using System.Windows.Threading;

namespace Client.ViewModels
{
    public class RealtimeViewModel : BindableBase, INavigationAware
    {
        // DI
        private readonly IProductionLineService _productionLineService;
        private readonly IRealtimeService _realtimeService;

        // View 字段
        private ObservableCollection<ProductionLineTreeNode> _productLineTreeNodes = new();
        private string _selectedDeviceName = string.Empty;
        private string _deviceStatus = string.Empty;
        private bool _isDeviceSelected;
        private ObservableCollection<DataPointDisplayModel> _dataPoints = new();

        // VM 私有字段
        private DispatcherTimer? timer;
        private int selectedItemId;

        // View 命令
        public DelegateCommand<object?> SelectedItemChangedCommand { get; }

        // 反向属性：未选中设备时显示占位文本
        public bool IsDeviceNotSelected => !IsDeviceSelected;

        public RealtimeViewModel(IProductionLineService lineService, IRealtimeService realtimeService)
        {
            _productionLineService = lineService;
            _realtimeService = realtimeService;
            SelectedItemChangedCommand = new DelegateCommand<object?>(OnSelectedItemChanged);
        }

        // ======== INavigationAware ========

        // 是否重用视图实例（返回 true，离开页面后再回来时保持状态）
        public bool IsNavigationTarget(NavigationContext navigationContext) => true;

        // 离开视图时停止定时器
        public void OnNavigatedFrom(NavigationContext navigationContext)
        {
            StopRefreshTimer();
        }

        // 进入视图时：树为空才加载，每次都启动定时器
        public void OnNavigatedTo(NavigationContext navigationContext)
        {
            if (ProductionLineTreeNodes.Count == 0)
            {
                _ = LoadTreeAsync();
            }
            StartRefreshTimer();
        }

        // ======== 设备树加载 ========
        private async Task LoadTreeAsync()
        {
            var lines = await _productionLineService.GetAllAsync();
            if (lines == null || lines.Count == 0) return;

            var allDeviceData = await _realtimeService.GetAllDataAsync();
            if (allDeviceData == null) return;

            var treeNodes = new ObservableCollection<ProductionLineTreeNode>();

            foreach (var line in lines)
            {
                var lineNode = new ProductionLineTreeNode
                {
                    Id = line.Id,
                    Name = line.Name,
                    Devices = allDeviceData.Devices
                        .Where(d => d.ProductLineId == line.Id)
                        .Select(d => new DeviceTreeNode
                        {
                            Id = d.DeviceId,
                            Name = d.DeviceName,
                            Status = d.Status
                        })
                        .ToList()
                };
                treeNodes.Add(lineNode);
            }

            ProductionLineTreeNodes = treeNodes;
        }

        // ======== TreeView 选中事件处理 ========
        private void OnSelectedItemChanged(object? selectedItem)
        {
            if (selectedItem is DeviceTreeNode device)
            {
                IsDeviceSelected = true;
                selectedItemId = device.Id;
                _ = LoadDeviceDataAsync();
            }
        }

        // ======== 加载设备实时数据 ========
        private async Task LoadDeviceDataAsync()
        {
            var device = await _realtimeService.GetDeviceDataAsync(selectedItemId);
            if (device == null) return;

            SelectedDeviceName = device.DeviceName;
            DeviceStatus = device.Status;

            var pointsDisplay = device.DataPoints.Select(p => new DataPointDisplayModel
            {
                DataPointId = p.DataPointId,
                Name = p.Name,
                Value = p.Value,
                Unit = p.Unit,
                DecimalPlaces = p.DecimalPlaces,
                AlarmStatus = p.AlarmStatus,
                AlarmHigh = p.AlarmHigh,
                AlarmLow = p.AlarmLow
            });
            DataPoints = new ObservableCollection<DataPointDisplayModel>(pointsDisplay);
        }

        // ======== 定时任务 ========
        private void StartRefreshTimer()
        {
            if (timer == null)
            {
                timer = new DispatcherTimer { Interval = TimeSpan.FromSeconds(2) };
                timer.Tick += OnRefreshTick;
                timer.Start();
            }
        }

        private void StopRefreshTimer()
        {
            timer?.Stop();
        }

        private async void OnRefreshTick(object? sender, EventArgs e) => await LoadDeviceDataAsync();

        // ======== 属性 ========
        public ObservableCollection<DataPointDisplayModel> DataPoints
        {
            get => _dataPoints;
            set => SetProperty(ref _dataPoints, value);
        }

        public ObservableCollection<ProductionLineTreeNode> ProductionLineTreeNodes
        {
            get => _productLineTreeNodes;
            set => SetProperty(ref _productLineTreeNodes, value);
        }

        public string SelectedDeviceName
        {
            get => _selectedDeviceName;
            set => SetProperty(ref _selectedDeviceName, value);
        }

        public string DeviceStatus
        {
            get => _deviceStatus;
            set => SetProperty(ref _deviceStatus, value);
        }

        public bool IsDeviceSelected
        {
            get => _isDeviceSelected;
            set
            {
                if (SetProperty(ref _isDeviceSelected, value))
                {
                    RaisePropertyChanged(nameof(IsDeviceNotSelected));  // 同时通知反向属性
                }
            }
        }
    }
}
```

> **IsNavigationTarget 返回 true**：离开页面后再回来时复用同一个 ViewModel 实例，设备树保持上次状态。`OnNavigatedTo` 中判断 `ProductionLineTreeNodes.Count == 0` 才加载，避免重复请求。
>
> **反向属性通知**：`IsDeviceSelected` 的 setter 中同时触发 `RaisePropertyChanged(nameof(IsDeviceNotSelected))`，确保反向属性的绑定也能更新。
>
> **DispatcherTimer**：在 UI 线程上触发 Tick，可以直接更新绑定属性。间隔 2 秒，平衡实时性和性能。

***

### 任务四：实时趋势曲线（LiveCharts2 2.0.5，纯 MVVM）

采用 **方案 A：客户端滑动窗口 + 单测点选择**，趋势图数据更新与测点数值刷新在同一个 ViewModel 中，**不需要 IEventAggregator**。

> **核心设计**：每个测点有独立的 `ObservableCollection<double>`，切换测点时只切换 Series 绑定的数据源，不清空历史数据（类似 test.md 示例）。

#### 4a. 安装 NuGet 包

```xml
<PackageReference Include="LiveChartsCore.SkiaSharpView.WPF" Version="2.0.5" />
```

> **LiveCharts2 2.0.5**：稳定版本，提供高性能的数据点和自动更新机制。安装后会自动引入 SkiaSharp 渲染引擎。

#### 4b. RealtimeView\.xaml 趋势图布局

**布局规划**：

- 第二列第一行：实时数据面板（卡片数值，`Height="Auto"`）— **保持不变**
- 第二列第二行：实时趋势曲线（`Height="*"`, Grid 结构）

修改右侧 Grid 结构，趋势图使用独立的 Grid 行：

```xml
<!--右侧实时数据面板（第二列）-->
<Grid Grid.Column="1">
    <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>  <!--第一行：实时数值面板（自适应高度）-->
        <RowDefinition Height="*"/>     <!--第二行：实时趋势曲线（填充剩余空间）-->
    </Grid.RowDefinitions>

    <!--第一行：实时数值面板（保持不变）-->
    <Grid Grid.Column="1" Margin="10">
        <!-- 未选择设备时显示占位 -->
        <TextBlock Text="请在左侧选择一个设备查看实时数据"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Foreground="#999"
                   Visibility="{Binding IsDeviceNotSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- 选中设备后显示数据 -->
        <StackPanel Visibility="{Binding IsDeviceSelected,Converter={StaticResource BooleanToVisibilityConverter}}" >
            <!-- 设备标题栏 -->
            <DockPanel>
                <TextBlock Text="{Binding SelectedDeviceName}"
                           FontSize="18" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock DockPanel.Dock="Right" Text="{Binding DeviceStatus}"
                           Foreground="#666" VerticalAlignment="Center"
                           FontWeight="Bold" FontSize="15"/>
            </DockPanel>

            <!-- 测点卡片列表 -->
            <ItemsControl ItemsSource="{Binding DataPoints}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="12"
                                Background="#FAFAFA"
                                BorderBrush="#E0E0E0" BorderThickness="1"
                                CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="12" Foreground="#666" Margin="0,0,0,6" />

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DisplayValue}"
                                               FontSize="22" FontWeight="Bold">
                                        <TextBlock.Foreground>
                                            <SolidColorBrush Color="{Binding ValueColor}" />
                                        </TextBlock.Foreground>
                                    </TextBlock>

                                    <TextBlock Text="{Binding Unit}"
                                               FontSize="12" Foreground="#999"
                                               VerticalAlignment="Bottom" Margin="4,0,0,2" />
                                </StackPanel>

                                <!-- 报警状态指示 -->
                                <TextBlock Text="{Binding AlarmText}"
                                           FontSize="10" Margin="0,4,0,0">
                                    <TextBlock.Foreground>
                                        <SolidColorBrush Color="{Binding AlarmColor}" />
                                    </TextBlock.Foreground>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>

    <!--第二行：实时趋势曲线（新增）-->
    <Grid Grid.Row="1" Visibility="{Binding IsDeviceSelected,Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--测点选择区域-->
        <StackPanel Orientation="Horizontal"
                    Margin="10"
                    Grid.Row="0">
            <TextBlock Text="选择测点："
                       VerticalAlignment="Center"
                       Margin="0,0,10,0" />
            <ComboBox ItemsSource="{Binding TrendPointNames}"
                      SelectedItem="{Binding SelectedTrendPoint}"
                      Width="120"
                      VerticalAlignment="Center" />
        </StackPanel>

        <!--图表区域（填充剩余空间）-->
        <lvc:CartesianChart Grid.Row="1"
                            Series="{Binding TrendSeries}"
                            XAxes="{Binding TrendXAxes}"
                            YAxes="{Binding TrendYAxes}"/>
    </Grid>
</Grid>
```

XAML 头部添加命名空间：

```xml
xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
```

TreeView 模板保持不变（参考任务二的代码）。 <DataTemplate DataType="{x:Type models:DeviceTreeNode}"> <StackPanel Orientation="Horizontal"> <Ellipse Height="10" Width="10"
Fill="{Binding StatusColor}"
VerticalAlignment="Center"
Margin="0,5,5,0"/> <TextBlock Text="{Binding Name}"/> </StackPanel> </DataTemplate>

````

> **官方文档推荐的最简配置**：
>
> - 只绑定 `Series="{Binding TrendSeries}"`，LiveCharts2 自动处理轴、网格线、标签等
> - 不需要手动配置 `Axis`、`Separators` 等属性
> - ViewModel 中使用 `ObservableCollection<ISeries>` 和 `ObservableCollection<ObservablePoint>` 实现数据自动更新
>
> **命名空间修正**：`xmlns:models="clr-namespace:Client.Models;assembly=Client"` 必须加上 `assembly=Client`，否则 WPF 无法找到类型。
>
> **布局结构**：
>
> - 第一行（`Grid.Row="0"`）：实时数值面板，使用 `Height="Auto"` 自适应卡片高度
> - 第二行（`Grid.Row="1"`）：趋势曲线，使用 `Height="*"` 填充剩余空间
> - 占位文本使用 `Grid.RowSpan="2"` 覆盖两行，仅在未选中设备时显示
>
> **DisplayMemberPath 绑定**：ComboBox 显示测点名称，`SelectedItem` 双向绑定到 ViewModel 的 `SelectedTrendDataPoint`。

#### 4c. RealtimeViewModel.cs 趋势图逻辑（基于实际实现）

**核心设计**（参考 test.md）：
- 每个测点有独立的 `ObservableCollection<double>`，存储在 `Dictionary<int, ObservableCollection<double>>`
- 切换测点时只切换 Series 绑定的数据源，**不清空历史数据**（每个测点保留各自的历史曲线）
- ComboBox 绑定 `string[] TrendPointNames`（只包含测点名称，避免对象绑定导致 SelectedItem 丢失）

在 ViewModel 类中追加以下字段和属性：

```csharp
// ======== 趋势图数据结构 ========
private const int MaxTrendPoints = 30; // 最近30个点（约60秒历史，2秒刷新一次）

// 每个测点的数据集合（字典：DataPointId → ObservableCollection<double>）
private readonly Dictionary<int, ObservableCollection<double>> _trendDataCollections = new();

// 当前选中测点的 ID
private int _selectedTrendPointId = -1;

// 当前选中的测点名称（ComboBox 绑定）
private string _selectedTrendPoint = string.Empty;
public string SelectedTrendPoint
{
    get => _selectedTrendPoint;
    set
    {
        if (_selectedTrendPoint != value)
        {
            _selectedTrendPoint = value;
            RaisePropertyChanged();

            // 根据测点名称查找 ID，切换 Series 和 Y 轴
            var pointInfo = DataPoints.FirstOrDefault(p => p.Name == value);
            if (pointInfo != null)
            {
                _selectedTrendPointId = pointInfo.DataPointId;
                UpdateTrendSeries(); // 切换 Series 绑定的数据集合
                UpdateTrendYAxis();  // 调整 Y 轴单位/范围
            }
        }
    }
}

// 测点名称数组（ComboBox 数据源，避免绑定对象）
private string[] _trendPointNames = Array.Empty<string>();
public string[] TrendPointNames
{
    get => _trendPointNames;
    set => SetProperty(ref _trendPointNames, value);
}

// Series 数组（动态绑定）
private ISeries[] _trendSeries;
public ISeries[] TrendSeries
{
    get => _trendSeries;
    set => SetProperty(ref _trendSeries, value);
}

// Y 轴配置（动态调整范围）
private Axis[] _trendYAxes;
public Axis[] TrendYAxes
{
    get => _trendYAxes;
    set => SetProperty(ref _trendYAxes, value);
}

// X 轴配置（固定）
public Axis[] TrendXAxes { get; set; }
````

在 ViewModel 构造函数中初始化趋势图轴配置：

```csharp
public RealtimeViewModel(IProductionLineService lineService, IRealtimeService realtimeService)
{
    // ... 其他初始化代码 ...

    // 初始化趋势图轴配置（X 轴固定、Y 轴默认自动范围）
    TrendXAxes = new Axis[]
    {
        new Axis { Name = "时间 (秒)", Labels = null, NameTextSize = 12 }
    };

    TrendYAxes = new Axis[]
    {
        new Axis { Name = "数值", MinLimit = null, MaxLimit = null, NameTextSize = 12 }
    };
}
```

**首次选中设备时初始化趋势图**：

修改 `LoadDeviceDataAsync` 方法，第一次加载时初始化测点列表和数据集合：

```csharp
private async Task LoadDeviceDataAsync(bool initializeTrend = false)
{
    var device = await _realtimeService.GetDeviceDataAsync(_selectedDeviceId);
    if (device == null) return;

    SelectedDeviceName = device.DeviceName;
    DeviceStatus = device.Status;

    // 首次选中设备时初始化趋势图（只执行一次）
    if (initializeTrend)
    {
        InitializeTrendChart(device);
    }

    UpdateDataPoints(device);       // 刷新卡片数值面板
    UpdateTrendCollections(device); // 追加趋势图数据点

    EnsureTimerStarted(); // 保证 2 秒轮询定时器运行
}

private void InitializeTrendChart(RealtimeDeviceDataDto device)
{
    // 测点名称数组（ComboBox 数据源，避免绑定对象）
    TrendPointNames = device.DataPoints.Select(p => p.Name).ToArray();

    // 默认选中第一个测点（直接设字段避免触发 setter 中依赖 DataPoints 的逻辑）
    _selectedTrendPointId = device.DataPoints[0].DataPointId;
    _selectedTrendPoint = TrendPointNames[0];
    RaisePropertyChanged(nameof(SelectedTrendPoint));

    // 为每个测点创建独立的数据集合，并装入初始值
    foreach (var p in device.DataPoints)
    {
        _trendDataCollections[p.DataPointId] = new ObservableCollection<double> { p.Value };
    }

    UpdateTrendSeries(); // 绑定 Series → 第一个测点的数据集合
    UpdateTrendYAxis();  // 根据第一个测点的单位/报警范围设置 Y 轴
}
```

**切换测点时更新 Series 数据源**：

```csharp
private void UpdateTrendSeries()
{
    if (_selectedTrendPointId == -1) return;

    // 根据测点 ID 找到对应的 ObservableCollection<double>
    var values = _trendDataCollections[_selectedTrendPointId];

    // 创建新的 Series，绑定到对应测点的数据集合（不清空历史数据）
    TrendSeries = new ISeries[]
    {
        new LineSeries<double>
        {
            Values = values,
            Stroke = new SolidColorPaint(SKColors.DodgerBlue) { StrokeThickness = 2 },
            Fill = null,
            GeometryFill = null,
            GeometryStroke = null,
        }
    };
}
```

**切换测点时动态调整 Y 轴**：

```csharp
private void UpdateTrendYAxis()
{
    if (_selectedTrendPointId == -1) return;

    // 从 DataPoints 中找到选中测点的信息（单位、报警范围等）
    var pointInfo = DataPoints.FirstOrDefault(p => p.DataPointId == _selectedTrendPointId);
    if (pointInfo == null) return;

    string unit = pointInfo.Unit ?? "数值";
    double? minLimit = null;
    double? maxLimit = null;

    // 有报警阈值时，Y 轴范围设为报警范围的 ±20%（便于观察）
    if (pointInfo.AlarmHigh.HasValue && pointInfo.AlarmLow.HasValue)
    {
        minLimit = pointInfo.AlarmLow.Value * 0.8;
        maxLimit = pointInfo.AlarmHigh.Value * 1.2;
    }

    TrendYAxes = new Axis[]
    {
        new Axis
        {
            Name = unit,
            MinLimit = minLimit,
            MaxLimit = maxLimit,
            NameTextSize = 12
        }
    };
}
```

**为所有测点追加新数值（滑动窗口）**：

```csharp
private void UpdateTrendCollections(RealtimeDeviceDataDto device)
{
    foreach (var p in device.DataPoints)
    {
        var collection = _trendDataCollections[p.DataPointId];
        collection.Add(p.Value);

        // 滑动窗口：保持最多 30 个点（约 60 秒历史）
        if (collection.Count > MaxTrendPoints)
        {
            collection.RemoveAt(0);
        }
    }
}
```

**设备切换时重置趋势图**：

```csharp
private void OnSelectedItemChanged(object? selectedItem)
{
    if (selectedItem is DeviceTreeNode device)
    {
        IsDeviceSelected = true;
        _selectedDeviceId = device.Id;

        // 切换设备 → 清空趋势图所有数据
        _trendDataCollections.Clear();
        TrendPointNames = Array.Empty<string>();
        SelectedTrendPoint = string.Empty;
        _selectedTrendPointId = -1;

        _ = LoadDeviceDataAsync(initializeTrend: true); // 首次加载需初始化
    }
}
```

在文件顶部添加 using 语句：

```csharp
using LiveChartsCore;
using LiveChartsCore.SkiaSharpView;
using LiveChartsCore.SkiaSharpView.Painting;
using SkiaSharp;
```

> **核心设计点**：
>
> - **每个测点独立数据集合**：`Dictionary<int, ObservableCollection<double>>`，切换测点时只切换 Series 绑定的数据源，不清空历史数据
> - **ComboBox 绑定** **`string[]`**：只包含测点名称，避免对象绑定导致的 SelectedItem 丢失问题
> - **所有测点数据同时采集**：定时器回调中遍历所有测点追加数据（切换测点时保留各自的历史曲线）
> - **Y 轴动态调整**：根据测点单位/报警范围动态设置轴名称和范围
> - **滑动窗口**：保持最多 30 个点（约 60 秒历史），超出时移除最早的数据点
>
> **注意**：
>
> - 需要在构造函数中初始化 `TrendXAxes` 和默认的 `TrendYAxes`，否则会出现空引用异常
> - `InitializeTrendChart` 只在首次选中设备时执行一次，后续刷新不重新创建集合
> - `SelectedTrendPoint` setter 同步更新 `_selectedTrendPointId`，用于后续数据采集

***

### 实现总结

**Day 5 已完成文件**：

```
Server/
├── Controllers/
│   └── RealtimeController.cs                         # GET 单设备 / 全量实时数据
├── Services/
│   ├── IRealtimeService.cs                           # 实时数据服务接口
│   └── RealtimeService.cs                            # 缓存值 + 数据库元信息 → DTO
└── Models/Dtos/
    ├── RealtimeDeviceDataDto.cs                      # 含 ProductLineId + DataPointValueDto
    └── RealtimeAllDataDto.cs                         # 含时间戳

Client/
├── Models/
│   ├── Dtos/
│   │   ├── RealtimeDeviceDataDto.cs                  # 与 Server 端同步
│   │   └── RealtimeAllDataDto.cs                     # 与 Server 端同步
│   ├── DataPointDisplayModel.cs                      # UI 显示模型（格式化值 + 颜色 + 报警文本）
│   ├── ProductionLineTreeNode.cs                     # 产线树节点
│   └── DeviceTreeNode.cs                            # 设备树节点（含 StatusColor）
├── Service/
│   ├── IRealtimeService.cs                           # 客户端实时数据服务接口
│   └── RealtimeService.cs                            # HTTP 调用 Server API（PropertyNameCaseInsensitive=true）
├── Views/
│   └── RealtimeView.xaml                             # 设备树 + 卡片面板 + 趋势图（纯 MVVM）
└── ViewModels/
    └── RealtimeViewModel.cs                          # SelectedItemChangedCommand + 定时刷新 + 趋势图滑动窗口
```

**数据流**：

1. `ModbusService`（BackgroundService）每秒轮询设备 → 写入 `ModbusDataCache`
2. `GET /api/realtime/devices/{id}/data` → `RealtimeService` 从缓存读值 + 数据库读元信息 → 组装 DTO
3. `GET /api/realtime/devices/all` → 返回所有在线设备的完整数据（含测点）
4. Client `LoadTreeAsync()` → 并行请求产线列表 + 全量设备数据 → 按 `ProductLineId` 过滤构建树
5. 定时器 Tick（每 2 秒）：
   - `LoadDeviceDataAsync()` → 获取单设备数据 → 更新 `DataPoints` → 卡片面板自动刷新
   - `UpdateTrendCollections()` → 为所有测点追加数据点 → 滑动窗口保持最近 30 个点

**关键设计决策**：

- **IsNavigationTarget = true**：复用 ViewModel 实例，设备树保持上次状态
- **反向属性而非自定义转换器**：`IsDeviceNotSelected` 更简洁
- **AlarmStatus 服务端判定**：前端只负责渲染颜色，业务逻辑集中服务端
- **PropertyNameCaseInsensitive = true**：必须配置，否则 JSON 反序列化失败
- **纯 MVVM**：`Interaction.Triggers` + `InvokeCommandAction` 处理 TreeView 选中事件
- **趋势图独立数据集合**：`Dictionary<int, ObservableCollection<double>>`，每个测点保留各自的历史曲线
- **ComboBox 绑定** **`string[]`**：避免对象绑定导致 SelectedItem 丢失
- **滑动窗口**：保持最近 30 个点（约 60 秒历史），超出时移除最早的数据点

***

### DI 注册汇总

| 生命周期      | 注册项                                            | 说明                  |
| --------- | ---------------------------------------------- | ------------------- |
| Scoped    | `IRealtimeService` / `RealtimeService`（Server） | 实时数据查询（读缓存 + 关联元信息） |
| Singleton | `IRealtimeService` / `RealtimeService`（Client） | 客户端 HTTP 调用         |

***

## Day 6 — 报警系统（Server 报警引擎 + WPF 报警中心）

**目标**：实现完整的报警检测、记录、展示和确认流程。

**实现前提**：

- `Alarm` 实体已定义（包含 `DeviceId`、`DataPointId`、`Level`、`AlarmType`、`Message`、`Value`、`Threshold`、`Status`、`AcknowledgedBy`、`AcknowledgedAt`、`OccurredAt`、`CreatedAt`）
- `AlarmLevel`、`AlarmType`、`AlarmStatus` 枚举已定义
- `DbContext` 已注册 `DbSet<Alarm>`，并配置复合索引 `IX_Alarms_Device_Status_Time`
- 级联删除已配置为 `Restrict`（防止误删设备时连带删除报警记录）
- Client 端 `AlarmView.xaml` 和 `AlarmViewModel.cs` 占位文件已创建

**目录规划**（新建/修改以下文件）：

```
Server/
├── Infrastructure/
│   ├── Repositories/
│   │   ├── IAlarmRepository.cs                    # 报警仓储接口
│   │   └── AlarmRepository.cs                     # 报警仓储实现
│   └── Modbus/
│   │   └── AlarmDetectionService.cs               # 报警检测引擎（BackgroundService）
├── Services/
│   ├── IAlarmService.cs                           # 报警服务接口
│   └── AlarmService.cs                            # 报警服务实现
└── Infrastructure/Controllers/
    └── AlarmsController.cs                        # 报警 API

Client/
├── Models/Dtos/
│   ├── AlarmDto.cs                                # 客户端报警 DTO
│   └── PagedResult.cs                             # 分页结果 DTO
├── Service/
│   ├── IAlarmService.cs                           # 客户端报警服务接口
│   └── AlarmService.cs                            # 客户端报警服务实现
├── Views/
│   └── AlarmView.xaml                             # 报警中心页面
└── ViewModels/
    └── AlarmViewModel.cs                          # 报警中心 ViewModel
```

***

### 任务一：报警检测引擎（BackgroundService）

#### 1a. 报警实体定义（已存在）

```csharp
// Server/Models/Entities/Alarm.cs（已实现）
[Table("Alarms")]
public class Alarm
{
    [Key]
    [DatabaseGenerated(DatabaseGeneratedOption.Identity)]
    public long Id { get; set; }

    public int DeviceId { get; set; }              // 外键 → Device
    [ForeignKey(nameof(DeviceId))]
    public Device? Device { get; set; }

    public int DataPointId { get; set; }           // 外键 → DataPoint
    [ForeignKey(nameof(DataPointId))]
    public DataPoint? DataPoint { get; set; }

    /// <summary>报警级别: 0紧急, 1重要, 2一般, 3提示</summary>
    public AlarmLevel Level { get; set; } = AlarmLevel.Info;

    /// <summary>报警类型: 0高限, 1低限</summary>
    public AlarmType AlarmType { get; set; }

    [Required]
    [MaxLength(200)]
    public string Message { get; set; } = string.Empty;

    public double Value { get; set; }              // 触发报警时的实际值

    public double Threshold { get; set; }          // 报警阈值

    /// <summary>状态: 0未确认, 1已确认</summary>
    public AlarmStatus Status { get; set; } = AlarmStatus.Unacknowledged;

    [MaxLength(50)]
    public string? AcknowledgedBy { get; set; }    // 确认人用户名

    public DateTime? AcknowledgedAt { get; set; }  // 确认时间

    public DateTime OccurredAt { get; set; } = DateTime.UtcNow;  // 发生时间

    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
```

**枚举定义**（已存在）：

```csharp
// Server/Models/Common/EnumType.cs
public enum AlarmLevel : byte
{
    Critical = 0,  // 紧急（红色）
    Major = 1,     // 重要（橙色）
    Minor = 2,     // 一般（黄色）
    Info = 3       // 提示（蓝色）
}

public enum AlarmType : byte
{
    HighLimit = 0, // 高限报警（Value > AlarmHigh）
    LowLimit = 1   // 低限报警（Value < AlarmLow）
}

public enum AlarmStatus : byte
{
    Unacknowledged = 0, // 未确认
    Acknowledged = 1    // 已确认
}
```

> **数据库索引**：`IX_Alarms_Device_Status_Time` 复合索引（DeviceId、Status、OccurredAt），优化"按设备查询未确认报警"的场景。级联删除设为 `Restrict`，防止误删设备时连带删除报警记录。

#### 1b. AlarmDetectionService — 报警检测后台服务

```csharp
// Server/Infrastructure/Modbus/AlarmDetectionService.cs
using Server.Infrastructure.Repositories;
using Server.Models.Common;
using Server.Models.Entities;

namespace Server.Infrastructure.Modbus
{
    public class AlarmDetectionService : BackgroundService
    {
        private readonly IServiceScopeFactory _scopeFactory;
        private readonly ModbusDataCache _cache;
        private readonly ILogger<AlarmDetectionService> _logger;

        public AlarmDetectionService(
            IServiceScopeFactory scopeFactory,
            ModbusDataCache cache,
            ILogger<AlarmDetectionService> logger)
        {
            _scopeFactory = scopeFactory;
            _cache = cache;
            _logger = logger;
        }

        protected override async Task ExecuteAsync(CancellationToken stoppingToken)
        {
            _logger.LogInformation("报警检测引擎启动");

            while (!stoppingToken.IsCancellationRequested)
            {
                try
                {
                    await DetectAlarmsAsync();
                    await Task.Delay(1000, stoppingToken); // 每 1 秒检测一次
                }
                catch (Exception ex)
                {
                    _logger.LogError(ex, "报警检测异常");
                    await Task.Delay(5000, stoppingToken); // 异常后延迟 5 秒再继续
                }
            }

            _logger.LogInformation("报警检测引擎停止");
        }

        private async Task DetectAlarmsAsync()
        {
            var scope = _scopeFactory.CreateScope();
            var dataPointRepo = scope.ServiceProvider.GetRequiredService<IDataPointRepository>();
            var alarmRepo = scope.ServiceProvider.GetRequiredService<IAlarmRepository>();
            var allCacheData = _cache.GetAllData();

            foreach (var (deviceId, pointData) in allCacheData)
            {
                var dataPoints = await dataPointRepo.GetByDeviceIdAsync(deviceId);

                foreach (var dataPoint in dataPoints)
                {
                    if (!pointData.TryGetValue(dataPoint.Id, out double currentValue))
                        continue;

                    // 检测高限报警（独立判断）
                    if (dataPoint.AlarmHigh.HasValue && currentValue > dataPoint.AlarmHigh)
                    {
                        await CreateAlarmIfNotExistsAsync(
                            alarmRepo,
                            deviceId,
                            dataPoint.Id,
                            currentValue,
                            dataPoint.AlarmHigh.Value,
                            AlarmType.HighLimit,
                            dataPoint.Name,
                            dataPoint.AlarmHigh,
                            dataPoint.AlarmLow);
                    }

                    // 检测低限报警（独立判断，与高限并列）
                    if (dataPoint.AlarmLow.HasValue && currentValue < dataPoint.AlarmLow.Value)
                    {
                        await CreateAlarmIfNotExistsAsync(
                            alarmRepo,
                            deviceId,
                            dataPoint.Id,
                            currentValue,
                            dataPoint.AlarmLow.Value,
                            AlarmType.LowLimit,
                            dataPoint.Name,
                            dataPoint.AlarmHigh,
                            dataPoint.AlarmLow);
                    }
                }
            }
        }

        /// <summary>
        /// 报警去重：同一测点、同一类型、未确认的报警不重复生成
        /// </summary>
        private async Task CreateAlarmIfNotExistsAsync(
            IAlarmRepository alarmRepo,
            int deviceId,
            int dataPointId,
            double currentValue,
            double threshold,
            AlarmType alarmType,
            string pointName,
            double? alarmHigh,
            double? alarmLow)
        {
            // 查询是否存在未确认的同类报警
            var existAlarm = await alarmRepo.GetUnacknowledgedAlarmAsync(deviceId, dataPointId, alarmType);
            if (existAlarm != null)
                return;  // 已存在未确认报警，不重复生成（去重）

            var level = DetermineAlarmLevel(currentValue, threshold, alarmType, alarmHigh, alarmLow);

            var alarm = new Alarm
            {
                DeviceId = deviceId,
                DataPointId = dataPointId,
                Level = level,
                AlarmType = alarmType,
                Message = $"{pointName} {GetAlarmTypeText(alarmType)}报警（当前值: {currentValue:F2}, 阈值: {threshold:F2})",
                Value = currentValue,
                Threshold = threshold,
                Status = AlarmStatus.Unacknowledged,
                OccurredAt = DateTime.UtcNow
            };

            await alarmRepo.AddAsync(alarm);
            await alarmRepo.SaveChangedAsync();

            _logger.LogWarning($"报警生成: {alarm.Message}");
        }

        /// <summary>
        /// 根据偏差程度判定报警等级
        /// </summary>
        private AlarmLevel DetermineAlarmLevel(
            double value,
            double threshold,
            AlarmType alarmType,
            double? alarmHigh,
            double? alarmLow)
        {
            if (alarmHigh.HasValue && alarmLow.HasValue)
            {
                var range = alarmHigh.Value - alarmLow.Value;
                var gapValue = alarmType == AlarmType.HighLimit ? value - threshold : threshold - value;
                var ratio = gapValue / range;

                // 偏差超过 50% → 紧急
                if (ratio > 0.5)
                    return AlarmLevel.Critical;
                // 偏差超过 20% → 重要
                if (ratio > 0.2)
                    return AlarmLevel.Major;
                // 偏差超过 10% → 一般
                if (ratio > 0.1)
                    return AlarmLevel.Minor;
            }

            return AlarmLevel.Major;
        }

        private string GetAlarmTypeText(AlarmType type) => type == AlarmType.HighLimit ? "高限" : "低限";
    }
}
```

> **报警去重机制**：调用 `alarmRepo.GetUnacknowledgedAlarmAsync(deviceId, dataPointId, alarmType)` 查询是否存在未确认的同类报警，如果存在则不重复生成。
>
> **报警等级判定**：根据偏差程度动态判定（偏差超过50%→紧急，20%→重要，10%→一般），若无双限阈值则默认为重要级别。
>
> **高/低限独立检测**：高限和低限检测是并列关系，不是嵌套关系。若写成嵌套，会导致低限报警永远无法触发。
>
> **后台服务注册**：使用 `IServiceScopeFactory` 在后台服务中创建 Scoped 服务实例（Repository 是 Scoped 生命周期）。

#### 1c. IAlarmRepository + AlarmRepository

```csharp
// Server/Infrastructure/Repositories/IAlarmRepository.cs
using Server.Models.Common;
using Server.Models.Entities;

namespace Server.Infrastructure.Repositories
{
    public interface IAlarmRepository : IRepositoryBase<Alarm>
    {
        /// <summary>查询指定设备、测点、类型的未确认报警</summary>
        Task<Alarm?> GetUnacknowledgedAlarmAsync(int deviceId, int dataPointId, AlarmType alarmType);

        /// <summary>查询当前活跃报警（未确认）</summary>
        Task<List<Alarm>> GetCurrentAlarmsAsync();

        /// <summary>查询报警历史（分页 + 筛选）</summary>
        Task<PagedResult<Alarm>> GetHistoryAsync(int page, int pageSize, int? deviceId, AlarmLevel? level, AlarmStatus? status);

        /// <summary>确认报警</summary>
        Task<bool> AcknowledgeAsync(long alarmId, string acknowledgedBy);
    }
}
```

```csharp
// Server/Infrastructure/Repositories/AlarmRepository.cs
using Microsoft.EntityFrameworkCore;
using Server.Infrastructure.Data;
using Server.Models.Common;
using Server.Models.Entities;

namespace Server.Infrastructure.Repositories
{
    public class AlarmRepository : RepositoryBase<Alarm>, IAlarmRepository
    {
        public AlarmRepository(SmartFactoryDbContext context) : base(context) { }

        public async Task<Alarm?> GetUnacknowledgedAlarmAsync(int deviceId, int dataPointId, AlarmType alarmType)
        {
            return await _dbSet
                .Where(a => a.DeviceId == deviceId
                         && a.DataPointId == dataPointId
                         && a.AlarmType == alarmType
                         && a.Status == AlarmStatus.Unacknowledged)
                .FirstOrDefaultAsync();
        }

        public async Task<List<Alarm>> GetCurrentAlarmsAsync()
        {
            return await _dbSet
                .Where(a => a.Status == AlarmStatus.Unacknowledged)
                .Include(a => a.Device)
                .Include(a => a.DataPoint)
                .OrderByDescending(a => a.Level)      // 紧急报警优先
                .ThenByDescending(a => a.OccurredAt)  // 最新报警优先
                .ToListAsync();
        }

        public async Task<PagedResult<Alarm>> GetHistoryAsync(
            int page,
            int pageSize,
            int? deviceId,
            AlarmLevel? level,
            AlarmStatus? status)
        {
            var query = _dbSet
                .Include(a => a.Device)
                .Include(a => a.DataPoint)
                .AsQueryable();

            if (deviceId.HasValue)
                query = query.Where(a => a.DeviceId == deviceId);

            if (level.HasValue)
                query = query.Where(a => a.Level == level.Value);

            if (status.HasValue)
                query = query.Where(a => a.Status == status.Value);

            int totalCount = await query.CountAsync();

            var items = await query
                .OrderByDescending(a => a.OccurredAt)
                .Skip((page - 1) * pageSize)
                .Take(pageSize)
                .ToListAsync();

            return new PagedResult<Alarm>
            {
                Items = items,
                TotalCount = totalCount,
                Page = page,
                PageSize = pageSize
            };
        }

        public async Task<bool> AcknowledgeAsync(long alarmId, string acknowledgedBy)
        {
            // Alarm.Id 是 long 类型，不能直接用基类的 GetByIdAsync(int)
            var alarm = await _dbSet.FindAsync(alarmId);
            if (alarm == null || alarm.Status == AlarmStatus.Acknowledged)
                return false;

            alarm.Status = AlarmStatus.Acknowledged;
            alarm.AcknowledgedBy = acknowledgedBy;
            alarm.AcknowledgedAt = DateTime.UtcNow;

            Update(alarm);
            await SaveChangedAsync();
            return true;
        }
    }
}
```

> **Include 关联查询**：`Include(a => a.Device)` 和 `Include(a => a.DataPoint)` 加载关联实体，避免 DTO 映射时出现空引用。
>
> **排序策略**：`OrderByDescending(a => a.Level)` 紧急报警优先，然后按时间降序（最新报警优先）。
>
> **`Where`** **必须重新赋值**：`query.Where(...)` 返回新的 `IQueryable`，必须写 `query = query.Where(...)`，否则筛选条件会丢失。
>
> **`FindAsync`** **主键类型匹配**：`Alarm.Id` 是 `long`，而 `RepositoryBase.GetByIdAsync` 参数是 `int`，直接调用会导致类型不匹配异常。应使用 `_dbSet.FindAsync(alarmId)`。

#### 1d. Program.cs 注册

```csharp
// DI 注册
builder.Services.AddScoped<IAlarmRepository, AlarmRepository>();

// 后台服务注册
builder.Services.AddHostedService<AlarmDetectionService>();
```

***

### 任务二：报警 API

#### 2a. IAlarmService + AlarmService

```csharp
// Server/Services/IAlarmService.cs
using Server.Models.Common;
using Server.Models.Dtos;

namespace Server.Services
{
    public interface IAlarmService
    {
        Task<ApiResult<List<AlarmDto>>> GetCurrentAlarmsAsync();
        Task<ApiResult<PagedResult<AlarmDto>>> GetHistoryAsync(int page, int pageSize, int? deviceId, string? level, string? status);
        Task<ApiResult> AcknowledgeAsync(long alarmId, string acknowledgedBy);
    }
}
```

```csharp
// Server/Services/AlarmService.cs
using AutoMapper;
using Server.Infrastructure.Repositories;
using Server.Models.Common;
using Server.Models.Dtos;

namespace Server.Services
{
    public class AlarmService : IAlarmService
    {
        private readonly IAlarmRepository _alarmRepo;
        private readonly IMapper _mapper;

        public AlarmService(IAlarmRepository alarmRepo, IMapper mapper)
        {
            _alarmRepo = alarmRepo;
            _mapper = mapper;
        }

        public async Task<ApiResult<List<AlarmDto>>> GetCurrentAlarmsAsync()
        {
            var alarms = await _alarmRepo.GetCurrentAlarmsAsync();
            var alarmDtos = _mapper.Map<List<AlarmDto>>(alarms);
            return ApiResult<List<AlarmDto>>.Success(alarmDtos);
        }

        public async Task<ApiResult<PagedResult<AlarmDto>>> GetHistoryAsync(
            int page,
            int pageSize,
            int? deviceId,
            string? level,
            string? status)
        {
            AlarmLevel? levelEnum = level != null ? Enum.Parse<AlarmLevel>(level) : null;
            AlarmStatus? statusEnum = status != null ? Enum.Parse<AlarmStatus>(status) : null;

            // 注意：第二个参数必须是 pageSize，不能传成 page
            var paged = await _alarmRepo.GetHistoryAsync(page, pageSize, deviceId, levelEnum, statusEnum);
            var dtoItems = _mapper.Map<List<AlarmDto>>(paged.Items);

            return ApiResult<PagedResult<AlarmDto>>.Success(new PagedResult<AlarmDto>
            {
                Items = dtoItems,
                TotalCount = paged.TotalCount,
                Page = page,
                PageSize = pageSize
            });
        }

        public async Task<ApiResult> AcknowledgeAsync(long alarmId, string acknowledgedBy)
        {
            var success = await _alarmRepo.AcknowledgeAsync(alarmId, acknowledgedBy);
            if (!success)
                return ApiResult.Fail(400, "报警不存在或已确认");

            return ApiResult.Success("报警已确认");
        }
    }
}
```

> **枚举字符串解析**：`Enum.Parse<AlarmLevel>(level)` 默认区分大小写。客户端传参时要与枚举名称完全一致（如 `"Acknowledged"`）。

#### 2b. AlarmDto + AutoMapper 配置

```csharp
// Server/Models/Dtos/AlarmDto.cs（已存在）
public class AlarmDto
{
    public long Id { get; set; }
    public int DeviceId { get; set; }
    public string? DeviceName { get; set; }
    public int DataPointId { get; set; }
    public string? DataPointName { get; set; }
    public string Level { get; set; } = string.Empty;
    public string AlarmType { get; set; } = string.Empty;
    public string Message { get; set; } = string.Empty;
    public double Value { get; set; }
    public double Threshold { get; set; }
    public string Status { get; set; } = string.Empty;
    public DateTime OccurredAt { get; set; }
}
```

> **枚举转字符串**：DTO 中 `Level`、`AlarmType`、`Status` 使用字符串类型，便于前端直接显示（无需前端再转换枚举）。

AutoMapper 配置：

```csharp
// Server/Infrastructure/Mappings/MappingProfile.cs
public class MappingProfile : Profile
{
    public MappingProfile()
    {
        // 报警映射：枚举 → 字符串
        CreateMap<Alarm, AlarmDto>()
            .ForMember(dest => dest.DeviceName,
                opt => opt.MapFrom(
                    src => src.Device != null ? src.Device.Name : null))
            .ForMember(dest => dest.DataPointName,
                opt => opt.MapFrom(
                    src => src.DataPoint != null ? src.DataPoint.Name : null))
            .ForMember(dest => dest.Level, opt => opt.MapFrom(src => src.Level.ToString()))
            .ForMember(dest => dest.AlarmType, opt => opt.MapFrom(src => src.AlarmType.ToString()))
            .ForMember(dest => dest.Status, opt => opt.MapFrom(src => src.Status.ToString()));
    }
}
```

> **注意**：当前 `MappingProfile.cs` 中对 `Alarm → AlarmDto` 写了两次 `CreateMap`，第二次会覆盖第一次。保留带枚举转字符串的第二次即可，建议后续清理重复映射。

#### 2c. AlarmsController

```csharp
// Server/Infrastructure/Controllers/AlarmsController.cs
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Server.Services;
using System.Security.Claims;

namespace Server.Infrastructure.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    [Authorize]
    public class AlarmsController : ControllerBase
    {
        private readonly IAlarmService _alarmService;

        public AlarmsController(IAlarmService alarmService)
        {
            _alarmService = alarmService;
        }

        /// <summary>GET api/alarms/current — 当前活跃报警</summary>
        [HttpGet("current")]
        public async Task<IActionResult> GetCurrentAlarms()
        {
            var result = await _alarmService.GetCurrentAlarmsAsync();
            return Ok(result);
        }

        /// <summary>GET api/alarms/history — 报警历史（分页 + 筛选）</summary>
        [HttpGet("history")]
        public async Task<IActionResult> GetHistory(
            [FromQuery] int page = 1,
            [FromQuery] int pageSize = 20,
            [FromQuery] int? deviceId = null,
            [FromQuery] string? level = null,
            [FromQuery] string? status = null)
        {
            var result = await _alarmService.GetHistoryAsync(page, pageSize, deviceId, level, status);
            return Ok(result);
        }

        /// <summary>PUT api/alarms/{id}/acknowledge — 确认报警</summary>
        [HttpPut("{id}/acknowledge")]
        public async Task<IActionResult> Acknowledge(long id)
        {
            // 从 JWT Token 中获取当前用户名
            var username = User.FindFirst(ClaimTypes.Name)?.Value ?? "Unknown";

            var result = await _alarmService.AcknowledgeAsync(id, username);
            return result.Code == 200 ? Ok(result) : BadRequest(result);
        }
    }
}
```

> **Authorize 保护**：所有报警 API 都需要认证，`User.FindFirst(ClaimTypes.Name)` 从 JWT Token 中获取当前用户名作为确认人。
>
> **Controller 注入注意**：若需要日志，应注入 `ILogger<AlarmsController>`（泛型），不能注入非泛型 `ILogger`，否则 DI 无法解析。当前实现未使用日志，故构造函数只保留 `IAlarmService`。
>
> **筛选参数**：`deviceId`、`level`（Critical/Major/Minor/Info）、`status`（Unacknowledged/Acknowledged）均为可选参数。

#### 2d. Program.cs 注册

```csharp
builder.Services.AddScoped<IAlarmService, AlarmService>();
```

***

### 任务三：WPF 报警中心页面

#### 3a. 客户端 DTO + Service

```csharp
// Client/Models/Dtos/AlarmDto.cs（与 Server 同步）
namespace Client.Models.Dtos
{
    public class AlarmDto
    {
        public long Id { get; set; }
        public int DeviceId { get; set; }
        public string? DeviceName { get; set; }
        public int DataPointId { get; set; }
        public string? DataPointName { get; set; }
        public string Level { get; set; } = string.Empty;
        public string AlarmType { get; set; } = string.Empty;
        public string Message { get; set; } = string.Empty;
        public double Value { get; set; }
        public double Threshold { get; set; }
        public string Status { get; set; } = string.Empty;
        public DateTime OccurredAt { get; set; }
    }
}
```

```csharp
// Client/Models/Dtos/PagedResult.cs
namespace Client.Models.Dtos
{
    public class PagedResult<T>
    {
        public List<T> Items { get; set; } = new();
        public int TotalCount { get; set; }
        public int Page { get; set; }
        public int PageSize { get; set; }
    }
}
```

```csharp
// Client/Service/IAlarmService.cs
using Client.Models.Dtos;

namespace Client.Service
{
    public interface IAlarmService
    {
        Task<List<AlarmDto>?> GetCurrentAlarmsAsync();
        Task<PagedResult<AlarmDto>?> GetHistoryAsync(int page, int pageSize, int? deviceId, string? level, string? status);
        Task<bool> AcknowledgeAsync(long alarmId);
    }
}
```

```csharp
// Client/Service/AlarmService.cs
using Client.Infrastructure;
using Client.Models.Dtos;
using System.Net.Http.Json;
using System.Text.Json;

namespace Client.Service
{
    public class AlarmService : IAlarmService
    {
        private readonly HttpClientBase _http;
        private readonly JsonSerializerOptions _jsonOptions = new() { PropertyNameCaseInsensitive = true };

        public AlarmService(HttpClientBase http)
        {
            _http = http;
        }

        public async Task<List<AlarmDto>?> GetCurrentAlarmsAsync()
        {
            var response = await _http.GetAsync("api/alarms/current");
            if (!response.IsSuccessStatusCode)
                return null;

            var result = await response.Content.ReadFromJsonAsync<ApiResult<List<AlarmDto>>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<PagedResult<AlarmDto>?> GetHistoryAsync(
            int page,
            int pageSize,
            int? deviceId,
            string? level,
            string? status)
        {
            // 注意：pageSize 后面不能加 /，否则服务端参数解析失败
            var url = $"api/alarms/history?page={page}&pageSize={pageSize}";
            if (deviceId.HasValue)
                url += $"&deviceId={deviceId.Value}";
            if (!string.IsNullOrEmpty(level))
                url += $"&level={level}";
            if (!string.IsNullOrEmpty(status))
                url += $"&status={status}";

            var response = await _http.GetAsync(url);
            if (!response.IsSuccessStatusCode)
                return null;

            var result = await response.Content.ReadFromJsonAsync<ApiResult<PagedResult<AlarmDto>>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<bool> AcknowledgeAsync(long alarmId)
        {
            var response = await _http.PutAsync<object>($"api/alarms/{alarmId}/acknowledge", null);
            return response.IsSuccessStatusCode;
        }
    }
}
```

#### 3b. App.xaml.cs 注册

```csharp
// RegisterTypes 新增
containerRegistry.RegisterSingleton<IAlarmService, AlarmService>();
```

#### 3c. AlarmView\.xaml — 报警中心页面（完整实现）

```xml
<UserControl x:Class="Client.Views.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>  <!--标题栏-->
            <RowDefinition Height="auto"/>  <!--筛选控件-->
            <RowDefinition Height="*"/>     <!--报警列表-->
            <RowDefinition Height="auto"/>  <!--操作按钮 + 分页-->
        </Grid.RowDefinitions>

        <!--标题栏-->
        <TextBlock Grid.Row="0"
                   Text="报警中心"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!--筛选控件-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="报警等级：" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding AlarmLevels}"
                      SelectedItem="{Binding SelectedLevel}"
                      Margin="0,0,20,0"/>

            <TextBlock Text="报警状态：" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding AlarmStatuses}"
                      SelectedItem="{Binding SelectedStatus}"
                      Margin="0,0,20,0"/>

            <Button Content="查询历史"
                    Command="{Binding LoadHistoryCommand}"
                    Width="80"
                    Margin="0,0,10,0"/>

            <Button Content="刷新当前"
                    Command="{Binding RefreshCurrentCommand}"
                    Width="80"/>
        </StackPanel>

        <!--报警列表（DataGrid）-->
        <DataGrid Grid.Row="2"
                  Style="{StaticResource MinimalDataGridStyle}"
                  ItemsSource="{Binding Alarms}"
                  SelectedItem="{Binding SelectedAlarm}"
                  BorderThickness="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="等级" Binding="{Binding Level}" Width="80"/>
                <DataGridTextColumn Header="类型" Binding="{Binding AlarmType}" Width="80"/>
                <DataGridTextColumn Header="设备" Binding="{Binding DeviceName}" Width="120"/>
                <DataGridTextColumn Header="测点" Binding="{Binding DataPointName}" Width="120"/>
                <DataGridTextColumn Header="触发值" Binding="{Binding Value, StringFormat=F2}" Width="80"/>
                <DataGridTextColumn Header="阈值" Binding="{Binding Threshold, StringFormat=F2}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="发生时间" Binding="{Binding OccurredAt, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--操作按钮 + 分页-->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="确认报警"
                        Command="{Binding AcknowledgeCommand}"
                        Width="100"
                        Margin="0,0,10,0"/>
            </StackPanel>

            <!--分页控件（只在历史模式下显示）-->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        Visibility="{Binding IsHistoryMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button Content="上一页"
                        Command="{Binding PreviousPageCommand}"
                        Width="70"
                        Margin="0,0,10,0"/>
                <TextBlock Text="{Binding PageInfo}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <Button Content="下一页"
                        Command="{Binding NextPageCommand}"
                        Width="70"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
```

> **MinimalDataGridStyle**：使用全局精简样式统一 DataGrid 外观，包括列头、行高、选中态等，避免在每个页面重复定义 `RowStyle`。
>
> **分页控件显示控制**：只在历史模式下显示分页按钮（`IsHistoryMode` 绑定 Visibility）。

#### 3d. AlarmViewModel.cs — 报警中心 ViewModel

```csharp
// Client/ViewModels/AlarmViewModel.cs
using Client.Models.Dtos;
using Client.Service;
using System.Collections.ObjectModel;

namespace Client.ViewModels
{
    public class AlarmViewModel : BindableBase, INavigationAware
    {
        private readonly IAlarmService _alarmService;

        // === View 字段 ===
        private ObservableCollection<AlarmDto> _alarms = new();
        private AlarmDto? _selectedAlarm;
        private string _selectedLevel = "全部";
        private string _selectedStatus = "Unacknowledged";
        private bool _isHistoryMode;
        private int _currentPage = 1;
        private int _totalPages = 1;
        private string _pageInfo = "第 1 页";

        // === View 命令 ===
        public DelegateCommand RefreshCurrentCommand { get; }
        public DelegateCommand LoadHistoryCommand { get; }
        public DelegateCommand AcknowledgeCommand { get; }
        public DelegateCommand PreviousPageCommand { get; }
        public DelegateCommand NextPageCommand { get; }

        // === 筛选选项 ===
        public string[] AlarmLevels { get; } = { "全部", "Critical", "Major", "Minor", "Info" };
        public string[] AlarmStatuses { get; } = { "全部", "Unacknowledged", "Acknowledged" };

        public AlarmViewModel(IAlarmService alarmService)
        {
            _alarmService = alarmService;

            RefreshCurrentCommand = new DelegateCommand(async () => await LoadCurrentAlarmsAsync());
            LoadHistoryCommand = new DelegateCommand(async () => await LoadHistoryAlarmsAsync());
            AcknowledgeCommand = new DelegateCommand(async () => await AcknowledgeSelectedAlarmAsync(), CanAcknowledge);
            PreviousPageCommand = new DelegateCommand(async () => await PreviousPageAsync(), CanGoPrevious);
            NextPageCommand = new DelegateCommand(async () => await NextPageAsync(), CanGoNext);

            // 初始加载当前报警
            _ = LoadCurrentAlarmsAsync();
        }

        // === INavigationAware ===
        public bool IsNavigationTarget(NavigationContext navigationContext) => true;

        public void OnNavigatedFrom(NavigationContext navigationContext) { }

        public void OnNavigatedTo(NavigationContext navigationContext)
        {
            // 每次导航进入时刷新当前报警
            _ = LoadCurrentAlarmsAsync();
        }

        // === 加载当前报警 ===
        private async Task LoadCurrentAlarmsAsync()
        {
            IsHistoryMode = false;
            SelectedStatus = AlarmStatuses[1];  // 切换为 "Unacknowledged"，与当前模式保持一致
            var alarms = await _alarmService.GetCurrentAlarmsAsync();
            if (alarms != null)
            {
                Alarms = new ObservableCollection<AlarmDto>(alarms);
            }
        }

        // === 加载历史报警 ===
        private async Task LoadHistoryAlarmsAsync()
        {
            IsHistoryMode = true;
            _currentPage = 1;
            await LoadHistoryPageAsync();
        }

        private async Task LoadHistoryPageAsync()
        {
            string? level = SelectedLevel == "全部" ? null : SelectedLevel;
            string? status = SelectedStatus == "全部" ? null : SelectedStatus;

            var result = await _alarmService.GetHistoryAsync(_currentPage, 10, null, level, status);
            if (result != null)
            {
                Alarms = new ObservableCollection<AlarmDto>(result.Items);
                _totalPages = (int)Math.Ceiling(result.TotalCount / 10.0);
                PageInfo = $"第 {_currentPage}/{_totalPages} 页（共 {result.TotalCount} 条）";

                // 更新按钮状态
                PreviousPageCommand.RaiseCanExecuteChanged();
                NextPageCommand.RaiseCanExecuteChanged();
            }
        }

        // === 确认报警 ===
        private async Task AcknowledgeSelectedAlarmAsync()
        {
            if (SelectedAlarm == null)
                return;

            var success = await _alarmService.AcknowledgeAsync(SelectedAlarm.Id);
            if (success)
            {
                // 刷新列表
                if (IsHistoryMode)
                    await LoadHistoryPageAsync();
                else
                    await LoadCurrentAlarmsAsync();
            }
        }

        private bool CanAcknowledge() => SelectedAlarm != null && SelectedAlarm.Status == "Unacknowledged";

        // === 分页 ===
        private async Task PreviousPageAsync()
        {
            if (_currentPage > 1)
            {
                _currentPage--;
                await LoadHistoryPageAsync();
            }
        }

        private async Task NextPageAsync()
        {
            if (_currentPage < _totalPages)
            {
                _currentPage++;
                await LoadHistoryPageAsync();
            }
        }

        private bool CanGoPrevious() => _currentPage > 1;
        private bool CanGoNext() => _currentPage < _totalPages;

        // === 属性 ===
        public ObservableCollection<AlarmDto> Alarms
        {
            get => _alarms;
            set => SetProperty(ref _alarms, value);
        }

        public AlarmDto? SelectedAlarm
        {
            get => _selectedAlarm;
            set
            {
                SetProperty(ref _selectedAlarm, value);
                AcknowledgeCommand.RaiseCanExecuteChanged();
            }
        }

        public string SelectedLevel
        {
            get => _selectedLevel;
            set
            {
                if (SetProperty(ref _selectedLevel, value))
                {
                    _currentPage = 1;
                    _ = LoadHistoryPageAsync();  // 切换等级后重新加载
                }
            }
        }

        public string SelectedStatus
        {
            get => _selectedStatus;
            set
            {
                if (SetProperty(ref _selectedStatus, value))
                {
                    _currentPage = 1;
                    _ = LoadHistoryPageAsync();  // 切换状态后重新加载
                }
            }
        }

        public bool IsHistoryMode
        {
            get => _isHistoryMode;
            set => SetProperty(ref _isHistoryMode, value);
        }

        public string PageInfo
        {
            get => _pageInfo;
            set => SetProperty(ref _pageInfo, value);
        }
    }
}
```

> **Command CanExecute 条件**：
>
> - `AcknowledgeCommand`：只有选中未确认报警时才可执行
> - `PreviousPageCommand`：当前页 > 1 时可执行
> - `NextPageCommand`：当前页 < 总页数时可执行
>
> **RaiseCanExecuteChanged**：`SelectedAlarm` setter 中触发 `AcknowledgeCommand.RaiseCanExecuteChanged()`，确保按钮状态实时更新。
>
> **刷新当前按钮**：点击后将状态筛选重置为 `"Unacknowledged"`，然后调用 `GetCurrentAlarmsAsync()`，仅加载未确认报警。
>
> **分页大小**：历史报警分页固定为每页 10 条。

***

### 实现总结

**Day 6 已完成文件**：

```
Server/
├── Infrastructure/
│   ├── Repositories/
│   │   ├── IAlarmRepository.cs                    # GetUnacknowledgedAlarm / GetCurrentAlarms / GetHistory / Acknowledge
│   │   └── AlarmRepository.cs                     # Include(Device/DataPoint) + 排序 + 分页
│   └── Modbus/
│   │   └── AlarmDetectionService.cs               # 报警检测引擎（每1秒检测）+ 去重 + 等级判定
├── Models/
│   ├── Common/
│   │   └── PageResult.cs                          # 服务端分页结果（类名为 PagedResult<T>）
│   └── Dtos/
│       └── AlarmDto.cs                            # 报警 DTO
├── Services/
│   ├── IAlarmService.cs                           # GetCurrent / GetHistory / Acknowledge
│   └── AlarmService.cs                            # AutoMapper 映射 + 枚举转字符串
└── Infrastructure/Controllers/
    └── AlarmsController.cs                        # GET current / history + PUT acknowledge

Client/
├── Models/Dtos/
│   ├── AlarmDto.cs                                # 与 Server 同步
│   └── PagedResult.cs                             # 客户端分页结果 DTO
├── Service/
│   ├── IAlarmService.cs                           # GetCurrentAlarms / GetHistory / Acknowledge
│   └── AlarmService.cs                            # HTTP 调用 + PropertyNameCaseInsensitive=true
├── Views/
│   └── AlarmView.xaml                             # DataGrid + 筛选控件 + 分页
└── ViewModels/
    └── AlarmViewModel.cs                          # 当前/历史模式切换 + Command CanExecute 条件
```

**数据流**：

1. `AlarmDetectionService`（BackgroundService）每 1 秒检测 → 从 `ModbusDataCache` 读取当前值 → 判断是否超出阈值
2. 报警去重：查询 `GetUnacknowledgedAlarmAsync`，若存在未确认同类报警则不重复生成
3. 报警等级判定：根据偏差程度动态判定（50%→Critical，20%→Major，10%→Minor）
4. `GET /api/alarms/current` → 返回未确认报警列表（按等级降序）
5. `GET /api/alarms/history` → 分页查询历史报警（支持设备、等级、状态筛选）
6. `PUT /api/alarms/{id}/acknowledge` → 更新报警状态为已确认，记录确认人和时间

**关键设计决策**：

- **报警去重机制**：同一测点、同一类型、未确认的报警不重复生成，避免报警风暴
- **偏差程度等级判定**：根据相对偏差动态判定报警等级（而非固定阈值）
- **级联删除 Restrict**：防止误删设备时连带删除报警记录，保留历史审计数据
- **复合索引优化**：`IX_Alarms_Device_Status_Time` 优化"按设备查询未确认报警"场景
- **全局 DataGrid 样式**：通过 `MinimalDataGridStyle` 统一列表外观，页面内不再重复定义 RowStyle
- **Command CanExecute 条件**：确保确认按钮只在选中未确认报警时可用，防止误操作
- **服务端/客户端 PagedResult 分离**：服务端使用 `Server.Models.Common.PageResult.cs`（文件名 PageResult），客户端使用 `Client.Models.Dtos.PagedResult.cs`，结构一致但可独立演进

***

### DI 注册汇总

| 生命周期          | 注册项                                            | 说明                 |
| ------------- | ---------------------------------------------- | ------------------ |
| Scoped        | `IAlarmRepository` / `AlarmRepository`（Server） | 报警仓储（Include 关联查询） |
| Scoped        | `IAlarmService` / `AlarmService`（Server）       | 报警业务服务（枚举转字符串）     |
| Singleton     | `IAlarmService` / `AlarmService`（Client）       | 客户端 HTTP 调用        |
| HostedService | `AlarmDetectionService`                        | 报警检测后台服务（每1秒检测）    |

***

## Day 7 — 历史数据存储 + 历史趋势页面

**目标**：在现有 `DeviceDatas` 表基础上，实现历史数据批量归档、历史查询 API、WPF 历史趋势分析页面，并提供 Excel 导出功能。

**实现前提**：

- `SmartFactoryDbContext` 已注册 `DbSet<DeviceData>`，且已建立 `(DeviceId, DataPointId, RecordedAt)` 复合索引。
- `ModbusDataCache` 已提供 `GetAllData()` 快照方法。
- 实时数据接口（`RealtimeService`、`IRealtimeService`）已可用，客户端可用其构建设备树并获取测点列表。
- 客户端已安装 `LiveChartsCore.SkiaSharpView.WPF 2.0.5`。

**目录规划**：

```
Server/
├── Infrastructure/Repositories/
│   ├── IDeviceDataRepository.cs
│   └── DeviceDataRepository.cs
├── Infrastructure/Modbus/
│   └── DataArchiveService.cs
├── Models/Dtos/
│   ├── HistoryDataPointDto.cs
│   └── HistoryStatisticsDto.cs
├── Services/
│   ├── IHistoryService.cs
│   └── HistoryService.cs
└── Infrastructure/Controllers/
    └── HistoryController.cs

Client/
├── Models/Dtos/
│   ├── HistoryDataPointDto.cs
│   └── HistoryStatisticsDto.cs
├── Service/
│   ├── IHistoryService.cs
│   └── HistoryService.cs
├── ViewModels/
│   └── HistoryViewModel.cs
└── Views/
    └── HistoryView.xaml
```

***

### 任务一：历史数据批量存储

#### 1a. IDeviceDataRepository.cs + DeviceDataRepository.cs

```csharp
// Server/Infrastructure/Repositories/IDeviceDataRepository.cs
using Server.Models.Entities;

namespace Server.Infrastructure.Repositories
{
    public interface IDeviceDataRepository : IRepositoryBase<DeviceData>
    {
        Task BulkInsertAsync(List<DeviceData> entities);
        Task<int> CleanupOldDataAsync(DateTime cutoff);
        Task<List<DeviceData>> GetHistoryAsync(int deviceId, int dataPointId, DateTime start, DateTime end);
    }
}
```

```csharp
// Server/Infrastructure/Repositories/DeviceDataRepository.cs
using Microsoft.EntityFrameworkCore;
using Server.Infrastructure.Data;
using Server.Models.Entities;

namespace Server.Infrastructure.Repositories
{
    public class DeviceDataRepository : RepositoryBase<DeviceData>, IDeviceDataRepository
    {
        public DeviceDataRepository(SmartFactoryDbContext context) : base(context) { }

        public async Task BulkInsertAsync(List<DeviceData> entities)
        {
            await _dbSet.AddRangeAsync(entities);
            await _context.SaveChangesAsync();
        }

        public async Task<int> CleanupOldDataAsync(DateTime cutoff)
        {
            var oldData = await _dbSet.Where(d => d.RecordedAt < cutoff).ToListAsync();
            if (oldData.Count == 0) return 0;

            _dbSet.RemoveRange(oldData);
            await _context.SaveChangesAsync();
            return oldData.Count;
        }

        public async Task<List<DeviceData>> GetHistoryAsync(int deviceId, int dataPointId, DateTime start, DateTime end)
        {
            return await _dbSet
                .Where(d => d.DeviceId == deviceId
                            && d.DataPointId == dataPointId
                            && d.RecordedAt >= start
                            && d.RecordedAt <= end)
                .OrderBy(d => d.RecordedAt)
                .ToListAsync();
        }
    }
}
```

> **批量写入策略**：历史表数据量大，每次直接 `AddRangeAsync` + `SaveChangesAsync` 入库。后续如果数据量继续增长，可替换为 `EFCore.BulkExtensions` 或 SQLBulkCopy。

#### 1b. DataArchiveService.cs

```csharp
// Server/Infrastructure/Modbus/DataArchiveService.cs
using Server.Infrastructure.Repositories;
using Server.Models.Entities;

namespace Server.Infrastructure.Modbus
{
    public class DataArchiveService : BackgroundService
    {
        private readonly IServiceScopeFactory _scopeFactory;
        private readonly ModbusDataCache _cache;
        private readonly ILogger<DataArchiveService> _logger;

        private readonly TimeSpan _archiveInterval = TimeSpan.FromSeconds(5);
        private readonly int _retentionDays = 30;

        public DataArchiveService(
            IServiceScopeFactory scopeFactory,
            ModbusDataCache cache,
            ILogger<DataArchiveService> logger)
        {
            _scopeFactory = scopeFactory;
            _cache = cache;
            _logger = logger;
        }

        protected override async Task ExecuteAsync(CancellationToken stoppingToken)
        {
            _logger.LogInformation("历史数据归档服务启动");

            while (!stoppingToken.IsCancellationRequested)
            {
                try
                {
                    await ArchiveAsync();
                    await CleanupAsync();
                    await Task.Delay(_archiveInterval, stoppingToken);
                }
                catch (Exception ex)
                {
                    _logger.LogError(ex, "历史数据归档异常");
                    await Task.Delay(TimeSpan.FromSeconds(5), stoppingToken);
                }
            }

            _logger.LogInformation("历史数据归档服务停止");
        }

        private async Task ArchiveAsync()
        {
            using var scope = _scopeFactory.CreateScope();
            var deviceRepo = scope.ServiceProvider.GetRequiredService<IDeviceRepository>();
            var deviceDataRepo = scope.ServiceProvider.GetRequiredService<IDeviceDataRepository>();

            var allCacheData = _cache.GetAllData();
            var now = DateTime.UtcNow;
            var batch = new List<DeviceData>();

            foreach (var (deviceId, pointData) in allCacheData)
            {
                var device = await deviceRepo.GetByIdAsync(deviceId);
                if (device == null || device.Status == DeviceStatus.Offline)
                    continue;

                foreach (var (pointId, value) in pointData)
                {
                    batch.Add(new DeviceData
                    {
                        DeviceId = deviceId,
                        DataPointId = pointId,
                        Value = value,
                        Quality = 0,
                        RecordedAt = now,
                        CreatedAt = now
                    });
                }
            }

            if (batch.Count > 0)
            {
                await deviceDataRepo.BulkInsertAsync(batch);
                _logger.LogInformation("归档 {Count} 条历史数据", batch.Count);
            }
        }

        private async Task CleanupAsync()
        {
            using var scope = _scopeFactory.CreateScope();
            var deviceDataRepo = scope.ServiceProvider.GetRequiredService<IDeviceDataRepository>();
            var cutoff = DateTime.UtcNow.AddDays(-_retentionDays);
            var removed = await deviceDataRepo.CleanupOldDataAsync(cutoff);

            if (removed > 0)
                _logger.LogInformation("清理 {Count} 条超过 {Days} 天的历史数据", removed, _retentionDays);
        }
    }
}
```

> **与报警检测服务的区别**：`AlarmDetectionService` 每 1 秒检测并生成报警记录；`DataArchiveService` 每 5 秒批量写入所有测点的历史值，两者职责独立，互不影响。

#### 1c. Program.cs 注册

```csharp
// Server/Program.cs
builder.Services.AddScoped<IDeviceDataRepository, DeviceDataRepository>();
builder.Services.AddScoped<IHistoryService, HistoryService>();

builder.Services.AddHostedService<DataArchiveService>();
```

***

### 任务二：历史数据查询 API

#### 2a. DTOs

```csharp
// Server/Models/Dtos/HistoryDataPointDto.cs
namespace Server.Models.Dtos
{
    public class HistoryDataPointDto
    {
        public int DeviceId { get; set; }
        public string DeviceName { get; set; } = string.Empty;
        public int DataPointId { get; set; }
        public string DataPointName { get; set; } = string.Empty;
        public DateTime RecordedAt { get; set; }
        public double Value { get; set; }
    }
}
```

```csharp
// Server/Models/Dtos/HistoryStatisticsDto.cs
namespace Server.Models.Dtos
{
    public class HistoryStatisticsDto
    {
        public int DeviceId { get; set; }
        public string DeviceName { get; set; } = string.Empty;
        public int DataPointId { get; set; }
        public string DataPointName { get; set; } = string.Empty;
        public double Max { get; set; }
        public double Min { get; set; }
        public double Average { get; set; }
        public int Count { get; set; }
    }
}
```

#### 2b. IHistoryService.cs + HistoryService.cs

```csharp
// Server/Services/IHistoryService.cs
using Server.Models.Common;
using Server.Models.Dtos;

namespace Server.Services
{
    public interface IHistoryService
    {
        Task<ApiResult<List<HistoryDataPointDto>>> GetHistoryAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end, string? interval);

        Task<ApiResult<HistoryStatisticsDto>> GetStatisticsAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end);
    }
}
```

```csharp
// Server/Services/HistoryService.cs
using Server.Infrastructure.Repositories;
using Server.Models.Common;
using Server.Models.Dtos;
using Server.Models.Entities;

namespace Server.Services
{
    public class HistoryService : IHistoryService
    {
        private readonly IDeviceDataRepository _deviceDataRepo;
        private readonly IDeviceRepository _deviceRepo;
        private readonly IDataPointRepository _dataPointRepo;

        public HistoryService(
            IDeviceDataRepository deviceDataRepo,
            IDeviceRepository deviceRepo,
            IDataPointRepository dataPointRepo)
        {
            _deviceDataRepo = deviceDataRepo;
            _deviceRepo = deviceRepo;
            _dataPointRepo = dataPointRepo;
        }

        public async Task<ApiResult<List<HistoryDataPointDto>>> GetHistoryAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end, string? interval)
        {
            var device = await _deviceRepo.GetByIdAsync(deviceId);
            if (device == null)
                return ApiResult<List<HistoryDataPointDto>>.Fail(404, "设备不存在");

            var point = await _dataPointRepo.GetByIdAsync(dataPointId);
            if (point == null)
                return ApiResult<List<HistoryDataPointDto>>.Fail(404, "测点不存在");

            var raw = await _deviceDataRepo.GetHistoryAsync(deviceId, dataPointId, start, end);
            var aggregated = Aggregate(raw, interval);

            var dtos = aggregated.Select(d => new HistoryDataPointDto
            {
                DeviceId = deviceId,
                DeviceName = device.Name,
                DataPointId = dataPointId,
                DataPointName = point.Name,
                RecordedAt = d.RecordedAt,
                Value = d.Value
            }).ToList();

            return ApiResult<List<HistoryDataPointDto>>.Success(dtos);
        }

        public async Task<ApiResult<HistoryStatisticsDto>> GetStatisticsAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end)
        {
            var raw = await _deviceDataRepo.GetHistoryAsync(deviceId, dataPointId, start, end);
            if (raw.Count == 0)
                return ApiResult<HistoryStatisticsDto>.Fail(404, "该时间段无历史数据");

            var device = await _deviceRepo.GetByIdAsync(deviceId);
            var point = await _dataPointRepo.GetByIdAsync(dataPointId);

            return ApiResult<HistoryStatisticsDto>.Success(new HistoryStatisticsDto
            {
                DeviceId = deviceId,
                DeviceName = device?.Name ?? string.Empty,
                DataPointId = dataPointId,
                DataPointName = point?.Name ?? string.Empty,
                Max = raw.Max(d => d.Value),
                Min = raw.Min(d => d.Value),
                Average = raw.Average(d => d.Value),
                Count = raw.Count
            });
        }

        private List<DeviceData> Aggregate(List<DeviceData> raw, string? interval)
        {
            if (string.IsNullOrEmpty(interval)) return raw;

            var seconds = interval.ToLower() switch
            {
                "30s" => 30,
                "1m" => 60,
                "5m" => 300,
                "1h" => 3600,
                _ => 0
            };

            if (seconds == 0) return raw;

            var ticksPerBucket = TimeSpan.TicksPerSecond * seconds;
            return raw
                .GroupBy(d => d.RecordedAt.Ticks / ticksPerBucket)
                .Select(g => new DeviceData
                {
                    DeviceId = g.First().DeviceId,
                    DataPointId = g.First().DataPointId,
                    Value = g.Average(x => x.Value),
                    RecordedAt = new DateTime(g.Key * ticksPerBucket, DateTimeKind.Utc)
                })
                .OrderBy(d => d.RecordedAt)
                .ToList();
        }
    }
}
```

> **聚合策略说明**：按时间桶取平均值，桶大小由 `interval` 参数决定。这样可以在展示长时间范围时减少数据点数量，降低前端渲染压力。

#### 2c. HistoryController.cs

```csharp
// Server/Infrastructure/Controllers/HistoryController.cs
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Server.Services;

namespace Server.Infrastructure.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    [Authorize]
    public class HistoryController : ControllerBase
    {
        private readonly IHistoryService _historyService;

        public HistoryController(IHistoryService historyService)
        {
            _historyService = historyService;
        }

        [HttpGet("data")]
        public async Task<IActionResult> GetHistory(
            [FromQuery] int deviceId,
            [FromQuery] int pointId,
            [FromQuery] DateTime start,
            [FromQuery] DateTime end,
            [FromQuery] string? interval = null)
        {
            var result = await _historyService.GetHistoryAsync(deviceId, pointId, start, end, interval);
            return Ok(result);
        }

        [HttpGet("statistics")]
        public async Task<IActionResult> GetStatistics(
            [FromQuery] int deviceId,
            [FromQuery] int pointId,
            [FromQuery] DateTime start,
            [FromQuery] DateTime end)
        {
            var result = await _historyService.GetStatisticsAsync(deviceId, pointId, start, end);
            return result.Code == 200 ? Ok(result) : BadRequest(result);
        }
    }
}
```

> **URL 参数约定**：沿用项目现有风格，查询字符串使用小驼峰命名，相对路径不带前导斜杠（客户端调用时会自动拼接 `BaseAddress` 的尾斜杠）。

***

### 任务三：WPF 历史趋势页面

#### 3a. Client DTOs

```csharp
// Client/Models/Dtos/HistoryDataPointDto.cs
namespace Client.Models.Dtos
{
    public class HistoryDataPointDto
    {
        public int DeviceId { get; set; }
        public string DeviceName { get; set; } = string.Empty;
        public int DataPointId { get; set; }
        public string DataPointName { get; set; } = string.Empty;
        public DateTime RecordedAt { get; set; }
        public double Value { get; set; }
    }
}
```

```csharp
// Client/Models/Dtos/HistoryStatisticsDto.cs
namespace Client.Models.Dtos
{
    public class HistoryStatisticsDto
    {
        public double Max { get; set; }
        public double Min { get; set; }
        public double Average { get; set; }
        public int Count { get; set; }
    }
}
```

#### 3b. IHistoryService.cs + HistoryService.cs

```csharp
// Client/Service/IHistoryService.cs
using Client.Models.Dtos;

namespace Client.Service
{
    public interface IHistoryService
    {
        Task<List<HistoryDataPointDto>?> GetHistoryAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end, string? interval = null);

        Task<HistoryStatisticsDto?> GetStatisticsAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end);

        Task<byte[]?> ExportAsync(int deviceId, int dataPointId, DateTime start, DateTime end);
    }
}
```

```csharp
// Client/Service/HistoryService.cs
using Client.Infrastructure;
using Client.Models.Dtos;
using System.Net.Http.Json;
using System.Text.Json;

namespace Client.Service
{
    public class HistoryService : IHistoryService
    {
        private readonly HttpClientBase _http;
        private static readonly JsonSerializerOptions _jsonOptions = new() { PropertyNameCaseInsensitive = true };

        public HistoryService(HttpClientBase http)
        {
            _http = http;
        }

        public async Task<List<HistoryDataPointDto>?> GetHistoryAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end, string? interval = null)
        {
            var url = $"api/history/data?deviceId={deviceId}&pointId={dataPointId}&start={start:O}&end={end:O}";
            if (!string.IsNullOrEmpty(interval))
                url += $"&interval={interval}";

            var response = await _http.GetAsync(url);
            if (!response.IsSuccessStatusCode) return null;

            var result = await response.Content.ReadFromJsonAsync<ApiResult<List<HistoryDataPointDto>>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<HistoryStatisticsDto?> GetStatisticsAsync(
            int deviceId, int dataPointId, DateTime start, DateTime end)
        {
            var url = $"api/history/statistics?deviceId={deviceId}&pointId={dataPointId}&start={start:O}&end={end:O}";
            var response = await _http.GetAsync(url);
            if (!response.IsSuccessStatusCode) return null;

            var result = await response.Content.ReadFromJsonAsync<ApiResult<HistoryStatisticsDto>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<byte[]?> ExportAsync(int deviceId, int dataPointId, DateTime start, DateTime end)
        {
            var url = $"api/history/export?deviceId={deviceId}&pointId={dataPointId}&start={start:O}&end={end:O}";
            var response = await _http.GetAsync(url);
            if (!response.IsSuccessStatusCode) return null;

            return await response.Content.ReadAsByteArrayAsync();
        }
    }
}
```

> **时间格式**：使用 `DateTime.ToString("O")`（ISO 8601），服务端 `[FromQuery] DateTime` 可正确解析。

#### 3c. HistoryViewModel.cs

```csharp
// Client/ViewModels/HistoryViewModel.cs
using Client.Models;
using Client.Models.Dtos;
using Client.Service;
using LiveChartsCore;
using LiveChartsCore.SkiaSharpView;
using LiveChartsCore.SkiaSharpView.Painting;
using SkiaSharp;
using System.Collections.ObjectModel;

namespace Client.ViewModels
{
    public class HistoryViewModel : BindableBase, INavigationAware
    {
        private readonly IProductionLineService _lineService;
        private readonly IRealtimeService _realtimeService;
        private readonly IHistoryService _historyService;

        // 设备树与测点
        private ObservableCollection<ProductionLineTreeNode> _productionLineTreeNodes = new();
        private ObservableCollection<DataPointDisplayModel> _dataPoints = new();
        private DeviceTreeNode? _selectedDevice;
        private DataPointDisplayModel? _selectedPoint;

        // 查询条件
        private DateTime _startTime;
        private DateTime _endTime;
        private string _selectedInterval = "原始";
        private bool _isLoading;

        // 趋势图
        private ISeries[] _trendSeries = Array.Empty<ISeries>();
        private Axis[] _trendXAxes;
        private Axis[] _trendYAxes;

        // 统计
        private HistoryStatisticsDto _statistics = new();

        public string[] Intervals { get; } = { "原始", "30s", "1m", "5m", "1h" };

        public DelegateCommand QueryCommand { get; }
        public DelegateCommand ExportCommand { get; }
        public DelegateCommand<object?> SelectedItemChangedCommand { get; }

        public HistoryViewModel(
            IProductionLineService lineService,
            IRealtimeService realtimeService,
            IHistoryService historyService)
        {
            _lineService = lineService;
            _realtimeService = realtimeService;
            _historyService = historyService;

            _endTime = DateTime.Now;
            _startTime = _endTime.AddHours(-1);

            SelectedItemChangedCommand = new DelegateCommand<object?>(OnSelectedItemChanged);
            QueryCommand = new DelegateCommand(async () => await QueryAsync(), CanQuery);
            ExportCommand = new DelegateCommand(async () => await ExportAsync(), CanExport);

            // 默认坐标轴配置
            TrendXAxes = new Axis[]
            {
                new Axis { Name = "时间", NameTextSize = 12 }
            };
            TrendYAxes = new Axis[]
            {
                new Axis { Name = "数值", NameTextSize = 12 }
            };
        }

        /// <summary>加载产线 + 所有在线设备，构建设备树</summary>
        private async Task LoadTreeAsync()
        {
            var linesTask = _lineService.GetAllAsync();
            var allDataTask = _realtimeService.GetAllDataAsync();
            await Task.WhenAll(linesTask, allDataTask);

            var lines = await linesTask;
            var allData = await allDataTask;

            if (lines == null || allData == null) return;

            ProductionLineTreeNodes = new ObservableCollection<ProductionLineTreeNode>(
                lines.Select(line => new ProductionLineTreeNode
                {
                    Id = line.Id,
                    Name = line.Name,
                    Devices = allData.Devices
                        .Where(d => d.ProductLineId == line.Id)
                        .Select(d => new DeviceTreeNode
                        {
                            Id = d.DeviceId,
                            Name = d.DeviceName,
                            Status = d.Status
                        }).ToList()
                }));
        }

        /// <summary>TreeView 选中设备后，加载该设备的测点列表</summary>
        private async void OnSelectedItemChanged(object? selectedItem)
        {
            if (selectedItem is not DeviceTreeNode device) return;

            _selectedDevice = device;
            SelectedPoint = null;

            var deviceData = await _realtimeService.GetDeviceDataAsync(device.Id);
            if (deviceData == null) return;

            DataPoints.Clear();
            foreach (var p in deviceData.DataPoints)
            {
                DataPoints.Add(new DataPointDisplayModel
                {
                    DataPointId = p.DataPointId,
                    Name = p.Name,
                    Unit = p.Unit,
                    AlarmHigh = p.AlarmHigh,
                    AlarmLow = p.AlarmLow
                });
            }

            QueryCommand.RaiseCanExecuteChanged();
            ExportCommand.RaiseCanExecuteChanged();
        }

        /// <summary>查询历史数据并刷新趋势图与统计面板</summary>
        private async Task QueryAsync()
        {
            if (_selectedDevice == null || _selectedPoint == null) return;

            IsLoading = true;
            QueryCommand.RaiseCanExecuteChanged();
            ExportCommand.RaiseCanExecuteChanged();

            var interval = SelectedInterval == "原始" ? null : SelectedInterval;

            var dataTask = _historyService.GetHistoryAsync(
                _selectedDevice.Id, _selectedPoint.DataPointId, _startTime, _endTime, interval);
            var statsTask = _historyService.GetStatisticsAsync(
                _selectedDevice.Id, _selectedPoint.DataPointId, _startTime, _endTime);

            await Task.WhenAll(dataTask, statsTask);

            var data = await dataTask;
            var stats = await statsTask;

            if (data != null && data.Count > 0)
            {
                var values = new ObservableCollection<DateTimePoint>(
                    data.Select(d => new DateTimePoint(d.RecordedAt, d.Value)));

                TrendSeries = new ISeries[]
                {
                    new LineSeries<DateTimePoint>
                    {
                        Values = values,
                        Stroke = new SolidColorPaint(SKColors.DodgerBlue) { StrokeThickness = 2 },
                        Fill = null,
                        GeometryFill = null,
                        GeometryStroke = null
                    }
                };

                TrendXAxes = new Axis[]
                {
                    new Axis
                    {
                        Name = "时间",
                        Labeler = value => new DateTime((long)value).ToString("HH:mm:ss"),
                        NameTextSize = 12
                    }
                };

                TrendYAxes = new Axis[]
                {
                    new Axis
                    {
                        Name = _selectedPoint.Unit ?? "数值",
                        NameTextSize = 12
                    }
                };
            }

            if (stats != null) Statistics = stats;

            IsLoading = false;
            QueryCommand.RaiseCanExecuteChanged();
            ExportCommand.RaiseCanExecuteChanged();
        }

        private bool CanQuery() => _selectedDevice != null && _selectedPoint != null && !IsLoading;

        /// <summary>调用导出 API，弹出保存对话框写入文件</summary>
        private async Task ExportAsync()
        {
            if (_selectedDevice == null || _selectedPoint == null) return;

            var bytes = await _historyService.ExportAsync(
                _selectedDevice.Id, _selectedPoint.DataPointId, _startTime, _endTime);
            if (bytes == null) return;

            var dialog = new Microsoft.Win32.SaveFileDialog
            {
                FileName = $"history_{_selectedDevice.Name}_{_selectedPoint.Name}.xlsx",
                Filter = "Excel files (*.xlsx)|*.xlsx"
            };

            if (dialog.ShowDialog() == true)
            {
                await System.IO.File.WriteAllBytesAsync(dialog.FileName, bytes);
            }
        }

        private bool CanExport() => _selectedDevice != null && _selectedPoint != null && !IsLoading;

        // ====== INavigationAware ======
        public bool IsNavigationTarget(NavigationContext navigationContext) => true;
        public void OnNavigatedFrom(NavigationContext navigationContext) { }
        public void OnNavigatedTo(NavigationContext navigationContext)
        {
            if (ProductionLineTreeNodes.Count == 0)
                _ = LoadTreeAsync();
        }

        // ====== 属性 ======
        public ObservableCollection<ProductionLineTreeNode> ProductionLineTreeNodes
        {
            get => _productionLineTreeNodes;
            set => SetProperty(ref _productionLineTreeNodes, value);
        }

        public ObservableCollection<DataPointDisplayModel> DataPoints
        {
            get => _dataPoints;
            set => SetProperty(ref _dataPoints, value);
        }

        public DataPointDisplayModel? SelectedPoint
        {
            get => _selectedPoint;
            set
            {
                if (SetProperty(ref _selectedPoint, value))
                {
                    QueryCommand.RaiseCanExecuteChanged();
                    ExportCommand.RaiseCanExecuteChanged();
                }
            }
        }

        public DateTime StartTime
        {
            get => _startTime;
            set => SetProperty(ref _startTime, value);
        }

        public DateTime EndTime
        {
            get => _endTime;
            set => SetProperty(ref _endTime, value);
        }

        public string SelectedInterval
        {
            get => _selectedInterval;
            set => SetProperty(ref _selectedInterval, value);
        }

        public bool IsLoading
        {
            get => _isLoading;
            set => SetProperty(ref _isLoading, value);
        }

        public HistoryStatisticsDto Statistics
        {
            get => _statistics;
            set => SetProperty(ref _statistics, value);
        }

        public ISeries[] TrendSeries
        {
            get => _trendSeries;
            set => SetProperty(ref _trendSeries, value);
        }

        public Axis[] TrendXAxes
        {
            get => _trendXAxes;
            set => SetProperty(ref _trendXAxes, value);
        }

        public Axis[] TrendYAxes
        {
            get => _trendYAxes;
            set => SetProperty(ref _trendYAxes, value);
        }
    }
}
```

> **趋势图数据源**：使用 `DateTimePoint`（X 为时间、Y 为数值），配合 `Axis.Labeler` 将 X 坐标（Ticks）格式化为 `HH:mm:ss`。相比 `LineSeries<double>` + `Axis.Labels`，时间轴缩放与平移更自然。

#### 3d. HistoryView\.xaml

```xml
<!-- Client/Views/HistoryView.xaml -->
<UserControl x:Class="Client.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Client.Models"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：设备树 + 测点列表 -->
        <DockPanel Grid.Column="0" Margin="5">
            <TextBlock Text="设备导航" DockPanel.Dock="Top"
                       FontSize="15" FontWeight="Bold" Margin="5"/>

            <TreeView DockPanel.Dock="Top" Height="220"
                      ItemsSource="{Binding ProductionLineTreeNodes}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectedItemChanged">
                        <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}"
                                               CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:ProductionLineTreeNode}"
                                              ItemsSource="{Binding Devices}">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                    </HierarchicalDataTemplate>

                    <DataTemplate DataType="{x:Type local:DeviceTreeNode}">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Height="10" Width="10"
                                     Fill="{Binding StatusColor}"
                                     VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>

            <TextBlock Text="测点列表" FontSize="14" FontWeight="Bold" Margin="5,10,5,5"/>
            <ListBox ItemsSource="{Binding DataPoints}"
                     SelectedItem="{Binding SelectedPoint}"
                     DisplayMemberPath="Name"/>
        </DockPanel>

        <!-- 右侧：查询条件 + 统计 + 趋势图 -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!-- 查询条件 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="开始：" VerticalAlignment="Center"/>
                <hc:DateTimePicker SelectedDateTime="{Binding StartTime}"
                                   Width="170" Margin="0,0,10,0"/>

                <TextBlock Text="结束：" VerticalAlignment="Center"/>
                <hc:DateTimePicker SelectedDateTime="{Binding EndTime}"
                                   Width="170" Margin="0,0,10,0"/>

                <TextBlock Text="间隔：" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding Intervals}"
                          SelectedItem="{Binding SelectedInterval}"
                          Width="80" Margin="0,0,10,0"/>

                <Button Content="查询" Command="{Binding QueryCommand}"
                        Width="70" Margin="0,0,10,0"/>
                <Button Content="导出 Excel" Command="{Binding ExportCommand}"
                        Width="90"/>
            </StackPanel>

            <!-- 统计面板 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="{Binding Statistics.Max, StringFormat='最大：{0:F2}'}"
                           Margin="0,0,20,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding Statistics.Min, StringFormat='最小：{0:F2}'}"
                           Margin="0,0,20,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding Statistics.Average, StringFormat='平均：{0:F2}'}"
                           Margin="0,0,20,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding Statistics.Count, StringFormat='点数：{0}'}"
                           FontWeight="Bold"/>
            </StackPanel>

            <!-- 趋势图 -->
            <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1">
                <lvc:CartesianChart Series="{Binding TrendSeries}"
                                    XAxes="{Binding TrendXAxes}"
                                    YAxes="{Binding TrendYAxes}"/>
            </Border>

            <!-- 加载提示 -->
            <TextBlock Grid.Row="3" Text="加载中..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </Grid>
    </Grid>
</UserControl>
```

> **HandyControl 替代方案**：如果 `hc:DateTimePicker` 在项目里未引入对应资源字典，可改用标准 `DatePicker` + `TextBox` 分别绑定日期和时间部分。

#### 3e. App.xaml.cs 与 ShellViewModel 注册

```csharp
// Client/App.xaml.cs
containerRegistry.RegisterSingleton<IHistoryService, HistoryService>();
containerRegistry.RegisterForNavigation<HistoryView, HistoryViewModel>();
```

```csharp
// Client/ViewModels/ShellViewModel.cs
MenuItems = new ObservableCollection<MenuItemModel>
{
    new MenuItemModel { Header = "实时监控", ViewName = "RealtimeView" },
    new MenuItemModel { Header = "历史趋势", ViewName = "HistoryView" },
    new MenuItemModel { Header = "设备管理", ViewName = "DeviceListView" },
    new MenuItemModel { Header = "报警中心", ViewName = "AlarmView" },
    new MenuItemModel { Header = "系统设置", ViewName = "SettingsView" }
};
```

***

### 任务四：Excel 导出

#### 4a. Server 端导出接口

> 需先安装 EPPlus：`dotnet add package EPPlus`。

```csharp
// 在 HistoryController.cs 中追加
[HttpGet("export")]
public async Task<IActionResult> Export(
    [FromQuery] int deviceId,
    [FromQuery] int pointId,
    [FromQuery] DateTime start,
    [FromQuery] DateTime end)
{
    var result = await _historyService.GetHistoryAsync(deviceId, pointId, start, end, null);
    if (result.Code != 200 || result.Data == null)
        return BadRequest(result);

    using var package = new OfficeOpenXml.ExcelPackage();
    var worksheet = package.Workbook.Worksheets.Add("历史数据");

    worksheet.Cells[1, 1].Value = "设备";
    worksheet.Cells[1, 2].Value = "测点";
    worksheet.Cells[1, 3].Value = "时间";
    worksheet.Cells[1, 4].Value = "数值";

    int row = 2;
    foreach (var item in result.Data)
    {
        worksheet.Cells[row, 1].Value = item.DeviceName;
        worksheet.Cells[row, 2].Value = item.DataPointName;
        worksheet.Cells[row, 3].Value = item.RecordedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm:ss");
        worksheet.Cells[row, 4].Value = item.Value;
        row++;
    }

    worksheet.Cells.AutoFitColumns();

    var bytes = package.GetAsByteArray();
    return File(bytes,
        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        $"history_{deviceId}_{pointId}.xlsx");
}
```

> **EPPlus 许可证**：在 Program.cs 入口或模块初始化处设置 `ExcelPackage.LicenseContext = LicenseContext.NonCommercial;`，否则运行时会抛出授权异常。

#### 4b. Client 端下载保存

客户端导出逻辑已写在 `HistoryViewModel.ExportAsync()` 中，核心流程：

1. 调用 `IHistoryService.ExportAsync(...)` 获取 `byte[]`。
2. 弹出 `SaveFileDialog`，默认文件名包含设备名和测点名。
3. 用户确认后使用 `File.WriteAllBytesAsync` 写入磁盘。

***

### Day 7 DI 注册汇总

| 生命周期          | 注册项                                              | 说明                |
| ------------- | ------------------------------------------------ | ----------------- |
| Scoped        | `IDeviceDataRepository` / `DeviceDataRepository` | 历史数据仓储            |
| Scoped        | `IHistoryService` / `HistoryService`（Server）     | 历史数据业务服务          |
| HostedService | `DataArchiveService`                             | 历史数据归档后台服务（每 5 秒） |
| Singleton     | `IHistoryService` / `HistoryService`（Client）     | 客户端 HTTP 调用       |
| ForNavigation | `HistoryView` / `HistoryViewModel`               | Prism 导航注册        |

***

## Day 8 — 系统设置 + 完善优化

**目标**：实现系统设置页面（Modbus 配置 / 数据保留 / 日志查看 / 关于），新增系统信息 API 与 Serilog 文件日志，补充全局异常处理、参数校验、操作日志等完善优化。

**实现前提**：

- 已按 Day 7 完成历史数据归档与历史趋势页面。
- `ModbusService` / `ModbusPortManager` / `DataArchiveService` 均为单例后台服务，已有 `IModbusService` 接口。
- 客户端 `SettingsView` / `SettingsViewModel` 目前是空壳，菜单入口已存在（`ShellViewModel`）。
- `Infrastructure/Middleware/` 空目录已预留（`Server.csproj` 已包含）。

**技术决策（已确认）**：

| 决策点  | 结论                                                        |
| ---- | --------------------------------------------------------- |
| 配置存储 | 独立 JSON 文件 `setting.json`（免建表、免迁移，运行时可改），Modbus / 保留策略热生效 |
| 日志方案 | Serilog 写文件（滚动日志），`GET /api/system/logs` 读文件              |
| 操作日志 | 不建表，关键操作处写普通 `ILogger` 日志                                 |
| 参数校验 | `[ApiController]` 内置 + DataAnnotations，零第三方依赖             |

**目录规划**：

```
Server/
├── setting.json                            （新增：系统配置文件，运行时可改）
├── Infrastructure/Repositories/
│   ├── IAlarmRepository.cs               （改造：新增 CleanupOldAlarmsAsync）
│   └── AlarmRepository.cs                （改造）
├── Infrastructure/Middleware/
│   └── ExceptionHandlingMiddleware.cs    （新增）
├── Infrastructure/Modbus/
│   ├── IModbusService.cs                 （改造：新增 IsRunning / LastPollTime）
│   ├── ModbusPortManager.cs              （改造：串口参数改读 setting.json）
│   └── ModbusService.cs                  （改造：采集周期从配置读 + 状态字段）
├── Infrastructure/Controllers/
│   └── SystemController.cs               （新增）
├── Models/Dtos/
│   ├── SystemInfoDto.cs                  （新增）
│   ├── SystemLogsDto.cs                  （新增）
│   └── SystemSettingsDto.cs              （新增）
├── Services/
│   ├── ISystemConfigService.cs           （新增）
│   ├── SystemConfigService.cs            （新增：单例 + 内存缓存 + JSON 文件读写）
│   ├── ISystemService.cs                 （新增）
│   ├── SystemService.cs                  （新增）
│   ├── DataArchiveService.cs             （改造：保留天数从配置读 + 清理报警）
│   └── AuthService.cs / DeviceService.cs 等（改造：关键操作日志）
└── Program.cs                            （改造：Serilog + 异常中间件 + 注册）

Client/
├── Models/Dtos/
│   ├── SystemInfoDto.cs                  （新增）
│   └── SystemSettingsDto.cs              （新增）
├── Service/
│   ├── ISystemService.cs                 （新增）
│   └── SystemService.cs                  （新增）
├── ViewModels/
│   └── SettingsViewModel.cs              （完整实现）
├── Views/
│   └── SettingsView.xaml                 （TabControl 四块）
└── App.xaml.cs                           （改造：注册 ISystemService）
```

***

### 任务一：setting.json 配置文件与配置服务

#### 1a. setting.json 配置文件

系统配置**不入数据库**，统一放独立 JSON 文件 `Server/setting.json`。嵌套结构与点分键一一对应（`Modbus.BaudRate` ↔ `Modbus` 节点下的 `BaudRate`），人可直接编辑。**文件需手工创建**（直接照下面内容抄一份），`LoadAsync()` 发现文件缺失会直接抛异常提示，不做自动生成：

```json
// Server/setting.json
{
  "Modbus": {
    "PortMappings": {
      "COM1": "COM2",
      "COM3": "COM4"
    },
    "BaudRate": 9600,
    "DataBits": 8,
    "StopBits": "One",
    "Parity": "None",
    "ReadTimeout": 500,
    "WriteTimeout": 500,
    "PollIntervalMs": 1000
  },
  "DataRetention": {
    "DeviceDataDays": 30,
    "AlarmDays": 30
  }
}
```

**配置键清单**（内存中统一用点分键访问）：

| 键                              | 示例值                             | 说明                                    |
| ------------------------------ | ------------------------------- | ------------------------------------- |
| `Modbus.PortMappings`          | `{"COM1":"COM2","COM3":"COM4"}` | 端口映射（从站→主站），JSON 存储                   |
| `Modbus.BaudRate`              | `9600`                          | 波特率                                   |
| `Modbus.DataBits`              | `8`                             | 数据位                                   |
| `Modbus.StopBits`              | `One`                           | 停止位（字符串，Enum.Parse 解析）                |
| `Modbus.Parity`                | `None`                          | 校验位（字符串，Enum.Parse 解析）                |
| `Modbus.ReadTimeout`           | `500`                           | 读超时（ms）                               |
| `Modbus.WriteTimeout`          | `500`                           | 写超时（ms）                               |
| `Modbus.PollIntervalMs`        | `1000`                          | 采集周期（ms），替换 ModbusService 硬编码 1000    |
| `DataRetention.DeviceDataDays` | `30`                            | 历史数据保留天数，替换 DataArchiveService 硬编码 30 |
| `DataRetention.AlarmDays`      | `30`                            | 报警保留天数（新增清理逻辑）                        |

`Server.csproj` 中让该文件随构建复制（发布场景也需要）：

```xml
<ItemGroup>
  <None Update="setting.json">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
</ItemGroup>
```

> 文件路径用 `IWebHostEnvironment.ContentRootPath` 定位（开发时=项目根目录，发布后=应用根目录），不依赖工作目录，不会因为启动位置不同而读错文件。

#### 1b. ISystemConfigService 接口

接口签名与数据库版完全一致（调用方无感知），只是实现从"查库"换成"读文件"：

```csharp
// Server/Services/ISystemConfigService.cs
using Server.Models.Dtos;

namespace Server.Services
{
    public interface ISystemConfigService
    {
        /// <summary>启动时调用一次：把 setting.json 全量配置加载进内存缓存</summary>
        Task LoadAsync();

        /// <summary>从内存缓存读配置（线程安全），不存在返回 null</summary>
        string? GetValueStr(string key);

        /// <summary>读取 int 配置，解析失败返回默认值</summary>
        int GetValueInt(string key, int defaultValue);

        /// <summary>保存（新增或更新）单个配置：改内存树 + 写回文件 + 同步缓存</summary>
        Task AddOrUpdateAsync(string key, string value, string? description = null);

        /// <summary>批量保存配置（只写一次文件）</summary>
        Task AddOrUpdateAllAsync(List<SystemSettingsDto> settings);
    }
}
```

#### 1c. SystemConfigService（单例 + 内存缓存 + JSON 文件读写）

后台服务（`ModbusPortManager` 等）都是单例，`SystemConfigService` 也注册为**单例**。实现思路：启动时读 `setting.json` 解析成 `JsonObject`（内存树）并**扁平化**成点分键缓存到 `ConcurrentDictionary`；读取走缓存；保存 = 修改内存树 → 整体写回文件 → 同步缓存。

```csharp
// Server/Services/SystemConfigService.cs
using Server.Models.Dtos;
using System.Collections.Concurrent;
using System.Diagnostics.CodeAnalysis;
using System.Text.Json;
using System.Text.Json.Nodes;

namespace Server.Services
{
    /// <summary>setting.json 配置服务：读文件 → 扁平化进内存缓存；保存 = 改内存树 + 写回文件</summary>
    public class SystemConfigService : ISystemConfigService
    {
        private static readonly JsonSerializerOptions _writeOptions = new() { WriteIndented = true };

        private readonly string _filePath;
        private readonly ILogger<SystemConfigService> _logger;
        private readonly ConcurrentDictionary<string, string> _cache = new();
        private JsonObject? _root;   // setting.json 的原始嵌套结构（保存时整体写回）

        public SystemConfigService(IWebHostEnvironment env, ILogger<SystemConfigService> logger)
        {
            _filePath = Path.Combine(env.ContentRootPath, "setting.json");
            _logger = logger;
        }

        public async Task LoadAsync()
        {
            if (!File.Exists(_filePath))
                throw new FileNotFoundException(
                    "缺少配置文件 setting.json，请按开发日志 Day 8 任务一 1a 的内容创建", _filePath);

            var json = await File.ReadAllTextAsync(_filePath);
            _root = JsonNode.Parse(json)?.AsObject() ?? new JsonObject();

            // 扁平化：Modbus.BaudRate → "9600"，对象节点 → JSON 字符串
            _cache.Clear();
            Flatten(_root, prefix: string.Empty);

            _logger.LogInformation("系统配置加载完成（setting.json），共 {Count} 项", _cache.Count);
        }

        public string? GetValueStr(string key)
            => _cache.TryGetValue(key, out var value) ? value : null;

        public int GetValueInt(string key, int defaultValue)
            => int.TryParse(GetValueStr(key), out var value) ? value : defaultValue;

        public async Task AddOrUpdateAsync(string key, string value, string? description = null)
        {
            // description 不落盘（JSON 文件不存描述），参数保留只为兼容接口
            _root ??= new JsonObject();
            SetValue(_root, key, value);

            await File.WriteAllTextAsync(_filePath, _root.ToJsonString(_writeOptions));
            _cache[key] = value;   // 先落盘再改缓存
        }

        public async Task AddOrUpdateAllAsync(List<SystemSettingsDto> settings)
        {
            _root ??= new JsonObject();
            foreach (var setting in settings)
                SetValue(_root, setting.Key, setting.Value);

            // 批量只写一次文件，避免循环写盘
            await File.WriteAllTextAsync(_filePath, _root.ToJsonString(_writeOptions));

            foreach (var setting in settings)
                _cache[setting.Key] = setting.Value;
        }

        // ===== 私有工具方法 =====

        /// <summary>按点分路径写入节点："Modbus.BaudRate"=19200 → root["Modbus"]["BaudRate"]=19200。
        /// 整数字符串存数字节点；合法 JSON 对象/数组字符串（如 PortMappings）存为节点；其余存字符串</summary>
        private static void SetValue(JsonObject root, string key, string value)
        {
            var parts = key.Split('.');
            var current = root;
            for (int i = 0; i < parts.Length - 1; i++)
            {
                if (current[parts[i]] is not JsonObject child)
                {
                    child = new JsonObject();
                    current[parts[i]] = child;
                }
                current = child;
            }

            JsonNode node = int.TryParse(value, out var n)
                ? JsonValue.Create(n)!
                : TryParseJson(value, out var parsed) ? parsed!
                : JsonValue.Create(value)!;
            current[parts[^1]] = node;
        }

        /// <summary>尝试把字符串解析为 JSON 对象/数组。返回 true 时 out 参数必不为 null（供编译器推导）</summary>
        private static bool TryParseJson(string value, [NotNullWhen(true)] out JsonNode? node)
        {
            try
            {
                node = JsonNode.Parse(value);
                return node is JsonObject or JsonArray;
            }
            catch (JsonException)
            {
                node = null;
                return false;
            }
        }

        /// <summary>递归扁平化：嵌套对象下钻，基元节点转字符串存入缓存</summary>
        private void Flatten(JsonObject obj, string prefix)
        {
            foreach (var (name, node) in obj)
            {
                var key = prefix.Length == 0 ? name : $"{prefix}.{name}";
                switch (node)
                {
                    case JsonObject inner:
                        Flatten(inner, key);
                        break;
                    case JsonValue v when v.TryGetValue<string>(out var s):
                        _cache[key] = s;                 // 字符串节点
                        break;
                    default:
                        _cache[key] = node?.ToString() ?? string.Empty;   // 数字/布尔
                        break;
                }
            }
        }
    }
}
```

> **三个关键点**：
>
> 1. **读取不碰文件**：启动 `LoadAsync()` 后全部走 `ConcurrentDictionary`，后台服务高频读配置零 IO；
> 2. **扁平化约定**：嵌套 JSON（`Modbus` → `BaudRate`）与点分键（`"Modbus.BaudRate"`）双向映射，`SetValue` 写回时保持文件结构可读（带缩进），`Flatten` 加载时展开；
> 3. **保存原子顺序**：先写文件成功、再更新缓存，避免"缓存已改但文件写失败"的不一致。

> **对比数据库方案的收益**：不需要实体 / 仓储 / DbSet / EF 迁移（少 4 个文件、1 次迁移），不需要 `IServiceScopeFactory`，配置改动直接反映在可见的 setting.json 文件里，备份 = 复制文件。适合单机部署的工业场景。

***

### 任务二：Modbus 模块与归档服务改造

#### 2a. ModbusPortManager 改读 setting.json

原实现从 `appsettings.Development.json` 的 `Modbus:DefaultSerialPort` / `Modbus:PortMappings` 读配置，改为**只读 setting.json**（配置单一来源，不再回退 appsettings；键缺失时用代码内常量兜底）。`IConfiguration _config` 字段随之删除。

```csharp
// Server/Infrastructure/Modbus/ModbusPortManager.cs（改造后关键部分）
public class ModbusPortManager : IDisposable
{
    private readonly ILogger<ModbusPortManager> _logger;
    private readonly ISystemConfigService _configService;
    private readonly Dictionary<string, SerialPort> _portPool = new();

    public ModbusPortManager(ILogger<ModbusPortManager> logger, ISystemConfigService configService)
    {
        _logger = logger;
        _configService = configService;
    }

    /// <summary>读取串口配置（全部来自 setting.json，键缺失时用常量兜底）</summary>
    private (int baudRate, int dataBits, StopBits stopBits, Parity parity, int readTimeout, int writeTimeout)
        GetPortSettings()
        => (
            _configService.GetValueInt("Modbus.BaudRate", 9600),
            _configService.GetValueInt("Modbus.DataBits", 8),
            Enum.Parse<StopBits>(_configService.GetValueStr("Modbus.StopBits") ?? "One"),
            Enum.Parse<Parity>(_configService.GetValueStr("Modbus.Parity") ?? "None"),
            _configService.GetValueInt("Modbus.ReadTimeout", 500),
            _configService.GetValueInt("Modbus.WriteTimeout", 500)
        );

    /// <summary>构建端口映射（来自 setting.json）</summary>
    private Dictionary<string, string> BuildComMapping()
    {
        var json = _configService.GetValueStr("Modbus.PortMappings");
        var mappings = string.IsNullOrEmpty(json)
            ? new Dictionary<string, string>()
            : System.Text.Json.JsonSerializer.Deserialize<Dictionary<string, string>>(json)
              ?? new Dictionary<string, string>();

        // 反转后：{ "COM2": "COM1" }，方便根据从站端口找主站端口
        return mappings.ToDictionary(m => m.Value, m => m.Key);
    }
    // 其余方法（InitializePorts / GetPort / Dispose）不变
}
```

> 配置来源统一为 setting.json 一处；`GetValueInt`/`GetValueStr` 的第二参数（兜底默认值）与 setting.json 初始内容一致，即使手工编辑时删错了某个键，服务也能以默认参数启动。appsettings 里的 `Modbus:DefaultSerialPort` / `Modbus:PortMappings` 节点可以删除（留着也不读）。

#### 2b. ModbusService：采集周期可配置 + 运行状态

`IModbusService` 接口扩展两个状态属性，供 `GET /api/system/info` 使用：

```csharp
// Server/Infrastructure/Modbus/IModbusService.cs（改造后）
public interface IModbusService
{
    Dictionary<int, Dictionary<int, double>> GetCurrentData();

    /// <summary>手动触发重新加载设备配置</summary>
    Task ReloadConfigsAsync();

    /// <summary>Modbus 采集是否在运行</summary>
    bool IsRunning { get; }

    /// <summary>最近一次轮询完成时间（UTC）</summary>
    DateTime? LastPollTime { get; }
}
```

`ModbusService.cs` 改造点：

```csharp
public class ModbusService : BackgroundService, IModbusService
{
    private readonly ILogger<ModbusService> _logger;
    private readonly IConfiguration _config;
    private readonly ISystemConfigService _configService;
    private readonly ModbusConfigLoader _configLoader;
    private readonly ModbusPortManager _portManager;
    private readonly ModbusDataReader _dataReader;
    private readonly ModbusDataCache _cache;

    // ===== 新增状态属性 =====
    public bool IsRunning { get; private set; }
    public DateTime? LastPollTime { get; private set; }

    public ModbusService(
        ILogger<ModbusService> logger,
        IConfiguration config,
        ISystemConfigService configService,
        ModbusConfigLoader configLoader,
        ModbusPortManager portManager,
        ModbusDataReader dataReader,
        ModbusDataCache cache)
    {
        // ...赋值...
    }

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        _logger.LogInformation("Modbus 采集服务启动中...");

        // 1.加载系统配置（确保串口参数/采集周期已进内存缓存）
        await _configService.LoadAsync();

        // 2.加载设备配置 + 初始化端口池（逻辑不变）...

        IsRunning = true;
        try
        {
            while (!stoppingToken.IsCancellationRequested)
            {
                try
                {
                    await PollAllDevicesAsync(stoppingToken);
                    LastPollTime = DateTime.UtcNow;   // 每轮轮询完成更新一次
                }
                catch (OperationCanceledException)
                {
                    break;
                }
                catch (Exception ex)
                {
                    _logger.LogError(ex, "Modbus 轮询异常");
                }

                // 3.采集周期改为从配置读取，运行时改配置立即生效
                var pollIntervalMs = _configService.GetValueInt("Modbus.PollIntervalMs", 1000);
                await Task.Delay(pollIntervalMs, stoppingToken);
            }
        }
        finally
        {
            IsRunning = false;
            _logger.LogInformation("Modbus 轮询循环已退出");
        }
    }
    // ReloadConfigsAsync 逻辑不变（Dispose 端口 → 重新 LoadConfigs → 重新 InitializePorts）
}
```

> `IsRunning` 用 `try/finally` 包裹，保证异常或取消时状态一定被复位；`LastPollTime` 每轮更新，前端可据此判断采集是否"卡死"。

#### 2c. DataArchiveService：保留天数可配置 + 报警清理

原 `_retentionDays = 30` 是硬编码，且只清理历史数据。改造后两个保留天数都从配置读，并在同一轮里清理报警表：

```csharp
// Server/Services/DataArchiveService.cs（改造后关键部分）
public class DataArchiveService(
    IServiceScopeFactory _scopeFactory,
    ModbusDataCache _cache,
    ISystemConfigService _configService,
    ILogger<DataArchiveService> _logger
    ) : BackgroundService
{
    private readonly TimeSpan _archiveInterval = TimeSpan.FromSeconds(5);

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        _logger.LogInformation("历史数据归档服务启动");
        await _configService.LoadAsync();   // 启动时加载配置
        while (!stoppingToken.IsCancellationRequested)
        {
            try
            {
                await ArchiveAsync();
                await CleanupAsync();
                await Task.Delay(_archiveInterval, stoppingToken);
            }
            catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested)
            {
                break;
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "历史数据归档异常");
                await Task.Delay(TimeSpan.FromSeconds(5), stoppingToken);
            }
        }
        _logger.LogInformation("历史数据归档服务停止");
    }

    // ArchiveAsync 不变...

    private async Task CleanupAsync()
    {
        using var scope = _scopeFactory.CreateScope();
        var deviceDataRepo = scope.ServiceProvider.GetRequiredService<IDeviceDataRepository>();
        var alarmRepo = scope.ServiceProvider.GetRequiredService<IAlarmRepository>();

        // 保留天数每次清理时现读，运行时改配置立即生效
        var deviceDataDays = _configService.GetValueInt("DataRetention.DeviceDataDays", 30);
        var alarmDays = _configService.GetValueInt("DataRetention.AlarmDays", 30);

        var removedData = await deviceDataRepo.CleanupOldDataAsync(DateTime.UtcNow.AddDays(-deviceDataDays));
        if (removedData > 0)
            _logger.LogInformation("清理 {Count} 条超过 {Days} 天的历史数据", removedData, deviceDataDays);

        var removedAlarms = await alarmRepo.CleanupOldAlarmsAsync(DateTime.UtcNow.AddDays(-alarmDays));
        if (removedAlarms > 0)
            _logger.LogInformation("清理 {Count} 条超过 {Days} 天的报警记录", removedAlarms, alarmDays);
    }
}
```

`IAlarmRepository` / `AlarmRepository` 新增方法：

```csharp
// IAlarmRepository 新增
Task<int> CleanupOldAlarmsAsync(DateTime cutoff);

// AlarmRepository 实现
public async Task<int> CleanupOldAlarmsAsync(DateTime cutoff)
{
    var oldAlarms = await _dbSet.Where(a => a.OccurredAt < cutoff).ToListAsync();
    if (oldAlarms.Count == 0) return 0;

    _dbSet.RemoveRange(oldAlarms);
    await _context.SaveChangesAsync();
    return oldAlarms.Count;
}
```

> 清理条件用 `OccurredAt`（报警发生时间）而不是 `CreatedAt`，语义与历史数据用 `RecordedAt` 一致——都是"业务时间"。

***

### 任务三：Serilog 文件日志

#### 3a. 引入包

`Server.csproj` 新增：

```xml
<PackageReference Include="Serilog.AspNetCore" Version="8.0.3" />
<PackageReference Include="Serilog.Sinks.File" Version="6.0.0" />
```

#### 3b. Program.cs 配置 Serilog

```csharp
// Server/Program.cs（改造后关键部分）
using Serilog;

Log.Logger = new LoggerConfiguration()
    .MinimumLevel.Information()                          // 只记录 Information 及以上
    .WriteTo.Console()                                   // 控制台输出（开发调试）
    .WriteTo.File("logs/log-.txt",                       // 写到 logs 目录，文件按天滚动
        rollingInterval: RollingInterval.Day,            // log-20260809.txt
        retainedFileCountLimit: 14,                      // 最多保留 14 个文件
        outputTemplate: "[{Timestamp:HH:mm:ss} {Level:u3}] {SourceContext}{NewLine}{Message:lj}{NewLine}{Exception}")
    .CreateLogger();

var builder = WebApplication.CreateBuilder(args);

// 关键：把 ASP.NET Core 的 ILogger 全部重定向到 Serilog
builder.Host.UseSerilog();
```

> `builder.Host.UseSerilog()` 会让 `ILogger<T>` 的输出进入 Serilog 管道——原有 `_logger.LogInformation(...)` 一行都不用改，全部自动落到文件。

***

### 任务四：SystemController 系统信息 API

#### 4a. DTO

```csharp
// Server/Models/Dtos/SystemInfoDto.cs
namespace Server.Models.Dtos
{
    public class SystemInfoDto
    {
        public string Version { get; set; } = "1.0.0";
        public DateTime ServerStartedAt { get; set; }
        public string UpTime { get; set; } = string.Empty;     // 已格式化的运行时长
        public int OnlineDeviceCount { get; set; }             // 缓存中有数据的设备数
        public int CachedPointCount { get; set; }              // 缓存测点总数
        public bool ModbusRunning { get; set; }
        public DateTime? LastPollTime { get; set; }
    }
}
```

```csharp
// Server/Models/Dtos/SystemLogsDto.cs
namespace Server.Models.Dtos
{
    public class SystemLogsDto
    {
        public string FileName { get; set; } = string.Empty;
        public List<string> Lines { get; set; } = new();
    }
}
```

```csharp
// Server/Models/Dtos/SystemSettingsDto.cs
namespace Server.Models.Dtos
{
    public class SystemSettingsDto
    {
        public string Key { get; set; } = string.Empty;
        public string Value { get; set; } = string.Empty;
        public string? Description { get; set; }
    }
}
```

#### 4b. SystemService

```csharp
// Server/Services/ISystemService.cs
using Server.Models.Common;
using Server.Models.Dtos;

namespace Server.Services
{
    public interface ISystemService
    {
        Task<ApiResult<SystemInfoDto>> GetInfoAsync();
        Task<ApiResult<SystemLogsDto>> GetLogsAsync(int maxLines);
        Task<ApiResult<List<SystemSettingsDto>>> GetSettingsAsync();
        Task<ApiResult> UpdateSettingsAsync(List<SystemSettingsDto> settings);
        Task<ApiResult> ReloadModbusAsync();
    }
}
```

```csharp
// Server/Services/SystemService.cs
using Server.Infrastructure.Modbus;
using Server.Models.Common;
using Server.Models.Dtos;
using System.Diagnostics;

namespace Server.Services
{
    public class SystemService : ISystemService
    {
        private readonly IModbusService _modbusService;
        private readonly ModbusDataCache _cache;
        private readonly ISystemConfigService _configService;
        private readonly ILogger<SystemService> _logger;

        public SystemService(
            IModbusService modbusService,
            ModbusDataCache cache,
            ISystemConfigService configService,
            ILogger<SystemService> logger)
        {
            _modbusService = modbusService;
            _cache = cache;
            _configService = configService;
            _logger = logger;
        }

        public async Task<ApiResult<SystemInfoDto>> GetInfoAsync()
        {
            var allData = _cache.GetAllData();

            var elapsed = DateTime.UtcNow - Process.GetCurrentProcess().StartTime.ToUniversalTime();

            var info = new SystemInfoDto
            {
                Version = typeof(SystemService).Assembly.GetName().Version?.ToString() ?? "1.0.0",
                ServerStartedAt = Process.GetCurrentProcess().StartTime.ToUniversalTime(),
                UpTime = $"{(int)elapsed.TotalDays}天 {elapsed.Hours}小时 {elapsed.Minutes}分钟",
                OnlineDeviceCount = allData.Count,                       // 有缓存数据的设备视为在线
                CachedPointCount = allData.Values.Sum(d => d.Count),     // 缓存测点总数
                ModbusRunning = _modbusService.IsRunning,
                LastPollTime = _modbusService.LastPollTime
            };

            _logger.LogInformation("查询系统信息：在线设备 {Devices}，测点 {Points}", info.OnlineDeviceCount, info.CachedPointCount);
            return ApiResult<SystemInfoDto>.Success(info);
        }

        public async Task<ApiResult<SystemLogsDto>> GetLogsAsync(int maxLines)
        {
            // Serilog 的相对路径 "logs" 基于进程工作目录（Server 项目目录）
            var dir = Path.Combine(Directory.GetCurrentDirectory(), "logs");
            if (!Directory.Exists(dir))
                return ApiResult<SystemLogsDto>.Success(new SystemLogsDto());

            // 取最近修改的日志文件，读最后 maxLines 行
            var file = Directory.GetFiles(dir, "*.txt")
                .OrderByDescending(f => File.GetLastWriteTime(f))
                .FirstOrDefault();
            if (file == null)
                return ApiResult<SystemLogsDto>.Success(new SystemLogsDto());

            var lines = (await File.ReadAllLinesAsync(file))
                .TakeLast(Math.Max(maxLines, 1))
                .ToList();

            return ApiResult<SystemLogsDto>.Success(new SystemLogsDto
            {
                FileName = Path.GetFileName(file),
                Lines = lines
            });
        }

        public async Task<ApiResult<List<SystemSettingsDto>>> GetSettingsAsync()
        {
            // 直接按已知键清单从内存缓存读取（单例缓存，无需访问文件）
            var keys = new[]
            {
                "Modbus.PortMappings", "Modbus.BaudRate", "Modbus.DataBits", "Modbus.StopBits",
                "Modbus.Parity", "Modbus.ReadTimeout", "Modbus.WriteTimeout",
                "Modbus.PollIntervalMs", "DataRetention.DeviceDataDays", "DataRetention.AlarmDays"
            };

            var settings = keys.Select(key => new SystemSettingsDto
            {
                Key = key,
                Value = _configService.GetValueStr(key) ?? string.Empty
            }).ToList();

            return ApiResult<List<SystemSettingsDto>>.Success(settings);
        }

        public async Task<ApiResult> UpdateSettingsAsync(List<SystemSettingsDto> settings)
        {
            await _configService.AddOrUpdateAllAsync(settings);

            // 配置已进内存缓存，重载 Modbus（重建串口连接 + 重新加载设备配置）
            await _modbusService.ReloadConfigsAsync();

            _logger.LogInformation("系统配置已更新并触发 Modbus 重载，共 {Count} 项", settings.Count);
            return ApiResult.Success("配置保存成功");
        }

        public async Task<ApiResult> ReloadModbusAsync()
        {
            _logger.LogInformation("手动触发 Modbus 配置重载");
            await _modbusService.ReloadConfigsAsync();
            return ApiResult.Success("Modbus 已重载");
        }
    }
}
```

> `ApiResult.Success(...)` 返回无泛型的 `ApiResult`（`Server/Models/Common/ApiResult.cs` 已有），Controller 层直接 `Ok(result)` 即可。

#### 4c. SystemController

```csharp
// Server/Infrastructure/Controllers/SystemController.cs
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Server.Models.Dtos;
using Server.Services;

namespace Server.Infrastructure.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    [Authorize]   // 系统设置属于管理功能，要求登录
    public class SystemController : ControllerBase
    {
        private readonly ISystemService _systemService;

        public SystemController(ISystemService systemService)
        {
            _systemService = systemService;
        }

        /// <summary>系统信息：运行时长 / Modbus 状态 / 在线设备数</summary>
        [HttpGet("info")]
        public async Task<IActionResult> GetInfo()
        {
            var result = await _systemService.GetInfoAsync();
            return Ok(result);
        }

        /// <summary>最近日志（读 Serilog 文件）</summary>
        [HttpGet("logs")]
        public async Task<IActionResult> GetLogs([FromQuery] int maxLines = 200)
        {
            var result = await _systemService.GetLogsAsync(maxLines);
            return Ok(result);
        }

        /// <summary>读取全部系统配置</summary>
        [HttpGet("settings")]
        public async Task<IActionResult> GetSettings()
        {
            var result = await _systemService.GetSettingsAsync();
            return Ok(result);
        }

        /// <summary>保存系统配置并触发 Modbus 重载</summary>
        [HttpPut("settings")]
        public async Task<IActionResult> UpdateSettings([FromBody] List<SystemSettingsDto> settings)
        {
            if (settings == null || settings.Count == 0)
                return BadRequest(ApiResult.Fail(400, "配置不能为空"));

            var result = await _systemService.UpdateSettingsAsync(settings);
            return result.Code == 200 ? Ok(result) : BadRequest(result);
        }

        /// <summary>手动重载 Modbus 配置</summary>
        [HttpPost("modbus/reload")]
        public async Task<IActionResult> ReloadModbus()
        {
            var result = await _systemService.ReloadModbusAsync();
            return Ok(result);
        }
    }
}
```

> 注意 `UpdateSettings` 中先判断空再调服务，这是业务校验放 Controller 的轻量场景；复杂规则（如 `[FromBody]` DTO 字段校验）交给 DataAnnotations（见任务六）。

***

### 任务五：全局异常处理中间件

`Infrastructure/Middleware/` 目录已预留，新建中间件统一捕获未处理异常，保证任何异常都以统一的 `ApiResult` 格式返回，避免前端收到裸 500 / 堆栈泄漏。

```csharp
// Server/Infrastructure/Middleware/ExceptionHandlingMiddleware.cs
using Server.Models.Common;

namespace Server.Infrastructure.Middleware
{
    /// <summary>全局异常处理：统一返回 ApiResult 格式，开发环境附带异常详情</summary>
    public class ExceptionHandlingMiddleware
    {
        private readonly RequestDelegate _next;
        private readonly ILogger<ExceptionHandlingMiddleware> _logger;
        private readonly IHostEnvironment _env;

        public ExceptionHandlingMiddleware(RequestDelegate next, ILogger<ExceptionHandlingMiddleware> logger, IHostEnvironment env)
        {
            _next = next;
            _logger = logger;
            _env = env;
        }

        public async Task InvokeAsync(HttpContext context)
        {
            try
            {
                await _next(context);
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "未处理异常：{Message}", ex.Message);

                context.Response.StatusCode = StatusCodes.Status500InternalServerError;
                context.Response.ContentType = "application/json";

                // 开发环境返回详细错误便于排查，生产只返回统一文案
                // 注意：ApiResult.Fail 只有两个参数，异常堆栈已通过上面的 LogError 记录
                var result = _env.IsDevelopment()
                    ? ApiResult.Fail(500, $"服务器内部错误: {ex.Message}")
                    : ApiResult.Fail(500, "服务器内部错误");

                await context.Response.WriteAsJsonAsync(result);
            }
        }
    }
}
```

`Program.cs` 注册（必须放在管道最前面，保证能捕获后续所有中间件/控制器抛出的异常）：

```csharp
app.UseMiddleware<ExceptionHandlingMiddleware>();
app.UseCors("AllowClient");
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();
```

> **中间件顺序**：异常处理中间件必须是第一个注册的 `Use`，否则它只能捕获管道下游的异常，捕获不到它自己之前注册的中间件。

***

### 任务六：参数校验（DataAnnotations + \[ApiController]）

`[ApiController]` 特性开启后，会自动执行模型绑定 + DataAnnotations 校验：校验失败直接返回 `400 Bad Request`（含 ValidationProblemDetails），**不会进入 Controller 方法体**，无需手写 if 判断。

现有 DTO 已有部分 `[Required]`，本次统一补齐常用校验特性：

```csharp
// Server/Models/Dtos/CreateDeviceRequest.cs（补齐后）
using System.ComponentModel.DataAnnotations;

namespace Server.Models.Dtos
{
    public class CreateDeviceRequest
    {
        [Required(ErrorMessage = "设备名称不能为空")]
        [MaxLength(50)]
        public string Name { get; set; } = string.Empty;

        [Required(ErrorMessage = "设备编号不能为空")]
        [MaxLength(20)]
        public string Code { get; set; } = string.Empty;

        [Range(1, int.MaxValue, ErrorMessage = "请选择所属产线")]
        public int ProductionLineId { get; set; }

        [Range(1, 255, ErrorMessage = "从站地址范围 1-255")]
        public byte SlaveAddress { get; set; } = 1;

        public int BaudRate { get; set; } = 9600;
        public string ComPort { get; set; } = "COM3";
        [MaxLength(200)]
        public string? Description { get; set; }
    }
}
```

同理补齐：

- `UpdateDeviceRequest`：与上面对齐（`[Required]` + `[MaxLength]` + `[Range]`）。
- `CreateProductionLineRequest` / `UpdateProductionLineRequest`：`Name` 加 `[MaxLength(50)]`。
- `CreateDataPointRequest` / `UpdateDataPointRequest`：`Name` 加 `[MaxLength(50)]`；`RegisterAddress` 加 `[Range(0, 65535)]`；`AlarmHigh`/`AlarmLow` 若为必填可加 `[Required]`。

> 校验触发条件：Controller 必须带 `[ApiController]`（现有 Controller 全部已带）。若想让校验失败返回**自定义格式**而非默认的 ValidationProblemDetails，可在 `Program.cs` 中 `builder.Services.AddControllers().ConfigureApiBehaviorOptions(...)` 配置，Day 8 采用默认即可。

***

### 任务七：操作日志（关键操作写 ILogger）

不建表，仅在关键操作处补充日志。因为已接 Serilog，这些日志自动落文件。示例：

```csharp
// Server/Services/AuthService.cs（改造片段）
private readonly ILogger<AuthService> _logger;   // 构造函数注入

// LoginAsync 成功分支末尾：
_logger.LogInformation("用户 {User} 登录成功", user.Username);

// LoginAsync 失败分支：
_logger.LogWarning("登录失败：用户名 {User}，原因：密码错误或用户不存在", request.Username);
```

其他补日志的位置（改造点清单，均在对应 Service 中注入 `ILogger<T>`）：

| 位置                                                          | 日志级别           | 示例                           |
| ----------------------------------------------------------- | -------------- | ---------------------------- |
| `AuthService.LoginAsync` 成功/失败                              | Info / Warning | 登录成功 / 失败                    |
| `AuthService.RevokeAllTokensAsync`                          | Info           | 用户注销                         |
| `DeviceService.CreateAsync` / `UpdateAsync` / `DeleteAsync` | Info           | 创建设备 `{Code}`                |
| `ProductionLineService` 增删改                                 | Info           | 新增产线 `{Name}`                |
| `DataPointService` 增删改                                      | Info           | 新增测点 `{Name}`                |
| `HistoryController.Export`                                  | Info           | 导出历史数据 `deviceId={deviceId}` |

> 日志内容遵循结构化日志规范（用 `{占位符}` 而不是字符串拼接），便于 Serilog 输出为可查询的键值对。

***

### 任务八：客户端设置页

#### 8a. 客户端 DTO

```csharp
// Client/Models/Dtos/SystemInfoDto.cs
namespace Client.Models.Dtos
{
    public class SystemInfoDto
    {
        public string Version { get; set; } = "1.0.0";
        public DateTime ServerStartedAt { get; set; }
        public string UpTime { get; set; } = string.Empty;
        public int OnlineDeviceCount { get; set; }
        public int CachedPointCount { get; set; }
        public bool ModbusRunning { get; set; }
        public DateTime? LastPollTime { get; set; }
    }
}
```

```csharp
// Client/Models/Dtos/SystemSettingsDto.cs
namespace Client.Models.Dtos
{
    public class SystemSettingsDto
    {
        public string Key { get; set; } = string.Empty;
        public string Value { get; set; } = string.Empty;
        public string? Description { get; set; }
    }
}
```

```csharp
// Client/Models/Dtos/SystemLogsDto.cs
namespace Client.Models.Dtos
{
    public class SystemLogsDto
    {
        public string FileName { get; set; } = string.Empty;
        public List<string> Lines { get; set; } = new();
    }
}
```

#### 8b. 客户端 ISystemService / SystemService

```csharp
// Client/Service/ISystemService.cs
using Client.Models.Dtos;

namespace Client.Service
{
    public interface ISystemService
    {
        Task<SystemInfoDto?> GetInfoAsync();
        Task<SystemLogsDto?> GetLogsAsync(int maxLines = 200);
        Task<List<SystemSettingsDto>?> GetSettingsAsync();
        Task<bool> UpdateSettingsAsync(List<SystemSettingsDto> settings);
        Task<bool> ReloadModbusAsync();
    }
}
```

```csharp
// Client/Service/SystemService.cs
using Client.Infrastructure;
using Client.Models.Dtos;
using System.Net.Http.Json;
using System.Text.Json;

namespace Client.Service
{
    public class SystemService(HttpClientBase _http) : ISystemService
    {
        private static readonly JsonSerializerOptions _jsonOptions = new() { PropertyNameCaseInsensitive = true };

        public async Task<SystemInfoDto?> GetInfoAsync()
        {
            var response = await _http.GetAsync("api/system/info");
            if (!response.IsSuccessStatusCode) return null;
            var result = await response.Content.ReadFromJsonAsync<ApiResult<SystemInfoDto>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<SystemLogsDto?> GetLogsAsync(int maxLines = 200)
        {
            var response = await _http.GetAsync($"api/system/logs?maxLines={maxLines}");
            if (!response.IsSuccessStatusCode) return null;
            var result = await response.Content.ReadFromJsonAsync<ApiResult<SystemLogsDto>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<List<SystemSettingsDto>?> GetSettingsAsync()
        {
            var response = await _http.GetAsync("api/system/settings");
            if (!response.IsSuccessStatusCode) return null;
            var result = await response.Content.ReadFromJsonAsync<ApiResult<List<SystemSettingsDto>>>(_jsonOptions);
            return result?.Code == 200 ? result.Data : null;
        }

        public async Task<bool> UpdateSettingsAsync(List<SystemSettingsDto> settings)
        {
            var response = await _http.PutAsJsonAsync("api/system/settings", settings);
            if (!response.IsSuccessStatusCode) return false;
            var result = await response.Content.ReadFromJsonAsync<ApiResult>(_jsonOptions);
            return result?.Code == 200;
        }

        public async Task<bool> ReloadModbusAsync()
        {
            var response = await _http.PostAsync("api/system/modbus/reload", null);
            if (!response.IsSuccessStatusCode) return false;
            var result = await response.Content.ReadFromJsonAsync<ApiResult>(_jsonOptions);
            return result?.Code == 200;
        }
    }
}
```

> 需要确认 `HttpClientBase` 是否有 `PostAsync` 无 body 的重载；若没有，用 `PostAsJsonAsync("api/system/modbus/reload", new { })` 代替。

#### 8c. SettingsViewModel（完整实现）

端口映射在 UI 中编辑为行列表，保存时序列化为 JSON 字符串：

```csharp
// Client/ViewModels/SettingsViewModel.cs
using Client.Models;
using Client.Models.Dtos;
using Client.Service;
using System.Collections.ObjectModel;
using System.Text.Json;

namespace Client.ViewModels
{
    /// <summary>端口映射行（SlaveCom=从站端口，MasterCom=主站端口）</summary>
    public class PortMappingItem : BindableBase
    {
        private string _slaveCom = "COM2";
        private string _masterCom = "COM1";
        public string SlaveCom { get => _slaveCom; set => SetProperty(ref _slaveCom, value); }
        public string MasterCom { get => _masterCom; set => SetProperty(ref _masterCom, value); }
    }

    public class SettingsViewModel : BindableBase, INavigationAware
    {
        private readonly ISystemService _systemService;

        // ===== Modbus 配置 =====
        public ObservableCollection<PortMappingItem> PortMappings { get; } = new();
        private string _baudRate = "9600";
        private string _dataBits = "8";
        private string _stopBits = "One";
        private string _parity = "None";
        private string _readTimeout = "500";
        private string _writeTimeout = "500";
        private string _pollIntervalMs = "1000";

        // ===== 数据保留 =====
        private string _deviceDataDays = "30";
        private string _alarmDays = "30";

        // ===== 日志 =====
        private string _logText = string.Empty;
        private string _logFileName = string.Empty;

        // ===== 关于 =====
        private SystemInfoDto _systemInfo = new();

        private bool _isLoading;
        private string _statusMessage = string.Empty;

        public DelegateCommand SaveCommand { get; }
        public DelegateCommand ReloadModbusCommand { get; }
        public DelegateCommand AddMappingCommand { get; }
        public DelegateCommand RemoveMappingCommand { get; }
        public DelegateCommand LoadLogsCommand { get; }
        public DelegateCommand LoadInfoCommand { get; }

        public SettingsViewModel(ISystemService systemService)
        {
            _systemService = systemService;

            SaveCommand = new DelegateCommand(async () => await SaveAsync(), () => !IsLoading);
            ReloadModbusCommand = new DelegateCommand(async () => await ReloadModbusAsync(), () => !IsLoading);
            AddMappingCommand = new DelegateCommand(() => PortMappings.Add(new PortMappingItem()));
            RemoveMappingCommand = new DelegateCommand<object?>(RemoveMapping);
            LoadLogsCommand = new DelegateCommand(async () => await LoadLogsAsync(), () => !IsLoading);
            LoadInfoCommand = new DelegateCommand(async () => await LoadInfoAsync(), () => !IsLoading);
        }

        // ===== INavigationAware =====
        public bool IsNavigationTarget(NavigationContext navigationContext) => true;
        public void OnNavigatedFrom(NavigationContext navigationContext) { }
        public async void OnNavigatedTo(NavigationContext navigationContext)
        {
            await LoadAllAsync();
        }

        /// <summary>进入页面加载全部数据：配置 + 系统信息</summary>
        private async Task LoadAllAsync()
        {
            IsLoading = true;
            try
            {
                var settingsTask = _systemService.GetSettingsAsync();
                var infoTask = _systemService.GetInfoAsync();
                await Task.WhenAll(settingsTask, infoTask);

                var settings = await settingsTask;
                if (settings != null)
                    ApplySettings(settings);

                var info = await infoTask;
                if (info != null)
                    SystemInfo = info;
            }
            finally
            {
                IsLoading = false;
            }
        }

        /// <summary>把键值对配置映射到界面字段</summary>
        private void ApplySettings(List<SystemSettingsDto> settings)
        {
            var dict = settings.ToDictionary(s => s.Key, s => s.Value);

            PortMappings.Clear();
            var mappings = JsonSerializer.Deserialize<Dictionary<string, string>>(
                dict.GetValueOrDefault("Modbus.PortMappings", "{}"));
            if (mappings != null)
                foreach (var (slaveCom, masterCom) in mappings)
                    PortMappings.Add(new PortMappingItem { SlaveCom = slaveCom, MasterCom = masterCom });

            BaudRate = dict.GetValueOrDefault("Modbus.BaudRate", "9600");
            DataBits = dict.GetValueOrDefault("Modbus.DataBits", "8");
            StopBits = dict.GetValueOrDefault("Modbus.StopBits", "One");
            Parity = dict.GetValueOrDefault("Modbus.Parity", "None");
            ReadTimeout = dict.GetValueOrDefault("Modbus.ReadTimeout", "500");
            WriteTimeout = dict.GetValueOrDefault("Modbus.WriteTimeout", "500");
            PollIntervalMs = dict.GetValueOrDefault("Modbus.PollIntervalMs", "1000");
            DeviceDataDays = dict.GetValueOrDefault("DataRetention.DeviceDataDays", "30");
            AlarmDays = dict.GetValueOrDefault("DataRetention.AlarmDays", "30");
        }

        /// <summary>把界面字段汇总成配置清单并保存</summary>
        private async Task SaveAsync()
        {
            IsLoading = true;
            StatusMessage = string.Empty;
            try
            {
                // 端口映射：从行列表反序列化为 {从站:主站} 字典 → JSON
                var mappings = PortMappings
                    .Where(m => !string.IsNullOrWhiteSpace(m.SlaveCom) && !string.IsNullOrWhiteSpace(m.MasterCom))
                    .ToDictionary(m => m.SlaveCom.Trim(), m => m.MasterCom.Trim());

                var settings = new List<SystemSettingsDto>
                {
                    new() { Key = "Modbus.PortMappings", Value = JsonSerializer.Serialize(mappings) },
                    new() { Key = "Modbus.BaudRate", Value = BaudRate },
                    new() { Key = "Modbus.DataBits", Value = DataBits },
                    new() { Key = "Modbus.StopBits", Value = StopBits },
                    new() { Key = "Modbus.Parity", Value = Parity },
                    new() { Key = "Modbus.ReadTimeout", Value = ReadTimeout },
                    new() { Key = "Modbus.WriteTimeout", Value = WriteTimeout },
                    new() { Key = "Modbus.PollIntervalMs", Value = PollIntervalMs },
                    new() { Key = "DataRetention.DeviceDataDays", Value = DeviceDataDays },
                    new() { Key = "DataRetention.AlarmDays", Value = AlarmDays }
                };

                var ok = await _systemService.UpdateSettingsAsync(settings);
                StatusMessage = ok ? "保存成功，Modbus 已重载" : "保存失败";
            }
            finally
            {
                IsLoading = false;
            }
        }

        private async Task ReloadModbusAsync()
        {
            var ok = await _systemService.ReloadModbusAsync();
            StatusMessage = ok ? "Modbus 已重载" : "重载失败";
        }

        private void RemoveMapping(object? parameter)
        {
            if (parameter is PortMappingItem item)
                PortMappings.Remove(item);
        }

        private async Task LoadLogsAsync()
        {
            IsLoading = true;
            try
            {
                var logs = await _systemService.GetLogsAsync(300);
                if (logs != null)
                {
                    LogFileName = logs.FileName;
                    LogText = string.Join(Environment.NewLine, logs.Lines);
                }
                else
                {
                    LogText = "日志加载失败";
                }
            }
            finally
            {
                IsLoading = false;
            }
        }

        private async Task LoadInfoAsync()
        {
            var info = await _systemService.GetInfoAsync();
            if (info != null)
                SystemInfo = info;
        }

        // ===== 属性（全部走 SetProperty 通知 UI）=====
        public string BaudRate { get => _baudRate; set => SetProperty(ref _baudRate, value); }
        public string DataBits { get => _dataBits; set => SetProperty(ref _dataBits, value); }
        public string StopBits { get => _stopBits; set => SetProperty(ref _stopBits, value); }
        public string Parity { get => _parity; set => SetProperty(ref _parity, value); }
        public string ReadTimeout { get => _readTimeout; set => SetProperty(ref _readTimeout, value); }
        public string WriteTimeout { get => _writeTimeout; set => SetProperty(ref _writeTimeout, value); }
        public string PollIntervalMs { get => _pollIntervalMs; set => SetProperty(ref _pollIntervalMs, value); }
        public string DeviceDataDays { get => _deviceDataDays; set => SetProperty(ref _deviceDataDays, value); }
        public string AlarmDays { get => _alarmDays; set => SetProperty(ref _alarmDays, value); }
        public string LogText { get => _logText; set => SetProperty(ref _logText, value); }
        public string LogFileName { get => _logFileName; set => SetProperty(ref _logFileName, value); }
        public SystemInfoDto SystemInfo { get => _systemInfo; set => SetProperty(ref _systemInfo, value); }
        public string StatusMessage { get => _statusMessage; set => SetProperty(ref _statusMessage, value); }
        public bool IsLoading
        {
            get => _isLoading;
            set
            {
                if (SetProperty(ref _isLoading, value))
                {
                    SaveCommand.RaiseCanExecuteChanged();
                    ReloadModbusCommand.RaiseCanExecuteChanged();
                    LoadLogsCommand.RaiseCanExecuteChanged();
                    LoadInfoCommand.RaiseCanExecuteChanged();
                }
            }
        }
    }
}
```

> `PortMappingItem` 单独声明为命名空间下的类（而非嵌套），方便 XAML 绑定；端口映射用 `{从站:主站}` 字典序列化，与服务端 `BuildComMapping` 的反转逻辑对应。

#### 8d. SettingsView\.xaml（TabControl 四块）

```xml
<!-- Client/Views/SettingsView.xaml -->
<UserControl x:Class="Client.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Client.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:SettingsViewModel/>
    </UserControl.DataContext>

    <Grid Margin="16">
        <TabControl>
            <!-- Tab1: Modbus 配置 -->
            <TabItem Header="Modbus 配置">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <GroupBox Header="端口映射（从站 → 主站）">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding PortMappings}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <TextBox Text="{Binding SlaveCom, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                                                <TextBlock Text=" → " VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding MasterCom, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                                                <Button Content="删除" Margin="12,0,0,0" Padding="6,2"
                                                        Command="{Binding DataContext.RemoveMappingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ 添加映射" Command="{Binding AddMappingCommand}" HorizontalAlignment="Left" Margin="0,8,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="串口参数" Margin="0,12,0,0">
                            <UniformGrid Columns="3">
                                <StackPanel Margin="4">
                                    <TextBlock Text="波特率" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding BaudRate, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="数据位" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding DataBits, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="停止位 (One/Two)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding StopBits, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="校验位 (None/Odd/Even)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding Parity, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="读超时 (ms)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding ReadTimeout, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Margin="4">
                                    <TextBlock Text="写超时 (ms)" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding WriteTimeout, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </UniformGrid>
                        </GroupBox>

                        <GroupBox Header="采集周期" Margin="0,12,0,0">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Text="轮询间隔 (ms)" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding PollIntervalMs, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="12,0,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                            <Button Content="重载 Modbus" Command="{Binding ReloadModbusCommand}" Width="110" Height="30" Margin="0,0,8,0"/>
                            <Button Content="保存配置" Command="{Binding SaveCommand}" Width="110" Height="30"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab2: 数据保留 -->
            <TabItem Header="数据保留">
                <StackPanel Margin="8" Width="380" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Margin="0,12">
                        <TextBlock Text="历史数据保留天数" Width="140" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding DeviceDataDays, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,12">
                        <TextBlock Text="报警记录保留天数" Width="140" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding AlarmDays, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                    </StackPanel>
                    <Button Content="保存" Command="{Binding SaveCommand}" Width="110" Height="30" HorizontalAlignment="Left" Margin="0,20,0,0"/>
                    <TextBlock Text="归档服务每 5 秒检查一次，超过保留天数的数据会被自动清理。"
                               Foreground="Gray" TextWrapping="Wrap" Margin="0,12,0,0"/>
                </StackPanel>
            </TabItem>

            <!-- Tab3: 日志查看 -->
            <TabItem Header="日志查看">
                <DockPanel Margin="8">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="加载日志" Command="{Binding LoadLogsCommand}" Width="100" Height="28"/>
                        <TextBlock Text="{Binding LogFileName}" VerticalAlignment="Center" Foreground="Gray" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBox Text="{Binding LogText}" IsReadOnly="True" FontFamily="Consolas"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </DockPanel>
            </TabItem>

            <!-- Tab4: 关于 -->
            <TabItem Header="关于">
                <StackPanel Margin="20" Width="400" HorizontalAlignment="Left">
                    <TextBlock Text="SmartFactory Monitor" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding SystemInfo.Version, StringFormat=版本：{0}}" Margin="0,20,0,4"/>
                    <TextBlock Text="{Binding SystemInfo.ServerStartedAt, StringFormat=服务启动：{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.UpTime, StringFormat=已运行：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.OnlineDeviceCount, StringFormat=在线设备：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.CachedPointCount, StringFormat=缓存测点：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.ModbusRunning, StringFormat=Modbus 状态：{0}}" Margin="0,4"/>
                    <TextBlock Text="{Binding SystemInfo.LastPollTime, StringFormat=最近轮询：{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,4"/>
                    <Button Content="刷新" Command="{Binding LoadInfoCommand}" Width="90" Height="28" HorizontalAlignment="Left" Margin="0,16,0,0"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
```

> **关于 Tab 的绑定**：`StringFormat=服务启动：{0:yyyy-MM-dd HH:mm:ss}` 是 `StringFormat` 在 XAML 中的正确写法（大括号需用 `{0}` 占位，前面加中文字符后 `{0` 不会被当作扩展标记，无需 `{}` 前缀）；`LastPollTime` 为 `DateTime?`，值为 null 时显示为空，不报错。

#### 8e. App.xaml.cs 注册

```csharp
// Client/App.xaml.cs（RegisterTypes 方法内追加）
containerRegistry.RegisterSingleton<ISystemService, SystemService>();
```

> 若客户端 `HttpClientBase` 没有 `PutAsJsonAsync` / `PostAsync` 无 body 重载，参考其现有 `GetAsync` 实现补一个（核心是附带 Authorization 头并序列化请求体）；或者 `PostAsync` 用 `PostAsJsonAsync("api/system/modbus/reload", new { })` 代替。

***

### 任务九：Program.cs 改造汇总

```csharp
// Server/Program.cs（改造点汇总）

// ====== DI 登记：Service（原有注册代码后追加） ======
builder.Services.AddSingleton<ISystemConfigService, SystemConfigService>();   // 单例：内存缓存 + setting.json 读写
builder.Services.AddScoped<ISystemService, SystemService>();

// ====== 构建应用（原有） ======
var app = builder.Build();

// ====== 中间件管道（原有，异常中间件放最前） ======
app.UseMiddleware<ExceptionHandlingMiddleware>();
app.UseCors("AllowClient");
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();

// ====== 启动初始化：加载 setting.json 进缓存（app.Run 前；文件缺失会抛异常，属预期——提示手工创建） ======
var configService = app.Services.GetRequiredService<ISystemConfigService>();
await configService.LoadAsync();                   // 文件内容全量加载进内存缓存

app.Run();
```

> `SystemConfigService` 不再依赖任何仓储/DbContext，直接从根容器解析即可（无需 CreateScope）；`Main` 需改为 `public static async Task Main(string[] args)` 才能用 `await`。`LoadAsync` 放在 `app.Run()` 前，保证后台服务启动时配置已就绪。

***

### Day 8 DI 注册汇总

| 生命周期          | 注册项                                            | 说明                                      |
| ------------- | ---------------------------------------------- | --------------------------------------- |
| Singleton     | `ISystemConfigService` / `SystemConfigService` | 配置服务（内存缓存 + setting.json 读写，无仓储依赖）      |
| Scoped        | `ISystemService` / `SystemService`（Server）     | 系统信息 / 日志 / 配置管理                        |
| Singleton     | `IModbusService` / `ModbusService`（改造）         | 采集周期可配置，暴露 `IsRunning` / `LastPollTime` |
| Singleton     | `ModbusPortManager`（改造）                        | 串口参数与端口映射从 setting.json 读取              |
| HostedService | `DataArchiveService`（改造）                       | 保留天数可配置，联动清理报警                          |
| Scoped        | `IAlarmRepository` / `AlarmRepository`（扩展）     | 新增 `CleanupOldAlarmsAsync`              |
| Middleware    | `ExceptionHandlingMiddleware`                  | 全局异常统一返回 ApiResult                      |
| Singleton     | `ISystemService` / `SystemService`（Client）     | 客户端 HTTP 调用                             |
| ForNavigation | `SettingsView` / `SettingsViewModel`（已注册，补全实现） | 系统设置页                                   |

> **改动文件清单**：
>
> **Server**：新增 `setting.json`、`ISystemConfigService` / `SystemConfigService`、`SystemInfoDto` / `SystemLogsDto` / `SystemSettingsDto`、`ISystemService` / `SystemService`、`SystemController`、`ExceptionHandlingMiddleware`；改造 `ModbusPortManager`、`ModbusService`、`IModbusService`、`DataArchiveService`、`IAlarmRepository` / `AlarmRepository`、各 Service 关键操作日志、`Server.csproj`（Serilog 包 + setting.json 复制）、`Program.cs`（Serilog + 中间件 + 注册）。**无需迁移**。
>
> **Client**：新增 `SystemInfoDto` / `SystemLogsDto` / `SystemSettingsDto`、`ISystemService` / `SystemService`；补全 `SettingsView.xaml` / `SettingsViewModel`；改造 `App.xaml.cs`（注册）。
>
> **注意**：`Program.cs` 构建后需调用 `configService.LoadAsync()`（在 `app.Run()` 前）加载 setting.json 进缓存；`ModbusService` / `DataArchiveService` 启动时也会再调一次 `LoadAsync()` 兜底。setting.json 需手工创建（任务一 1a），文件缺失时启动会抛 `FileNotFoundException`。

