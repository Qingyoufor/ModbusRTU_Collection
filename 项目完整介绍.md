# SmartFactoryMonitor 项目完整介绍

> **本文档用途**：完整、详尽地介绍 SmartFactoryMonitor 项目（后端 + WPF 客户端），供其他 AI Agent 撰写简历时取材。第十节专门给出简历写作指引。
>
> **资料来源说明**：本文依据项目源码、README、开发日志（Day 1-8）、问题日志（85 条问题记录）、数据库设计文档整理。注意：`复盘笔记.md` 只覆盖了 Server 后端的知识点（EF Core、JWT、Modbus），**WPF 客户端的开发记录主要在《问题日志》Day 4-8 章节和《开发日志5-8》中**，客户端部分以源码为准。

---

## 一、项目定位与概述

**SmartFactory Monitor** 是一个面向工业现场的**设备数据采集与监控系统**（简化版 SCADA），采用 **C/S 架构**：

- **后端**：ASP.NET Core Web API，通过 **Modbus RTU 协议**轮询采集 PLC/仪器仪表的寄存器数据，提供实时监控、报警检测、历史趋势、系统配置等 RESTful API
- **前端**：WPF 桌面客户端（Prism + MVVM），提供登录认证、设备管理、实时监控（趋势曲线）、报警中心、历史查询（CSV 导出）、系统设置等功能
- **数据库**：SQL Server，EF Core Code-First 迁移

一句话概括数据流：**Modbus RTU 从站 → 后台轮询引擎 → 内存实时缓存 → （实时 API / 报警检测 / 历史归档）三条消费路径 → WPF 客户端展示**。

## 二、代码规模与量化数据

| 指标 | 数值 |
|---|---|
| 后端代码量（.cs + .xaml） | 约 **6,900 行** |
| WPF 客户端代码量 | 约 **3,400 行** |
| 合计 | 约 **10,300 行** |
| 数据库表 | **7 张**核心表 |
| API 控制器 | 7 个，**30+ 个 RESTful 端点** |
| EF Core 迁移 | 多轮迭代（Migrations 目录 17 个文件） |
| 后台服务 | 2 个（报警检测、数据归档）+ 1 个 Modbus 轮询引擎 |
| 开发周期 | 8 个开发日迭代（Day 1-4 后端，Day 4-8 WPF 客户端） |
| 问题记录 | 独立《问题日志》记录 **85 条**开发问题及解决过程 |
| 开发调试环境 | 虚拟串口工具（com0com / ELTIMA）模拟 Modbus RTU 主从通讯 |

## 三、技术栈（含版本）

### 后端（Server，net10.0）
| 技术 | 版本 | 用途 |
|---|---|---|
| ASP.NET Core Web API | .NET 10 | RESTful API |
| EF Core (SqlServer) | 10.0.9 | ORM、Code-First 迁移 |
| NModbus / NModbus.Serial | 3.0.83 | Modbus RTU 主站通讯 |
| AutoMapper | 16.1.1 | Entity ↔ DTO 映射 |
| BCrypt.Net-Next | 4.2.0 | 密码哈希 |
| JWT Bearer 认证 | 10.0.9 | 双 Token 认证体系 |
| Serilog (AspNetCore + File) | 10.0.0 / 7.0.0 | 结构化日志、按天滚动文件 |
| Swashbuckle.AspNetCore | 10.2.1 | Swagger（带 Bearer 安全定义） |
| System.IO.Ports | 10.0.9 | 串口通讯 |

### WPF 客户端（Client，net10.0-windows）
| 技术 | 版本 | 用途 |
|---|---|---|
| Prism.DryIoc | 9.0.537 | MVVM、模块化、区域导航、对话框服务 |
| LiveChartsCore.SkiaSharpView.WPF | 2.0.5 | 实时/历史趋势曲线图 |
| HandyControl | 3.5.1 | UI 控件库 |
| Microsoft.Xaml.Behaviors.Wpf | 1.1.142 | XAML 事件行为绑定 |

### 数据与工具
- SQL Server（复合索引、唯一索引设计）
- 虚拟串口对（com0com / ELTIMA：COM1⇄COM2、COM3⇄COM4）模拟现场主从站
- Git 版本控制

## 四、系统架构

```
后端 Server（Repository → Service → Controller 三层 + 后台服务）

  setting.json ──> SystemConfigService（内存缓存，点分键扁平化）
                          │
  [Modbus RTU 从站] ──> ModbusService（轮询引擎：跨口并行、同口串行）
                          │
                    ModbusDataCache（实时值两级缓存）
                          │                        │
                          ├─> Realtime API ──> 实时监控页（2 秒刷新 + LiveCharts2）
                          ├─> AlarmDetectionService（1s 越限检测）──> Alarm 表 ──> 报警中心
                          └─> DataArchiveService（5s 批量归档）──> DeviceData 历史表 ──> 历史趋势 + CSV 导出
```

```
WPF 客户端（Prism 模块化 + 严格 MVVM）

  App（LoginView 启动）──登录成功──> ShellView（左侧菜单 + MainRegion）
    ├─ 实时监控页（产线-设备树 + 数值面板 + 趋势曲线）
    ├─ 报警中心（分页 / 筛选 / 确认）
    ├─ 历史趋势页（时间段查询 + 统计 + CSV 导出）
    ├─ 系统设置页（Modbus 参数 / 数据保留 / 日志查看）
    └─ 设备管理模块（Prism Module：列表 + 对话框式编辑）
```

## 五、数据库设计（7 张表）

| 表 | 说明 | 关键设计 |
|---|---|---|
| Users | 用户 | `Username` 唯一索引；BCrypt 密码哈希；角色枚举（Admin/Operator/Viewer） |
| RefreshTokens | 刷新令牌 | `Token` 唯一索引加速查找；与 User 关联；支持撤销（RevokedAt）与轮换 |
| ProductionLines | 产线 | 产线 → 设备一对多 |
| Devices | 设备 | 串口号、从站地址、状态枚举（Offline/Running/Alarm/Stopped）；仅 Online 设备参与采集 |
| DataPoints | 测点 | 寄存器地址（PLC 绝对地址 40001 起）、寄存器类型（Holding=4/Input=3）、数据类型（Int16/UInt16/Int32/Float）、缩放系数、小数位、高/低限报警阈值 |
| DeviceData | 历史数据 | **bigint 主键**（估算 10 设备×6 测点≈60 条/秒、约 518 万条/天）；复合索引 `(DeviceId, DataPointId, RecordedAt)` |
| Alarms | 报警记录 | 快照字段 `Value` + `Threshold`（配置变更后历史仍可读）；复合索引 `(DeviceId, Status, OccurredAt)` |

**关系约束要点**：
- 所有指向核心业务表（Device、DataPoint、ProductionLine）的外键显式配置 `DeleteBehavior.Restrict`，防止 EF Core 默认级联删除误删报警/历史数据，同时规避 SQL Server 多重级联路径错误
- 一对多集合导航属性统一使用 `ICollection<T>`
- 实体类用 `[Index(IsUnique = true)]` 声明唯一约束（唯一约束物理上靠唯一索引实现）
- 只读查询使用 `AsNoTracking` 优化性能

## 六、后端详解（Server）

### 6.1 启动流程（Program.cs，.NET 6+ 最小托管模型）
1. **Serilog 引导**：在 `builder.Build()` 之前初始化，保证启动阶段异常也有日志
2. 服务注册：EF Core DbContext（SqlServer）、AutoMapper（`AddAutoMapper` 扫描 MappingProfile）、JWT Bearer（Issuer=SmartFactoryMonitor / Audience=SmartFactoryClient / 120 分钟过期 / 零时钟偏移）、CORS 策略 `AllowClient`（允许 http://localhost:5000 携带凭证）、Swagger（仅 Development，含 Bearer 安全定义）
3. 业务服务 + 仓储注册（接口 → 实现，Scoped）；Modbus 组件注册；两个 `BackgroundService` 注册为 Hosted 服务
4. **中间件顺序（关键设计）**：`ExceptionHandlingMiddleware` 注册在最外层 → CORS → 认证 → 授权 → Swagger，保证后续所有中间件抛出的异常都被统一捕获并转为标准 ApiResult 格式
5. **`app.Run()` 之前**执行 `SystemConfigService.LoadAsync()` 预加载 setting.json —— fail-fast 式初始化，确保配置先于后台服务与 API 可用

### 6.2 分层架构（Repository → Service → Controller）
- **Repository 层**：`RepositoryBase` 泛型基类持有 `DbSet<TEntity>` 与 `DbContext`（protected 供子类使用）；7 个业务仓储（Device/DataPoint/Alarm/DeviceData/User/RefreshToken/ProductionLine），各自提供定制查询（如 `GetUnacknowledgedAlarmAsync`）。子类统一调用基类 `SaveChangedAsync()` 保持事务处理一致
- **Service 层**：业务逻辑与 DTO 转换；跨网络边界返回 `ApiResult<T>`，进程内消费返回内存类型（如 ModbusDataCache 引用）
- **Controller 层**：薄控制器，使用 .NET 6 主构造函数注入（如 `HistoryController(IHistoryService _historyService)`），`[Authorize]` 保护 + 登录/刷新端点 `[AllowAnonymous]`
- **DTO 规范**：按业务域分文件夹（Auth/Device/DataPoint/Alarm/History/Realtime/System/ProductionLine），一类一文件；请求 DTO 使用 C# `record` + init-only 属性保证不可变；一个实体可映射多个 DTO（如 DeviceDto 列表视图 vs 详情视图）

### 6.3 认证体系（JWT 双 Token）
- **登录**：BCrypt 校验密码 → 签发 AccessToken（120 分钟）+ RefreshToken（7 天，随机串入库并建唯一索引）
- **刷新**：`/api/auth/refresh` 校验库中 RefreshToken 有效性 → 旧 Token 标记撤销（RevokedAt）并轮换新 Token 对（Refresh Token Rotation）
- **无感续期**：WPF 端 HttpClient 拦截 401 自动调用刷新接口并重放原请求（见 7.4）
- `JwtHelper` 统一负责 Token 签发/校验/从过期 Token 提取 Claims（`GetPrincipalFromExpiredToken`，关闭时钟偏移）

### 6.4 API 端点全表（7 个控制器，30+ 端点）

| 控制器 | 端点 | 说明 |
|---|---|---|
| AuthController | POST signIn / refresh / logout | 登录签发 Token 对、刷新轮换、登出撤销 |
| ProductionLinesController | GET / GET{id} / POST / PUT{id} / DELETE{id} | 产线 CRUD |
| DevicesController | GET / GET{id} / POST / PUT{id} / DELETE{id} | 设备 CRUD（含串口与从站地址配置） |
| DataPointsController | GET / POST / PUT{id} / DELETE{id} | 测点 CRUD（寄存器地址、类型、阈值） |
| RealtimeController | GET devices/{deviceId}/data | 单设备实时数据（含测点值/单位/报警状态） |
| | GET devices/all | 全量设备实时数据（供总览页） |
| AlarmsController | GET current / GET history | 当前报警 / 历史报警（分页筛选） |
| | PUT {id}/acknowledge | 确认报警 |
| HistoryController | GET data / statistics | 历史趋势数据（支持 interval 聚合）/ 统计值 |
| | GET export | CSV 导出（UTF-8 BOM，手写转义，零第三方依赖） |
| SystemController | GET info / logs | 运行信息 / 读取最近日志 |
| | GET settings / PUT settings | 读取 / 保存系统配置（写回 setting.json） |
| | POST modbus/reload | **Modbus 配置热重载**（不重启服务生效） |

- 统一响应格式：`{ "Code": 200, "Message": "...", "Data": ... }`（ApiResult）+ 通用分页 PageResult
- 全局异常由 `ExceptionHandlingMiddleware` 捕获，转为统一错误格式，业务代码无需 try-catch 包接口

### 6.5 Modbus RTU 采集引擎（项目核心亮点）

位于 `Infrastructure/ModbusRTU/`，共 7 个类各司其职：

| 组件 | 职责 |
|---|---|
| `ModbusConfigLoader` | 从数据库加载 Status=Online 的设备及测点，PLC 绝对地址（40001 起）→ 协议偏移地址换算，按设备组织 `DataPointConfig` |
| `ModbusPortManager` | 串口与 Master 生命周期管理（详见下） |
| `ModbusGroupingHelper` | **寄存器分组算法**（详见下） |
| `ModbusDataReader` | 按分组实际读取（功能码 03/04），带重试与退避；单组失败不影响整体轮询；解析后写入缓存 |
| `ModbusDataParser` | 原始寄存器值 → 工程值：支持 Int16/UInt16/Int32/Float，**Float 用 `BitConverter.Int32BitsToSingle` 直接位重解释**（规避字节序坑），再经缩放系数与小数位处理 |
| `ModbusDataCache` | 两级 `ConcurrentDictionary<int, ConcurrentDictionary<int, double>>`（DeviceId → DataPointId → 最新值），线程安全读写 |
| `ModbusService` | 轮询主引擎：加载配置 → 初始化端口 → 分组调度 → 写缓存，支持运行时热重载与统一 Dispose |

**关键技术设计**：

1. **轮询调度策略——"跨口并行、同口串行"**：设备按主站 COM 口 `GroupBy` 分组，不同串口之间的轮询任务并行执行（多任务），同一 RS485 总线上的设备严格串行请求 —— 与 RS485 半双工物理约束对齐，既提高吞吐又避免总线冲突。

2. **寄存器分组合并读取**：将测点按两个条件分组——寄存器类型一致（决定功能码 03/04）且地址连续（当前测点地址 = 上一测点地址 + 其占用寄存器数）。Int32/Float 占 2 个寄存器，`GroupLength` 按寄存器数量（非测点个数）计算。一次请求读回整段寄存器数组，读取器通过"组内偏移 = 测点地址 - 组起始地址"定位切片解析，**显著减少串口请求次数**。

3. **端口池管理（ModbusPortManager）**：
   - `ConcurrentDictionary` 维护两个池：SerialPort 池 + IModbusSerialMaster 池（均按 COM 口名索引）——用并发字典而非 Dictionary+lock，解决轮询与配置重载并发访问抛 `InvalidOperationException` 的问题
   - 每个主站串口只打开一次（幂等检查防 IOException）；单个端口打开失败只记日志继续，不拖垮整个采集服务
   - `Dispose` 按序释放：先 Master 再 SerialPort 再清缓存，防止访问已释放对象
   - setting.json 的 `PortMappings` 存"从站端口 → 主站端口"，与代码使用方向一致，避免无谓反转

4. **实时缓存（ModbusDataCache）——"同一引用、两扇门"**：
   - 写路径 `UpdateDataPoint` 引用穿透直改内部字典，零拷贝高效写
   - 内部读 `GetDeviceData` 返回 `IReadOnlyDictionary` 活引用（实时、零拷贝，编译期阻断写操作）
   - 跨边界读 `GetAllData` 返回深拷贝快照（隔离、一致性视图），供报警检测等后台服务消费

5. **配置热重载**：系统设置页保存后调 `POST api/system/modbus/reload`，引擎在运行时重新加载设备/测点/端口配置，**不重启服务**；轮询主循环空设备时也保持运转，保证重载后自动恢复采集。

### 6.6 后台服务（Background/）

**AlarmDetectionService（报警检测，1 秒周期）**：
- 每秒从 ModbusDataCache 拉全量快照，测点阈值（AlarmHigh/AlarmLow）比较生成高限/低限报警
- **优化**：一次全量加载测点按设备 `ToLookup` 分组，避免逐设备查库（每秒 N 条 SQL → 1 条）
- **报警去重**：同一测点、同一类型存在未确认报警时不重复生成
- **分级算法**：按越限偏差占量程比例判定等级——偏差 >50% Critical、>20% Major、>10% Minor，兜底 Major
- **快照设计**：报警记录同时存触发时的实时值 `Value` 与阈值 `Threshold`，测点配置日后修改不影响历史报警可读性
- 异常处理：`OperationCanceledException`（正常停机）与一般异常分开捕获；错误退避用 `CancellationToken.None`（避免停机时 catch 块内二次抛取消异常）；`Task.Delay` 放在 try 内保证取消可被感知

**DataArchiveService（历史归档，5 秒周期）**：
- 每 5 秒从缓存批量写入 DeviceData 历史表
- 按 setting.json 的 `DataRetention`（DeviceDataDays/AlarmDays）定期清理过期数据，控制表体积

后台服务均为单例，访问 Scoped 仓储时用 `IServiceScopeFactory.CreateScope()` 手动建作用域，避免 DbContext 并发问题。

### 6.7 系统配置（SystemConfigService + setting.json）
- 运行时配置集中在 setting.json：Modbus 串口参数（BaudRate=9600、DataBits=8、StopBits=One、Parity=None、读写超时 500ms、轮询间隔 1000ms）、端口映射、数据保留天数
- 配置加载后在内存中**扁平化为点分键**（如 `Modbus.BaudRate`），读取走内存缓存、保存直接写回 JSON 文件
- 应用启动时（`app.Run()` 前）fail-fast 预加载，保证后台服务与 API 拿到的配置始终可用

### 6.8 日志（Serilog）
- 结构化日志，控制台 + 按天滚动文件双 Sink
- `MinimumLevel.Override("Microsoft.EntityFrameworkCore.Database.Command", Warning)` 压制 EF Core SQL 明细日志
- 系统设置页可直接查看最近日志（SystemController 读取日志文件，处理了文件被 Serilog 独占的共享读写问题）

## 七、WPF 客户端详解（Client）

### 7.1 Prism 引导（App.xaml.cs）
- 继承 `PrismApplication`（DryIoc 容器），`CreateShell()` 以 **LoginView** 为启动窗口
- `RegisterTypes()` 注册单例服务层、`RegisterForNavigation` 注册导航视图
- `ConfigureModuleCatalog()` 加载 `DeviceManagementModule`（模块化）

### 7.2 登录与窗口切换（LoginView → ShellView）
- LoginViewModel 读取 PasswordBox 调用 `IAuthService.SignInAsync()`，成功后交给 **ShellService.LoginToShell()**：
  - `Container.Resolve<ShellView>()`（容器解析而非 new，保证 RegionManager 等依赖注入生效）
  - 关闭当前登录窗口 → `RegionManager.SetRegionManager(shellView, _regionManager)` → 替换 `Application.Current.MainWindow` → Show
- Token 由 **TokenStore** 保存：`lock` 保护读写，线程安全存取 AccessToken/RefreshToken

### 7.3 Shell 布局与区域导航
- ShellView：左侧 ListBox 菜单 + 右侧 `ContentControl`（`RegionManager.RegionName="MainRegion"`）
- ShellViewModel：`ObservableCollection<MenuItemModel>` 驱动菜单；`SelectedMenuItem` **TwoWay 绑定**（否则 ListBox 选中不触发 setter，导航失效），变更时 `RequestNavigate("MainRegion", viewName)`
- 导航遵守 Prism 规范：视图含 `prism:ViewModelLocator.AutoWireViewModel="True"`、导航名与注册的 View 类名一致、构造函数内不做导航（依赖属性 setter 触发）

### 7.4 HTTP 基础设施（项目亮点）
**HttpClientBase（统一请求基类）**：
- 请求前自动附加 `Authorization: Bearer` 头
- **401 自动无感续期**：收到 401 → 调 `api/auth/refresh` 刷新 Token 对并更新 TokenStore → **重放原请求**，用户完全无感知
- 统一反序列化 `ApiResult<T>`、处理 BaseUrl 拼接（踩过 BaseAddress 末尾斜杠坑）、JSON 泛型推断等细节

**前端服务层（Service/）**：11 个接口 + 实现一一对应后端控制器（Realtime/History/Alarm/Auth/Device/ProductionLine/System/Shell 等），ViewModel 只依赖前端服务接口，不直接碰 HttpClient —— 客户端内部也保持了 MVVM 下的分层。

### 7.5 页面功能明细

**实时监控页（RealtimeView / RealtimeViewModel）**：
- 产线-设备两级 **TreeView**（`HierarchicalDataTemplate` 渲染 `ProductionLineTreeNode`/`DeviceTreeNode`），并发加载产线与设备数据组装树
- 选中设备后展示测点数值卡片（值/单位/报警状态）
- **DispatcherTimer 每 2 秒**轮询刷新实时数据（`LoadDeviceDataAsync(initializeTrend: false)` 首次初始化趋势、后续只追加）
- **LiveCharts2（CartesianChart）** 实时趋势曲线：ComboBox 选择测点切换曲线，自定义 X/Y 轴（踩过 LiveCharts2 的 Axis/Separators/Labelers API 变更坑）

**报警中心（AlarmView / AlarmViewModel）**：
- 分页 + 按状态/等级筛选，确认报警（PUT acknowledge）
- 踩坑记录：SelectedStatus setter 字段写错导致筛选失效、URL 末尾斜杠导致服务端参数解析失败等（详见问题日志 Day 6）

**历史趋势页（HistoryView / HistoryViewModel）**：
- 设备/测点/时间段选择 → 趋势图（LiveCharts2）+ 统计值（最大/最小/平均）
- CSV 导出（后端生成带 BOM 的 UTF-8 CSV，前端触发下载）
- 踩坑记录：前后端查询参数名不一致（pointId vs dataPointId）导致曲线空白、EPPlus 8 许可证异常后改用 CSV 零依赖方案

**系统设置页（SettingsView / SettingsViewModel）**：
- 编辑 Modbus 串口参数/端口映射/数据保留天数，保存后写回 setting.json 并可触发热重载
- 查看系统运行信息与最近日志

### 7.6 模块化：设备管理模块（Modules/DeviceManagement）
- 独立 Prism 模块（IModule），`RegisterForNavigation<DeviceListView, DeviceListViewModel>` 注册列表页
- `RegisterDialog<DeviceEditView, DeviceEditViewModel>` 注册**对话框式编辑页**（实现 Prism 9 的 `IDialogAware`，适配过 9.0 相对 8.0 的接口变化）
- 设备 CRUD 全流程：列表（DataGrid，统一 DataGridStyle 样式）→ 新增/编辑对话框（串口、从站地址、所属产线 ComboBox 绑定）→ 删除确认
- 踩坑记录：AsyncDelegateCommand 空引用、EF Core DbContext 并发访问、外键约束冲突、XAML 手动 new 有参 ViewModel 抛 NRE 等（问题日志 Day 4）

### 7.7 UI 与样式
- HandyControl 控件库 + 全局 `DataGridStyle.xaml` 统一表格样式
- Microsoft.Xaml.Behaviors.Wpf 在 XAML 中绑定事件与命令（View 零代码后置）

## 八、关键技术难点与解决实录（精选）

### 后端
| 问题 | 原因 | 解决 |
|---|---|---|
| Modbus Float 解析出错误大负值 | `Array.Reverse` 字节序操作在小端系统上错乱 | 改用 `BitConverter.Int32BitsToSingle` 直接位重解释 32 位寄存器组合 |
| 轮询与配置重载并发抛 InvalidOperationException | 端口池用 Dictionary 并发读写 | 改 `ConcurrentDictionary`，并配合幂等开端口与统一 Dispose 顺序 |
| 解析结果恒为 0 | ScaleFactor=0 或地址/类型读错 | Debug 输出 rawValue 与 ScaleFactor 快速定位；完善范围校验 |
| SQL Server 多重级联路径错误 | EF Core 必选关系默认 Cascade | 对 Alarm/DeviceData 等关键子表显式 `DeleteBehavior.Restrict` |
| 种子数据迁移报错 | HasData 中调用 BCrypt.HashPassword / DateTime.UtcNow 导致模型哈希漂移 | 种子值改为固定常量 |
| Modbus SlaveException: Illegal Data Address | 请求了从站不存在的寄存器区间 | 分组算法保证区间有效 + 读取重试退避 + 单组失败隔离 |
| 后台服务停机异常终止 Host | TaskCanceledException 未捕获逃出循环 | 区分 OperationCanceledException（正常）与一般异常（记日志退避），退避 Delay 不带 token |
| 读日志文件 IOException | Serilog 独占写 | 调整文件共享模式读取 |

### WPF 客户端
| 问题 | 原因 | 解决 |
|---|---|---|
| ListBox 点菜单不导航 | SelectedItem 只绑定 get 未触发 set | 改 TwoWay 绑定，setter 中触发 RequestNavigate |
| 导航后 MainRegion 空白 / RegionManager 未绑定 | 手动 `new ShellView()` 绕过了容器 | 统一 `Container.Resolve<ShellView>()` + `RegionManager.SetRegionManager` |
| 登录后窗口切换异常 | Shell 基类不匹配 / HandyControl Window 命名空间歧义 | 明确窗口类型引用，规范窗口创建流程 |
| 设备编辑对话框异常 | Prism 9 IDialogAware 接口变化、async void 异常安全 | 适配 9.0 接口，OnDialogOpened 异步安全处理 |
| LiveCharts2 图表不显示/命名空间报错 | 2.x API 与资料不匹配（Axis/Separators/Labelers） | 按 2.0.5 官方 API 重写轴与序列绑定 |
| 趋势图无曲线 | 前后端查询参数名不一致 | 统一参数契约（dataPointId） |
| Excel 导出抛 LicenseNotSetException | EPPlus 8 新许可证 API | 方案演进：EPPlus → **CSV（手写转义 + UTF-8 BOM），零第三方依赖** |
| 401 后请求失败 | Token 过期无处理 | HttpClientBase 拦截 401 自动刷新并重放原请求（无感续期） |
| URL 末尾斜杠导致参数解析失败 | 拼接路径带 / | 规范化前端服务层 URL 拼接 |

> 完整记录见《问题日志.md》（85 条）、《复盘笔记.md》（后端知识点：EF Core 导航/索引/级联、JWT 双 Token、AsNoTracking、Modbus 主从模型与 NModbus 分层）。

## 九、工程实践与编码规范亮点

- **三层架构 + 依赖注入**贯穿前后端：客户端也维护了 Service 接口层，ViewModel 零 HttpClient 依赖
- **线程安全意识**：ConcurrentDictionary 端口池/缓存、lock 保护的 TokenStore、跨口并行同口串行调度
- **性能设计**：寄存器分组减少串口请求、缓存零拷贝读与深拷贝快照分级、报警检测 SQL 合并（N→1）、bigint 主键 + 复合索引应对 518 万条/天写入、AsNoTracking 只读查询、唯一/复合索引加速查询
- **可靠性设计**：全局异常中间件、后台服务异常分级与退避、单组读取失败隔离、端口打开失败不拖垮服务、fail-fast 配置预加载、热重载不重启
- **数据可追溯性**：报警快照字段（Value+Threshold）、RefreshToken 轮换与撤销记录、按天滚动日志
- **开发过程管理**：独立维护开发日志（Day 1-8）、问题日志（85 条）、复盘笔记、数据库设计文档——习惯把踩坑沉淀为工程结论

## 十、给简历写作 Agent 的指引

1. **最大差异化亮点**：Modbus RTU 工业采集引擎——"跨口并行、同口串行"调度、寄存器分组合并读取、两级并发缓存、热重载。多数候选人没有工业协议/上位机经验，这应作为简历核心卖点。
2. **次级亮点**：JWT 双 Token 无感续期（前后端闭环）、LiveCharts2 实时趋势 + DispatcherTimer 轮询、Prism 模块化 + 对话框服务、复合索引 + Restrict 级联的数据库设计、CSV 零依赖导出方案演进。
3. **可量化素材**：~10,300 行代码、7 张表、30+ 端点、2 个后台服务、1 秒报警检测/5 秒归档/2 秒前端刷新、518 万条/天历史数据量估算、85 条问题记录、8 个开发日。
4. **注意事项**：
   - 《复盘笔记》只覆盖了 Server 端，**WPF 端经历请引用《问题日志》Day 4-8 与《开发日志5-8》**，不要声称"复盘笔记覆盖全栈"
   - 报警"分级"是按偏差比例的规则判定（非机器学习）；用户角色枚举已定义但权限粒度有限，简历措辞避免夸大为"完整 RBAC"
   - 后端为 .NET 10 / 客户端 net10.0-windows，如目标岗位偏保守可表述为".NET (6+ 现代版本特性)"
   - 面试可展开的追问素材已沉淀在《简历项目描述.md》，两份文档可配合使用
